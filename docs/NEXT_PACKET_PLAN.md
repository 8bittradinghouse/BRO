# Next Packet Plan

## Purpose
Keep the active BRO engineering order explicit without broad strategy drift or live-readiness overclaim.

This document now serves two jobs:
1. preserve the immediate active pickup point,
2. preserve the next macro lanes so they do not get lost after maintenance or side packets.

## Current Pickup Addendum (2026-05-21)
- latest committed checkpoint is now:
  - `e24fc34`
  - `docs: refresh current truth and pickup anchors`
- `af00d02` remains the last clean maker/taker hardening checkpoint described
  by older pickup surfaces; it is not the current present-tense body
- packet body is now:
  - dirty / in-flight
  - not yet reclosed to a clean commit boundary
- the active immediate pickup is no longer emergency Packet 2 surgery cleanup
- current active next move is:
  - truth-routing sync on broad truth / board / pickup surfaces
  - stale Packet 2 residue extinction
  - Packet 3 wallet doctrine lock and surgical cut-plan sync
  - no Packet 3 runtime/code slice opened yet
- strongest health anchor to use first is:
  - `b6336854-b2f6-44a7-862a-71b41b6ac60f`
  - maker and taker both alive
  - lifecycle clean
  - selector families behaving as gates, not obvious system choke
- quiet-time contrast anchor to use carefully is:
  - `c519e785-598c-4cd1-83af-51f0c37592b5`
  - healthy runtime, low-opportunity shoulder specimen
  - do not overread no-submit maker there as lane failure
- blueprint-defined maker/taker tuning alignment remains downstream once the
  routing layer is reclosed cleanly again
- do not reopen the prior depth / one-sided panic lane from reject volume alone
- if the next thread sees a loud gate family, force the SOP first:
  1. is the lane alive?
  2. is it producing valid action and some success?
  3. only then ask whether the gate is defective or correctly filtering trash

Master-guide continuity note:
- broader `BRO` restoration -> surgery -> `Barbatos` promotion planning may
  also be carried in external shop continuity, but that external copy does not
  govern BRO-local truth or lane order.
- inside `BRO`, this file preserves the active pickup point and lane history,
  while current BRO authority remains with:
  - `docs/PROJECT_TRUTH_STATE.md`
  - `docs/BRO_GFRAME_CORE_FIGHTER_AUDIT_2026-05-01.md`
  - `docs/BRO_GFRAME_CORE_RESTORATION_PACKET_PROGRAM_2026-05-01.md`

G-frame override:
- before any weapon-primary work, use the core-fighter packet program at:
  - `docs/BRO_GFRAME_CORE_RESTORATION_PACKET_PROGRAM_2026-05-01.md`
- completed packet artifacts now exist at:
  - `docs/BRO_GFRAME_PACKET_1_BONES_RELEASE_TRUTH_LOCK_2026-05-01.md`
  - `docs/BRO_GFRAME_PACKET_2_SPINAL_CORD_FAILURE_CHAIN_2026-05-01.md`
  - `docs/BRO_GFRAME_PACKET_3_RACK_TRUTH_SYNC_CONFIRMATION_2026-05-01.md`
  - `docs/BRO_GFRAME_PACKET_4_GRIP_WALLET_AUTHORITY_CLOSURE_MAP_2026-05-01.md`
  - `docs/BRO_GFRAME_PACKET_5_BRAIN_SEMANTIC_OWNERSHIP_CLOSURE_MAP_2026-05-02.md`
  - `docs/BRO_GFRAME_PACKET_6_NERVOUS_SYSTEM_CONSUMER_TRUTH_CLOSURE_MAP_2026-05-03.md`
  - `docs/BRO_GFRAME_PACKET_7_GFRAME_REGRADE_AND_WEAPON_AUTHORIZATION_GATE_2026-05-03.md`
- current maker diagnostics remain valid, but they are presently classified as
  core-frame forensic truth, not weapon-optimization authorization
- if older macro sections below read weapon-forward, this override outranks
  them until the live BRO-local truth screen and board sink say otherwise
- active next-step authority for G-frame work now lives in:
  - `docs/BRO_GFRAME_CORE_FIGHTER_AUDIT_2026-05-01.md`
  - `docs/BRO_GFRAME_CORE_RESTORATION_PACKET_PROGRAM_2026-05-01.md`
- completed packet artifacts for Packet 1, Packet 2, Packet 3, Packet 4,
  Packet 5, Packet 6, and Packet 7 remain historical closure and
  defect-lineage support only
- historical demotion rule:
  - all completed `BRO_GFRAME_PACKET_*` artifacts are important historical
    closure and defect-lineage records
  - they do not independently own the current `pilot_live` pickup
  - they do not outrank the active `pilot_live` board/program/packet owners
- Packet 5 operator-facing semantic rule:
  - this file is a pickup surface only
  - it does not own runtime Brain semantics
  - doctrine roots and the audit board sink outrank it
- Packet 6 operator/gate downstream rule:
  - this file is a pickup surface only
  - it does not own Nervous System truth
  - emitted runtime truth, Packet 6 findings, and the audit board sink outrank
    it
- G-frame bounded-section status:
  - `COMPLETE` on existing evidence and board-sync updates
  - Packet 4 completed as its own bounded work section
  - Packet 5 completed as its own bounded work section
  - Packet 6 completed as its own bounded work section
  - Packet 6 closeout hardening completed on existing packet, board, and pickup
    surfaces
  - Packet 7 completed as its own bounded work section
  - Packet 1-5 critical inspection closeout completed on existing packet, board,
    and pickup surfaces
  - no new G-frame artifact class or helper layer was justified by that
    inspection
  - the seven-packet G-frame program is complete as a handoff-grade board-call
    block
  - no further serial G-frame packet remains inside this program
- thread-transfer snapshot:
  - latest completed post-restoration hardening lane:
    - `timing spine hardening` achieved on
      `4b60bf3e-63c9-4fb0-a47d-69cfb76216d0`
  - current post-restoration proof frontier:
    - `pilot_live` authority proof
  - active macro lane inside that frontier:
    - `live trust qualification`
  - Packet 1 is now closed `bounded-live-test ready`; Packet 2 Maker-Live is
    the next implementation lane to open and downstream packets remain
    sequenced behind it
  - current achieved packet-scope closures that a new thread must not silently
    reopen:
    - Packet 1 clean current-code release-anchor closure achieved on
      `7bbde42c-003a-4f57-b59a-7ce138224075`
    - Packet 4 Grip current-code paper-stage truth closure achieved on
      `33e30bd8-e416-488e-83ce-f99c8665e7fc`
    - Packet 5 Brain source-layer mutation closure achieved on `656c...`
    - Packet 6 Nervous-system consumer-truth closure achieved on `13fd...`
  - weapons remain diagnostic-only for tuning/aggression work; maker and
    taker live-trust qualification are authorized as diagnostic proof
    work
  - current proving move:
    - Packet 2 `Maker-Live / Economic Trust Qualification` is now active in
      post-audit truth-owner / proof-semantics closeout through:
      - `docs/BRO_PILOT_LIVE_TRUST_PACKET_2_MAKER_QUALIFICATION_2026-05-10.md`
      - `docs/BRO_PILOT_LIVE_TRUST_QUALIFICATION_PACKET_PROGRAM_2026-05-05.md`
      - `docs/BRO_PILOT_LIVE_TRUST_BOARD_SINK_2026-05-05.md`
      - `docs/PROJECT_TRUTH_STATE.md` as the broad repo truth screen
      - `docs/OPEN_LIMITATIONS.md`
    - completed packet-entry hardening already materialized:
      - maker authority census
      - doctrine proposal delta map
      - current-code maker path forensic semantic audit
      - maker timing owner layer
      - support-tool vs runtime-owner split
      - history-only demotion map
      - small-loss + scar-tissue board
      - maker surgery self-hardening pack
    - completed closeout slices now materially landed:
      1. truth-owner demotion + Packet 2 pickup reroute
      2. starvation proof correction + steady-state WS grading repair
      3. lifecycle ownership-entry / maker timing collision closure
      4. selector-owned one-sided extinction + rerun
      5. diagnostic-surface hardening plus websocket / maker-submit false-blocker repair
    - strongest current provisional findings are:
      - lifecycle ownership-entry law is now current-code steel:
        - `lifecycle.selection.max_sec_to_expiry=90.0`
        - watched canon specimen `4f843da4-16ec-4616-948c-c7b19e7f5aea`
          kept owned markets inside the final `90s`
      - fresh watched specimen `187f3031-80da-4744-be2b-923e2731dd8a` narrowed
        the remaining live maker friction:
        - selection depth multiple remains `keep-now steel`
        - queue-depth quote quality remains `keep-now steel`
        - accessory sizing feasibility is now the narrower active patient
      - queue pressure current/live authority is cut; only legacy-config
        ignored compatibility and historical replay lineage remain
      - recovery / unwind current-owner cut is materially landed; remaining old
        names now live in compatibility readers, ignored dead-key support, and
        historical lineage/docs rather than active runtime/report authority
      - the optional later history/compat extinction artifact for that tail is:
        - `docs/BRO_PILOT_LIVE_TRUST_PACKET_2_RECOVERY_UNWIND_HISTORY_COMPAT_EXTINCTION_PACKET_2026-05-14.md`
      - cancel-only fail-close for open unfilled orders and hold-to-settlement
        for real accepted exposure are the replacement doctrine targets
      - historical packet-era `50-60 / 50` maker timing posture belongs to
        dirty-tree run manifests and is ancestry only, not current doctrine
      - official Polymarket WS/CLOB substrate transplant is materially landed
        and no longer the active maker patient
      - canonical lifecycle surgery is materially landed and no longer the
        active maker patient
      - current canon specimen `4f843da4-16ec-4616-948c-c7b19e7f5aea`
        stayed `VALID_ACTIVE`, fired maker and taker, and current-code replay
        closed the archived gate-accounting false blockers
      - the only remaining hard-fail on the current-code replay of that
        specimen is real execution-quality pressure:
        - `soak_execution_quality_capture_minus_adverse_too_low`
      - fresh watched specimen `187f3031-80da-4744-be2b-923e2731dd8a`
        stayed `VALID_ACTIVE` and current runtime blocker truth now chains
        through `maker_blocker_ledger.json`
      - the old recovery-tied small-loss family appears cut on current code:
        - current canon losses are discrete order outcomes under a deliberately
          detuned test profile
        - `outcome_truth_audit.lane_outcome_truth.*.lifecycle_residue_records=0`
        - reopen only if the old repeated tiny recovery-style bleed pattern
          returns
      - the active Packet 2 move is now surviving-blocker remeasurement and
        hostile queue closeout, not more timing surgery
      - current surgery queue is:
        1. `support-shadow / probe family` truth cleanup
        2. `accessory competitiveness bundle` zero-authority hardening on
           `paper_universal`
        3. `accessory maker sizing scaler bundle` zero-authority hardening on
           `paper_universal`
        4. `final trust / falsifier proof` after the blocker queue is honest
      - recovery / unwind current-owner cut is already landed; any remaining
        archaeology is optional history/compat cleanup, not a required
        live-owner surgery gate
    - the materialized maker-local board stack now lives under:
      - `docs/BRO_PILOT_LIVE_TRUST_PACKET_2_MAKER_*_2026-05-10.md`
    - treat `prelive_gate` / `live_canary` as bounded tools later in the lane,
      not the final authority by themselves
    - require every packet in the lane to carry:
      - forward semantics sweep
      - reverse semantics sweep
      - contradiction compression pass
      - negative-proof pass
      - combat timing doctrine verification
      - gate legitimacy sweep
      - packet-entry tribunal outputs before implementation starts
      - Packet 1 restoration replay:
        - reload current board owners and doctrine roots first
        - trace doctrine -> runtime -> validators -> reports -> operator
          consumers -> back to the strongest owner
        - classify surfaces as owner-law, valid interlock, compatibility,
          historical-only, or scar tissue
        - preserve the no-change list before any slice lands
      - for runtime surgery packets, force repeated self-hardening at packet
        entry, after owner mapping, after plan hardening, after watched proof,
        and before each next-cut or closeout recommendation
      - maker runtime surgery entry now starts through:
        - `docs/BRO_PILOT_LIVE_TRUST_PACKET_2_MAKER_SURGERY_SELF_HARDENING_PACK_2026-05-11.md`
    - no maker timing/gating mutation or generic weapon tuning by default unless
      a fresh contradiction is proven
  - current saved branch / cleanliness state:
    - `consultant/full-snapshot-public-20260402T055838Z`
    - pushed
    - verify live cleanliness with `git status`; older clean-tree statements
      below are historical continuity records, not present-tense authority

Historical-lane archive rule:
- remaining sections below may preserve continuity and comparative context
- they do not independently authorize the current primary lane
- they do not outrank the G-frame override, the audit board sink, or the packet
  program
- older sections may use then-current words like `current`, `next`, or `ready`
  for their own dated moment; unless reflected in the top active section, read
  them as historical archive only
- current next move after G-frame Packet 7 is:
  - re-enter the macro father-frame restoration road using the Packet 7 board
    call
  - Spinal Cord current-code canonical paper-stage closure is now achieved on
    `8a389b34-5df7-4d78-a750-3e2b909f17c8` and reconfirmed on fresh live
    specimen `d30b1c7c-ab05-494e-bfd1-3c5ac1205051`
  - fresh live Packet 3 deep red-team specimen
    `13fd56b5-3f12-48ec-a07d-04b7d83d07ac` hardens that same rack call:
    - `highest_passing_stage=paper`
    - `blocking_stage=pilot_live`
    - rack remains an honest proving witness even with bounded harness/soak
      realism-pressure findings on the same run
    - the bounded realism split remains descriptive-only (`100` canonical
      `paper_harness_audit.harness_realism_grade` vs nightly
      `exercised_harness_realism.grade=60`)
    - older whole-stream source-mix hard-fail contract remains historical-only
      lineage and is not restored as the current nightly canonical grade owner
    - rack is not the active next restoration lane; later post-packet board
      sync moves the current next restoration lane to Bones clean current-code
      release anchor
  - fresh live Packet 4 contradiction specimen
    `ed44e26c-c52d-4003-9a41-464d5d528ff9` proved the remaining paper-mode
    submit-readiness drift:
    - `wallet_startup_authority_refresh.order_submit_eligible=true`
    - `wallet_health_gate.allowed=true`
    - `order_submit=3`
    - downstream `wallet_state_refresh` / nightly wallet contract still carried
      `order_submit_eligible=false`
  - fresh live Packet 4 closeout specimen
    `33e30bd8-e416-488e-83ce-f99c8665e7fc` now closes that drift:
    - paper wallet authority remains authoritative and healthy
    - `order_capable_live=false`
    - `order_submit_eligible=true`
    - `canonical_live_nonce_available=false`
    - `canonical_live_pending_wallet_tx_available=false`
    - local/open-order wallet surfaces stayed explicitly non-canonical
    - `wallet_health_gate.allowed=true`
    - `wallet_authorization=1`
    - `order_submit=1`
    - `fill=3`
    - `wallet_reservation_released=1`
    - reservation mismatch stayed false
  - historical Packet 1 closeout specimen
    `4494f47e-9c0d-4ab0-80a3-141588388446` remains the host-time and contract-role
    hardening contrast anchor:
    - release-lineage chain closes cleanly on the earlier dirty current-code
      anchor
    - host-time authority reads cleanly on that specimen:
      - session-start / session-stop artifacts report synced via
        `timedatectl`
      - rolling status rows remain non-authoritative
        `clock_state=partial_visibility`
  - fresh clean Packet 1 release-anchor specimen
    `7bbde42c-003a-4f57-b59a-7ce138224075` now closes the missing
    clean-current-code anchor lane:
    - clean snapshot commit
      `24e8dcaa471f8651a5e9231fdf3564026d4294b0`
    - current live repo runtime fingerprint matches the clean anchor exactly:
      `492ea0b757623c0a9dade4a333c0bf743dcd05ffbaa8a3c34b24c16a22313ede`
    - `canonical_paper_validation.json` says:
      - `status=pass`
      - `highest_passing_stage=paper`
      - `blocking_stage=pilot_live`
      - `runtime_classification=VALID_ACTIVE`
      - `promotion_eligible=true`
      - `recommended_next_stage=pilot_live`
    - `paper_harness_audit.json` says:
      - `ok=true`
      - `finding_count=0`
    - `soak_hardening_gate.json` says:
      - `ok=true`
      - one non-blocking finding:
        - `soak_maker_submits_too_low:2.000000<min:50.000000`
    - clean-anchor core-fighter re-audit on this same specimen says:
      - `readiness_gate.runtime_findings=[]`
      - `execution_starvation_mode=none`
      - `maker_selection_authority_audit.current_decision_distribution={"selection_rejected":8,"submit_rejected":16,"submitted":2}`
      - `maker_quote_integrity_summary.next_repair_lane="D. Peak-hours confirmation specimen"`
      - `peak_hours_economic_conclusion_allowed=false`
    - this specimen closes Bones clean current-code release-anchor work,
      retires the active maker-core choke reading on the clean paper specimen,
      and moved the next macro frontier first to post-restoration timing spine
      hardening, now closed on current proof, then to `pilot_live` authority
      proof
  - Brain mutation closure is now source-closed on current code on fresh live
    specimen `656c9d42-070c-4f82-84cf-34aa333a9e7f`
  - Nervous-system consumer-truth closure is now materially closed on fresh
    downstream specimen `13fd56b5-3f12-48ec-a07d-04b7d83d07ac`
  - latest completed post-restoration hardening lane is
    `timing spine hardening`
  - current next macro proof frontier is `pilot_live` authority proof
  - watched peak-hours canonical paper confirmation is now materially achieved
    on current proof and is no longer the default next proving move
  - active macro lane on that frontier is `live trust qualification`
  - the lane is now governed by:
    - `docs/BRO_PILOT_LIVE_TRUST_QUALIFICATION_PACKET_PROGRAM_2026-05-05.md`
    - `docs/BRO_PILOT_LIVE_TRUST_BOARD_SINK_2026-05-05.md`
  - first packet inside that lane is `Taker Live / Economic and Firing
    Trust Qualification`
  - active packet artifact:
    - `docs/BRO_PILOT_LIVE_TRUST_PACKET_1_TAKER_QUALIFICATION_2026-05-06.md`
  - current Packet 1 verdict is explicit:
    - `bounded-live-test ready`
    - one-brain owner collapse is materially achieved on current code
    - recovery/handoff owner classification is now explicitly keep-now dead power
    - fresh watched closeout proof now exists on the stored bundle
    - Packet 1 now also serves as the live-trust restoration reference method
      for later `pilot_live` packets:
      - restore before tuning
      - runtime truth over wrapper-green
      - history as ancestry only
      - one owner map before closure
  - that first packet must force a full taker inherited-lineage split-brain map from core
    doctrine out through branch/contact surfaces and back into the true owner
  - bounded `prelive_gate` / `live_canary` work remains a proving tool inside
    the lane without default maker timing mutation
  - fixed downstream lane order after Packet 1 is now:
    1. Packet 2 `Maker-Live`
    2. Packet 3 `Grip-Live`
    3. Packet 4 `Bounded Live Hookup`
    4. Packet 5 `Immediate-Performance Proof`
  - later pilot-live proving split must stay explicit:
    - maker/taker packet proof = execution-lane trust
    - pre-launch hostile detuned long runs = capital-safety stress trust
    - pre-launch blueprint-tuned long runs = operational endurance trust
    - those are separate go/no-go lanes and must not be collapsed into one
      “feels ready” claim
  - active Packet 2 maker entry artifact is:
    - `docs/BRO_PILOT_LIVE_TRUST_PACKET_2_MAKER_QUALIFICATION_2026-05-10.md`
  - active Packet 2 recovery / unwind closeout artifact is:
    - `docs/BRO_PILOT_LIVE_TRUST_PACKET_2_RECOVERY_UNWIND_ROOT_DEATH_SURGERY_PLAN_2026-05-13.md`
  - Packet 2 relock law:
    - treat maker as a capital-trust packet, not a participation vanity packet
    - run recurring self-hardening after every 2 investigation slices, after
      watched runtime proof, before any implementation plan, and before any
      closeout recommendation
    - restate mission lock, no-change list, strongest owner map,
      support-tool boundary, history quarantine, strongest falsifier list, and
      residual risk register on each hardening pass
    - keep support tools useful but fenced, keep history historical-only, and
      prefer family-cut surgery over leaf-by-leaf residue cleanup once owner
      truth is fully mapped
  - later repo hygiene lane after current pilot-live foundation:
    - `schoolhouse clearing / deauth`
    - schoolhouse means external VPS toolbox/study material with zero BRO
      authority
    - active board sinks, packet owners, and doctrine roots stay with BRO
    - clear/deauth this only at the proper point after live-trust foundation
      and final doctrine-law audit, not by derailing the current runtime lane
  - fresh watched post-restoration contrast specimen
    `6e2826a6-d1bf-4cd5-8d18-2846e86b8db1` reaffirms that lane without
    reopening Bones:
    - `runtime_classification=VALID_ACTIVE`
    - all validators except `soak_hardening_gate` exited `0`
    - `highest_passing_stage=none`
    - `blocking_stage=paper`
    - blocking findings were:
      - `quote_uptime_ratio=0.04753138846848339 < 0.05`
      - `maker_submits=2 < 50`
    - `maker_selection_authority_audit.current_decision_distribution={"submit_rejected":2,"submitted":2}`
    - `maker_zero_submit_root_cause_audit.zero_submit_classification=upstream_starvation`
    - `maker_quote_integrity_summary.next_repair_lane="D. Peak-hours confirmation specimen"`
    - this is `pilot_live` authority evidence debt on an overnight specimen,
      not a renewed core-frame choke
  - consume Packet 4 through Packet 6 mappings directly
  - keep diagnostic ownership on runtime/doctrine events, canonical validation,
    and watched current-code tape
  - current Brain evidence set is now historical pre-cut proof plus fresh
    closure proof:
    - `8a389...` proved `3` late-window rewritten rows
    - fresh live specimen `7b4cabf6-fa6b-4d97-8ffb-3c7885547822` proves the
      nightly report mutation is still live and hits both duplicated branches:
      - late-window probe rewrites `2` rows from runtime
        `unknown/not_confirmed` into report `authoritative/confirmed`
      - mid-window probe rewrites `4` rows from runtime
        `unknown/not_confirmed` into report `authoritative/confirmed`
      - all `6` rewritten rows are labeled
        `market_reference_basis=report_book_top_pair_backfill`
    - fresh live specimen `bc4bc73b-7dd4-4060-b44b-07dc0228aaa3` proves the
      mutation is still live on current code even when the active branch
      shifts:
      - late-window probe rewrites `0` rows
      - mid-window probe rewrites `6` rows from runtime
        `unknown/not_confirmed` into report `authoritative/confirmed`
      - all `6` rewritten rows are labeled
        `market_reference_basis=report_book_top_pair_backfill`
    - fresh live closure specimen `656c...` now proves the bounded source cut
      held on current code:
      - `maker_cannon_late_window_probe.jsonl`:
        - `row_count=78`
        - backfill rows=`3`
        - runtime-vs-report oracle deltas=`0`
      - `maker_mid_window_probe.jsonl`:
        - `row_count=62`
        - runtime-vs-report oracle deltas=`0`
      - raw event-to-probe replay on the same run keeps runtime
        `not_available/missing/unknown/false` truth unchanged on the late-window
        backfill rows
      - downstream harvest on the same run does not resurrect the old second
        dialect
    - do not reopen Brain by momentum unless a fresh live specimen reproves a
      real owner split
  - fresh live maker-band tape on `bc4...` did **not** reconfirm the stronger
    executor-vs-shadow split claim:
    - maker-scope `edge_evaluation` rows in `MAKER_TAKER_SELECTIVE` already
      carried direct-midpoint market-reference truth plus confirmed
      `secondary_oracle_*`
    - same-time null-oracle duplicates were taker-scope
      `phase_disallow_taker` rows
    - `maker_market_snapshot` matched the maker-scope rows on that
      specimen
  - keep `maker_market_snapshot` semantically narrow as a separate
    runtime-emitted selection-shadow surface
  - next Brain move is the shared nightly backfill/recompute cut, with runtime
    owner surfaces kept classified tightly enough that we do not promote shadow
    rows or taker-scope duplicates into a fake second owner
  - do not let downstream nightly/waterfall compression
    (`secondary_oracle_not_confirmed`) become the owner of any remaining core
    lane
  - do not open generic weapon tuning from this surface

## Maker Doctrine Correction
`VERIFIED`:
- several historical maker entries below were written under a drift-era runtime
  posture where maker timing was configured at `50-60s`.
- those entries are preserved as packet history, not as current maker doctrine.
- intended maker timing doctrine anchor is:
  - `docs/GALAXY_MEGA_MAKER_CANNON_DOCTRINE_PROPOSAL_2026-04-28.md`
  - maker gate opens at `15s`
  - taker handoff opens at `7s`

Rule for reading the historical maker entries below:
- `50-60s` references describe drift-era runtime posture or superseded packet
  reasoning only.
- they do not define current maker doctrine.

## Latest Maker Diagnostic Pickup
`VERIFIED`: the active maker pickup is now anchored on the May 12, 2026
truth-reference starvation before/after pair, not the older selector or lag
forensics alone.

Current runtime truth anchors:
- pre-cut specimen:
  - `9a9cddf4-3738-4f66-82e0-b0fbc1d679fc`
  - `status=policy_failed`
  - `runtime_classification=NON_PROMOTABLE_NO_PARTICIPATION`
  - `book_feed_ws_books_missing_all_targets` fired `2` times
  - maker active-band truth was `0` authoritative rows / `0` depth-met /
    `0` full-cannon candidates
  - maker active-band truth remained entirely one-sided / non-authoritative
- post-cut specimen:
  - `c56545a7-2183-4d3f-8405-9ad072dc7188`
  - `status=policy_failed`
  - `runtime_classification=NON_PROMOTABLE_NO_PARTICIPATION`
  - current code now fires a one-shot existing-path self-heal on
    `book_feed_ws_books_missing_all_targets`
  - that degraded WS cycle fired `1` time, not `2`
  - maker active-band truth improved to `16` authoritative rows / `3`
    depth-met / `3` full-cannon candidates
  - current watched proof does **not** prove the self-heal caused those `16`
    authoritative rows:
    - the authoritative rows were pre-reset
    - the reset immediately armed bootstrap grace in that specimen
  - run still earned `0` submits / `0` fills, so the packet moved the disease
    but did not close the lane
- post-one-sided-cut specimen:
  - `03cfeff3-ad41-4b65-9172-89e78d472b52`
  - `status=policy_failed`
  - `runtime_classification=NON_PROMOTABLE_NO_PARTICIPATION`
  - runtime direct-midpoint maker rows now promote into real
    `BUY_ONLY` / `SELL_ONLY` posture
  - `maker_selection_authority_audit.blocked_count_by_canonical_reject_reason={}`
  - the narrow authority-blind one-sided activation bug is cut
  - the older selector-owned low-edge one-sided reject family is retired from
    canonical maker selection authority
  - run still earned `0` submits / `0` fills
  - this specimen regressed to active-band one-sided / non-authoritative truth:
    - `0` authoritative rows
    - `1` depth-met
    - `0` full-cannon candidates

Current next maker lane:
1. `VERIFIED`: the active packet is now the post-audit truth-owner /
   proof-semantics closeout.
   - reroute active Packet 2 pickup away from treating
     `PROJECT_TRUTH_STATE.md` as the maker-local owner
   - keep starvation proof language honest
   - keep one-sided closure language narrow and doctrine-accurate
2. `VERIFIED`: the late-window truth-reference starvation packet has a landed
   runtime slice.
   - existing watch/resubscribe machinery is reused for the connected-but-
     empty target WS cycle
   - no same-token side-merge or doctrine drift was introduced
3. `VERIFIED`: the next highest-ROI maker-local runtime family after this
   closeout is back on truth-thin active-band rows:
   - `secondary_oracle_not_confirmed`
   - one-sided WS pair-truth with no authoritative maker reference
   - fail-closed maker reference missing / non-authoritative truth
   - residual `maker_requires_ws_book_source` /
     `taker_requires_ws_book_source`
4. `INFERRED`: support-shadow / probe mass should be de-fatted before any
   broader competitiveness or small-loss surgery, because the live runtime is
   now giving us cleaner owner truth than the stale packet wording, but not yet
   a clean causal proof that self-heal created the old `16` authoritative rows.
   - no new harness
   - no new profile
   - no selector fork
   - no broad cleanup bundled into the next lane

Historical selector-stabilization notes below remain packet history and
comparison context, not the active pickup anchor.

## Historical Archive Fence
Everything below this line is preserved for continuity memory only.

Hard reading rule:
- none of the sections below independently define the current primary lane
- none of the sections below authorize weapon-primary work
- use them as historical comparison terrain unless the G-frame board explicitly
  reactivates them

Current bedrock pickup:
- repaired `$250` specimen:
  `484e533d-c9a1-4ac4-bc0d-ce379c624e09`
- newest logic verdict:
  - certified maker quote: `0.874`
  - actual launched maker quote after post-only clamp: `0.961`
  - accepted submit, then next-cycle cancel under
    `launch_safe_selection_reject`
  - the current `ExecutionQualityModel` treats materially different
    inside-spread prices as effectively equivalent
  - the current resting-order survival path is being evaluated on a re-derived
    desired quote and then killed by next-cycle depth revalidation

Current next repair lane:
1. `VERIFIED`: `A. Quality-model repair` is now implemented.
   - code:
     `prodesk/execution_quality.py`
   - proof:
     the model now distinguishes deep passive vs touch-adjacent post-only
     quotes and penalizes huge size vs tiny visible depth instead of scoring
     those rows as high-quality fills
2. `VERIFIED`: current active repair lane is now `B. Quote-consistency repair`.
   - code:
     `prodesk/order_manager.py`
   - current truth from repaired live specimen
     `0e70ac1b-1eb0-4683-86a0-eb6ab8357ada`:
     - `2` maker submits
     - `2` partial maker fills
     - `2` cancel-remainders under `launch_safe_selection_reject`
   - why next:
     shadow/selection had been certifying a pre-clamp quote while runtime
     launched a post-only-safe clamped quote
   - repair law:
     shadow/selection now certify the submission-facing quote while preserving
     the raw strategy quote as lineage
3. `VERIFIED`: `C. Maker Commitment / No-Cancel Doctrine Repair` is now implemented.
   - code:
     `prodesk/models.py`
     `prodesk/tx_manager.py`
     `prodesk/gateway.py`
     `prodesk/order_manager.py`
     `scripts/order_lifecycle_audit.py`
     `scripts/nightly_soak_report.py`
   - doctrinal closure:
     accepted maker `GTC + post_only` orders now become explicit
     commitment-held orders
   - active-window law:
     routine maker lifecycle cancels are suppressed pre-expiry and logged as
     `order_cancel_suppressed`
   - terminal law:
     unfilled remainder now cancels at expiry with canonical terminal reason
     `commitment_window_ended`
   - reporting law:
     lifecycle and nightly report surfaces now distinguish:
     - suppressed routine cancel requests
     - terminal window-end cleanup
     - exceptional shutdown/safety cleanup
4. `INFERRED`: the next likely single-lane follow-on after commitment repair is
   fresh live maker confirmation under the repaired hold doctrine, then maker
   economics / outcome reread.
   - reason:
     fills are now structurally possible, and the old cancel-after-commit
     contradiction has been removed from the canonical maker lane
5. `VERIFIED`: lifecycle truth hardening now preserves explicit maker
   ineligibility cancel reasons.
   - code:
     `executor.py`
     `prodesk/order_manager.py`
   - current law:
     tracked active targets that fall out of canonical maker eligibility now
     cancel with their real gate reason such as `maker_timing_gate_closed` or
     `phase_disallow_maker`, instead of falling through as fake
     `orphan_token_order` cancels
   - intent:
     keep doctrine unchanged while making resting-order deaths auditable
6. `VERIFIED`: peak-hours economic conclusions are still deferred.
   - the repaired `$250` specimen was around `02:04 AM Central`
   - use it as a logic pathology specimen, not a fillability/PnL verdict

## Historical Macro Order Archive
1. `VERIFIED`: primary lane is now the taker `Masamune Energy Blade Set` doctrine-forging pass.
   - Scope: hard-`<=7s` taker authority, taker recovery dead-power, maker/taker flat-and-clear ownership law, and truth-surface hardening.
   - This is still not a broad maker review.
2. `INFERRED`: after the taker/main seam lane is mature enough, open a dedicated maker `Solar Slug Maker Cannon` lane.
   - Goal: give maker the same sharp, truth-heavy, doctrine-clean treatment rather than letting it stay an implicit side actor.
   - Boundary: do not let this future maker lane back-propagate into a broad maker review before the current taker-first packet earns it.
3. `VERIFIED`: maker-side cleanup/recovery design is now the later macro lane.
   - Trigger: taker-side recovery authority is being unplugged now, so the remaining serious recovery work belongs on maker and shared lifecycle design, not on canonical taker.
   - Goal: determine what cleanup help maker actually needs in live-parity conditions without backsliding taker doctrine.

## Historical Active Pickup Archive
`VERIFIED`: the active pickup has advanced beyond the older taker-first
maintenance checkpoint.

Current live lane:
- maker baseline run-set harvest and exploitation lane:
  - seed data pack:
    - `docs/BRO_BASELINE_RUN_SET_USA_PM_2026-04-27.md`
  - newest accepted/warning-tag specimens:
    - clean baseline:
      - `f79550de-22f2-439a-844a-09fa27d5fc48`
      - `8fabbe70-742a-4e74-b791-2f885a59a010`
    - accepted with warning:
      - `3f1bc263-e209-4e05-a510-1218d63d70f4`
    - warning-only:
      - `0cfbe40a-8a9c-4ff6-b094-bec6c200ef23`
  - earlier comparative anchors still in force:
    - `dff97a9a-9953-47e6-939f-f5f1f8814ed4`
    - `2349650c-5a83-4d65-8e7a-c2c4578c2cd6`
    - `2c26e81e-084f-4b85-854d-161d9d81c2b4`
    - control: `2f607a7b-d5ec-4882-a521-9f07ca3e9dd5`
- `FMA` and `FM-2A1` are already in active support, and this is still a maker
  lane
- current next-step question is now:
  - how do we exploit the new baseline set to promote low-price viability and
    queue-depth shadow surfaces before the next maker mutation family is
    chosen?

Current truth:
- live maker combat-window rows are now machine-readable through
  `maker_fireability` report surfaces and `FMA` harvested window gauges
- the first paper-only cadence cut removed `replace_guard_min_rest` from the
  active-window suppressor stack in specimen `2f607a7b...`, but did not earn
  adoption
- two healthy 20-minute maker specimens now show the split more cleanly:
  - `2349650c...` exposed low-price viability conflict plus heavier queue-depth
    friction and ugly completed outcomes
  - `2c26e81e...` showed healthier viable target families, no low-price sizing
    conflicts, and materially better multi-fill outcome quality
- low-price viability conflict is now pinned as a specimen family at
  `0.025..0.035`, not as a universal maker constant
- queue-depth suppressions are now understood as two families:
  - hard bad-depth pressure on ugly stock
  - lighter viable-stock friction on healthier tapes
- a named baseline data pack now exists so dead-zone `FMA` and `FM-2A1` work
  can reuse the same accepted stock without re-deriving the batch
- no runtime behavior has changed yet in this packet chain
- the next likely lane is now a maker low-price viability / queue-depth
  instrumentation and shadow packet

Historical anchor:
- the earlier taker doctrine-forging checkpoint is preserved below for context,
  but it is not the current live pickup.

Current truth:
- taker remains the primary engineering lane
- the current packet is taker doctrine forging with maker/taker interaction and end-state-shape still in scope where it affects taker truth
- this is still not a broad maker review
- for the legacy `EXTREME_ONLY` / current `LINEAGE_ONLY_0_TO_20S`
  lineage-cleanup packet, this file is historical/reference only and does not
  outrank the current packet-local self-hardening pack or active Packet 1 board
  owners
- accepted normal taker is now hard-canonical:
  - hard `<=7s` window
  - fixed `$150` shot
  - ride to resolution
- taker-side recovery is now dead-power doctrine for accepted normal taker
- maker-originated handoff into taker recovery is not valid canonical taker authority
- historical packet-era interpretation at that point was:
  - maker and taker may run simultaneously across different market bases
  - once one lane "owns" a market base, the other stands down there until it is flat and clear
  - later Packet 1 doctrine review rejected that interpretation as a misunderstanding of maker/taker independence
- the fresh maintenance-era health run proved BRO starts, runs, fills, closes, and validates cleanly after the workshop cleanup
- that health run did **not** prove premium normal taker edge quality
- first proof run `c0defa2e-254f-46e1-81bc-d5d80851f2a5` validated the first `Taker Katana` slice:
  - `runtime_classification=VALID_ACTIVE`
  - `reports_complete=true`
  - `determinism_consistent=true`
  - `execution_error=false`
  - `status=policy_failed`
  - under the retired whole-stream source-mix hard-fail contract, the sole
    blocking finding was `book_updates_rest_ratio 0.352033 > 0.350000`
  - current source-purity law is narrower:
    - whole-stream REST-mix grading is historical-only
    - current support truth is explicit pair-truth missing / bounded WS
      classification
    - hard source-purity closure belongs only to action-row
      `edge_evaluation` records with `action_taken in {maker,taker}`
- latest healthy proof run `3ab39523-e4f3-4e98-9c90-862f8ec33b80` tightened the result to a clean pass:
  - `validation_summary.json`: `ok=true`, `overall_exit_code=0`
  - `canonical_paper_validation`: `status=pass`, `runtime_classification=VALID_ACTIVE`, `reports_complete=true`, all validator exit codes `0`
  - `nightly_soak_report.txt` showed:
    - `normal_submits=0`
    - `lifecycle_residue_override_submits=0`
    - `maker_to_taker_recovery_handoff_disabled=144`
    - `waiting_for_maker_exit=0`
- latest watched current-tree specimen `6957087b-488e-4bbb-b8b9-1f215b5e33d0`
  now provides the stronger closeout health anchor:
  - stored validator bundle `ok=true` with all validator and replay-validator
    exits `0`
  - maker `1 submit / 2 fills`
  - taker `1 submit / 1 fill`
  - action-row source purity clean on both lanes
  - accepted taker fire occurred at:
    - `edge_value=0.2735337456282695`
    against canonical `required_min_edge=0.11`
- repaired nightly valuation truth now carries the event-level bruise window
  honestly:
  - `valuation_bruise_state=recovered_clean`
  - `degraded_event_rows=68`
  - `hard_degraded_enter_count=2`
  - `hard_degraded_clear_count=2`
- latest 60-minute stability specimen `8bfb70eb-c942-48eb-87ff-b9628b3098c7` extended the health proof:
  - `canonical_paper_validation`: `status=pass`, `runtime_classification=VALID_ACTIVE`, all validator exit codes `0`
  - `nightly_soak_report` showed:
    - `maker_submits=61`
    - `maker_fills=77`
    - `maker_to_taker_recovery_handoff_disabled=828`
    - `waiting_for_maker_exit=0`
    - `recovery_taker_submit_count=0`
    - `valuation_hard_degraded_enter_count=10`
    - `valuation_hard_degraded_clear_count=10`
  - end-state truth:
    - `held_unpriceable_unrecovered_meaningful_count=0`
    - `held_unpriceable_escalation_rows=0`
    - final `wallet_stable_balance_total=3030.529105`
    - final `gauge.total_pnl=-969.4708949999999`
  - interpretation:
    - healthy robot
    - clean lane independence
    - noisy blocked-handoff telemetry seam
    - real negative paper economics needing analysis
- telemetry-compression proof run `e675467e-368a-49db-bc03-f35c96aebba8` closed the noisy chatter packet:
  - `canonical_paper_validation`: `status=pass`, `runtime_classification=VALID_ACTIVE`, all validator exit codes `0`
  - `nightly_soak_report` showed:
    - `maker_submits=14`
    - `maker_fills=12`
    - `maker_to_taker_recovery_handoff_disabled=160`
    - `waiting_for_maker_exit=0`
  - compression proof:
    - authoritative blocked count remained `160`
    - raw `preexpiry_emergency_taker_unwind` event rows compressed to `20`
    - summary rows carried `repeat_count_delta`, `repeat_count_total`, `repeat_distinct_token_count`, and `repeat_token_ids_sample`
  - interpretation:
    - doctrine preserved
    - report truth preserved
    - live/event chatter materially reduced

Current next-step doctrine:
1. wait for a better regime window before making stronger taker-edge claims
2. run a watched canonical validation with under-the-hood inspection
3. inspect for a true normal taker specimen that fires only inside `<=7s` and rides cleanly
4. treat any taker-side recovery appearance, same-market lane collision, or out-of-window taker submit as doctrine-breach evidence
5. telemetry compression for repeated `maker_to_taker_recovery_handoff_disabled` chatter is closed unless a new regression appears
6. treat valuation-degraded recurrence as instrumentation-first scope unless a meaningful unrecovered held exposure artifact appears
7. treat the `8bfb...` economic loss as a maker/outcome-analysis packet, not a runtime-health false positive
8. `VERIFIED_CLOSED`: valuation bruise instrumentation is now in place.
   - `nightly_soak_report.json` now exposes:
     - `valuation_bruise_state`
     - `valuation_dominant_reason_family_run`
     - `valuation_dominant_held_unpriceable_cause_run`
     - `valuation_dominant_source_degraded_rows`
     - `valuation_degraded_reason_family_counts_run`
     - `valuation_source_counts_degraded_rows`
   - `FMA` now harvests the bruise state and dominant cause surfaces corpus-wide, with deeper family/source truth available on regenerated specimen reports like `8bfb...`.
   - `8bfb...` bruise truth is now pinned:
     - state `recovered_clean`
     - dominant degraded reason family `degraded_using_last_known_mid`
     - dominant held cause `unknown_data_gap`
     - degraded-row source mix led by `last_known_mid`
9. immediate next packet after this closure is maker shot-group / economics forensics on `8bfb...`
   - intent: separate bad decisions from bad execution with the sharpened `FMA` and outcome-truth surfaces
   - boundary: analysis first, no maker tuning drift in the opening pass
   - additive forensic lens: include semantic runtime reporting truth across the maker circuit so order-level outcome truth, fill-event economics, and maker report language do not get blurred together
10. immediate follow-through packet after the opening `8bfb...` forensic is a cross-run maker comparator
   - scope: compare `8bfb...`, `e675...`, and `ed184...`
   - intent: prove whether the completed-incorrect-fight pattern is recurring structure rather than a one-off ugly specimen
   - current truth:
     - `VERIFIED_OPEN`: the pattern is recurring
     - `VERIFIED_OPEN`: execution remains favorable while completed decision quality skews negative
     - `VERIFIED_OPEN`: semantic claim-boundary discipline matters because maker completion rate, fill-event execution economics, and order-level outcome truth are different populations
     - `VERIFIED_OPEN`: the canonical maker outcome lens is materially horizon-sensitive
     - `VERIFIED_OPEN`: current maker eval references in the audited specimens are uniformly `edge_market_midpoint_series`
     - `VERIFIED_OPEN`: some `8bfb...` losses arrive as mirrored complement-pair wounds
11. `VERIFIED_CLOSED`: final maker truth hardening pass is now complete.
   - new `FMA` surfaces now expose:
     - complete vs incomplete maker record counts
     - complete/incomplete bad ratios
     - multi-fill complete count and incorrect ratio
     - fills-per-filled-order
     - execution-rescue coverage
     - lifecycle gap classes
     - same-target repeat clusters
     - complement-pair cluster counts/examples
     - basis provenance and canonical horizon context
   - current pinned `8bfb...` truth now includes:
     - `maker_multifill_complete_count = 22`
     - `maker_multifill_complete_incorrect_ratio = 0.9090909090909091`
     - `maker_same_target_repeat_cluster_count = 17`
     - `maker_complement_pair_cluster_count = 8`
     - `maker_complement_pair_cluster_decision_debt_sum = -104.36126999999996`
   - current corpus truth now includes:
     - multi-fill complete maker records are a recurring wound family
     - semantic truth-population guardrails are machine-readable in `anomaly_summary.json`
12. `VERIFIED_CLOSED`: bounded maker pathway/semantics schematic is now racked as a BRO diagnostic tool.
   - Human blueprint:
     - `docs/SOLAR_SLUG_MAKER_CIRCUIT_SCHEMATIC.md`
   - Machine-readable map:
     - `docs/SOLAR_SLUG_MAKER_TRUTH_SEMANTICS_MAP.json`
   - Tool rack:
     - `docs/BRO_DIAGNOSTIC_TOOLS.md`
   - Purpose:
     - preserve maker pathway order,
     - preserve truth-population boundaries,
     - support future tuning/debug packets without re-deriving the lane
   - Boundary:
     - maker only for now
     - future taker/shared schematics should be earned lane-by-lane after real evidence passes
   - next likely lane after this closure:
     - bounded maker tuning-hypothesis design
     - keep it analysis-first until major design/risk decisions earn explicit signoff
13. `VERIFIED_ACTIVE_RULE`: future unmapped money-touching lanes should use the same shop flow that maker just used.
   - Use `FMA` and primary artifacts first.
   - Take three bounded passes:
     - orientation
     - red-team semantic pressure test
     - truth hardening / hotspot promotion
   - Only then forge:
     - one human-readable schematic
     - one machine-readable truth-semantics map
   - Rack the resulting blueprint surfaces under `docs/BRO_DIAGNOSTIC_TOOLS.md`.
14. `VERIFIED_CLOSED`: maker fireability / friction forensic on valid specimen `dff97a9a-9953-47e6-939f-f5f1f8814ed4` is now pinned.
   - core packet:
     - `docs/MAKER_FIREABILITY_FORENSIC_dff97a9a.md`
   - current truth:
     - canonical validation passed with `runtime_classification=VALID_ACTIVE`
     - raw lifecycle truth showed:
       - `order_submit=24`
       - `fill=24`
       - `order_cancel=18`
     - harvested maker truth showed:
       - `maker_complete_record_count=12`
       - `maker_incomplete_record_count=12`
       - `complete_bad_ratio=0.6666666666666666`
       - `multifill_incorrect_ratio=1.0`
       - `singlefill_correct_ratio=0.6666666666666666`
   - no-fire anatomy:
     - `maker_timing_gate_closed=324`
     - `maker_no_submission=24`
     - no-submit cause mix:
       - `replace_guard_min_rest=16`
       - `submit_rejected_quote_quality_skip_fill_probability=6`
       - `submit_rejected_quote_quality_skip_queue_depth=2`
   - current interpretation:
     - submitted maker rows lived inside the then-configured drift-era
       `50-60s` timing window, so that packet's evidence fit its historical
       runtime gate behavior more than a hidden timing malfunction
     - inside the live window, the biggest additional suppressors are
       replace-guard min-rest and quote-quality friction
   - next likely lane after this closure:
     - bounded maker fireability / friction hypothesis design
     - use this specimen before collecting more runtime tape
15. `VERIFIED_CLOSED`: bounded maker fireability / friction hypothesis design is now pinned for specimen `dff97a9a-9953-47e6-939f-f5f1f8814ed4`.
   - core packet:
     - `docs/MAKER_FIREABILITY_HYPOTHESIS_DESIGN_dff97a9a.md`
   - machine-readable matrix:
     - `docs/MAKER_FIREABILITY_EXPERIMENT_MATRIX_dff97a9a.json`
   - pinned truth:
     - real maker combat-zone rows: `40`
     - within-window split:
       - `submitted = 16`
       - `replace_guard_min_rest = 16`
       - `quote_quality_skip_fill_probability = 6`
       - `quote_quality_skip_queue_depth = 2`
     - current specimen still shows:
       - `maker_complete_bad_ratio = 0.6666666666666666`
       - `maker_multifill_complete_incorrect_ratio = 1.0`
       - `maker_singlefill_correct_ratio = 0.6666666666666666`
   - current interpretation:
     - under that drift-era runtime frame, the `50-60s` timing window was not
       the first widening candidate
     - the highest-ROI next seam is within-window cadence truth and
       `replace_guard_min_rest`
     - quote-quality work should stay split between near-threshold and clearly
       bad rejects
   - next likely lane after this closure:
     - maker fireability instrumentation and shadow-pressure packet
     - promote within-window cadence and quote-skip severity truth before any
       paper-only knob mutation
16. `VERIFIED_CLOSED`: maker fireability instrumentation and shadow-pressure packet is now implemented and verified on the live specimen `dff97a9a-9953-47e6-939f-f5f1f8814ed4`.
   - report surfaces:
     - `nightly_soak_report.json -> maker_fireability`
   - harvested surfaces:
     - `maker_window_active_row_count`
     - `maker_window_submit_count`
     - `maker_window_replace_guard_count`
     - `maker_window_quote_quality_skip_total_count`
     - `maker_window_submit_rate`
     - `maker_window_replace_guard_rate`
     - `maker_window_quote_quality_skip_rate`
     - `maker_window_target_summary`
     - `maker_quote_quality_skip_fill_probability_severity_bins`
     - `maker_quote_quality_skip_queue_depth_severity_bins`
   - pinned specimen truth:
     - active-window rows: `40`
     - submits: `16`
     - replace-guard suppressions: `16`
     - quote-quality suppressions: `8`
     - submit rate: `0.4`
     - replace-guard rate: `0.4`
     - quote-quality skip rate: `0.2`
     - raw non-recovery quote-quality reject sparks: `18`
   - claim-boundary truth:
     - raw quote-quality reject sparks are event-level reject surfaces and are
       not the same population as maker edge-evaluation no-submit assignments
   - next likely lane after this closure:
     - one guarded paper-only maker cadence experiment
     - preferred first cut remains:
       - `maker_replace_min_rest_sec: 3.0 -> 2.0`
     - under that drift-era runtime frame, do not widen the `50-60s` runtime
       window in the same packet
     - do not broadly loosen quote-quality thresholds in the same packet
17. `VERIFIED_CLOSED`: first guarded paper-only maker cadence experiment is now complete on specimen `2f607a7b-d5ec-4882-a521-9f07ca3e9dd5`.
   - result packet:
     - `docs/MAKER_CADENCE_EXPERIMENT_RESULT_2f607a7b.md`
   - experiment change:
     - `runtime.maker_replace_min_rest_sec: 3.0 -> 2.0`
   - validation result:
     - `status=pass`
     - `runtime_classification=VALID_ACTIVE`
     - `promotion_eligible=true`
   - pinned truth:
     - active-window rows stayed `40`
     - active-window submits fell from `16` to `2`
     - active-window replace-guard fell from `16` to `0`
     - active-window queue-depth suppressions rose to `8`
     - active-window sizing rejects surfaced at `10`
     - maker fills fell from `24` to `0`
   - current interpretation:
     - the cadence seam was real but not the first true limiter in this regime
     - bottleneck migrated into:
       - low-price min-notional / max-shares conflict
       - queue-depth friction on the only viable target family
     - the `2.0s` cut is not earned for adoption
   - operational decision:
     - canonical `paper_universal` profile has been restored to `3.0s`
   - next likely lane after this closure:
     - bounded maker low-price viability / queue-depth forensic and design pass
     - do not widen the timing window next
     - do not broadly loosen quote-quality next
18. `VERIFIED_CLOSED`: bounded maker low-price viability / queue-depth forensic and design pass is now pinned on the valid maker set.
   - core packet:
     - `docs/MAKER_LOW_PRICE_VIABILITY_QUEUE_DEPTH_FORENSIC_dff97a9a_2349650c_2c26e81e.md`
   - machine-readable matrix:
     - `docs/MAKER_LOW_PRICE_VIABILITY_QUEUE_DEPTH_EXPERIMENT_MATRIX_dff97a9a_2349650c_2c26e81e.json`
   - specimen set:
     - baseline valid maker specimen:
       - `dff97a9a-9953-47e6-939f-f5f1f8814ed4`
     - healthy 20-minute friction-heavy specimen:
       - `2349650c-5a83-4d65-8e7a-c2c4578c2cd6`
     - healthy 20-minute healthier specimen:
       - `2c26e81e-084f-4b85-854d-161d9d81c2b4`
     - cadence control:
       - `2f607a7b-d5ec-4882-a521-9f07ca3e9dd5`
   - pinned truth:
     - timing doctrine still held across the valid maker set
     - low-price viability conflict is real at `0.025..0.035`, but not present
       in every valid specimen
     - queue-depth friction now splits into:
       - hard bad-depth pressure on ugly stock
       - lighter viable-stock friction on healthier stock
     - completed multi-fill maker fights remain the main economic wound even
       when participation improves
   - current interpretation:
     - do not rerun cadence first
     - under that drift-era runtime frame, do not widen the `50-60s` timing
       window next
     - do not broadly loosen quote-quality next
     - promote low-price viability and queue-depth truth into first-class shadow
       surfaces before the next maker mutation family is chosen
   - next likely lane after this closure:
     - maker low-price viability / queue-depth instrumentation and shadow packet
     - keep it runtime-neutral in the opening pass
19. `VERIFIED_CLOSED`: first named baseline run-set harvest is now preserved for USA-PM stock on `2026-04-27`.
   - core set docs:
     - `docs/BRO_BASELINE_RUN_SET_USA_PM_2026-04-27.md`
     - `docs/BRO_BASELINE_RUN_SET_USA_PM_2026-04-27.json`
   - accepted set composition:
     - clean baseline:
       - `f79550de-22f2-439a-844a-09fa27d5fc48`
       - `8fabbe70-742a-4e74-b791-2f885a59a010`
     - accepted with warning:
       - `3f1bc263-e209-4e05-a510-1218d63d70f4`
     - warning-only:
       - `0cfbe40a-8a9c-4ff6-b094-bec6c200ef23`
   - current interpretation:
     - the shop now has reusable baseline stock for dead-zone `FMA` and
       `FM-2A1` work
     - maker remains the richest immediate exploitation lane
     - taker can still benefit later from the same pack as comparison
       stock, but this set does not replace their own mapping packets
   - next likely lane after this closure:
     - exploit the baseline set inside the maker low-price viability /
       queue-depth instrumentation and shadow packet
20. `VERIFIED_CLOSED`: baseline-set runtime-neutral maker viability / queue-depth shadow packet is now refreshed through current shop tooling.
   - refreshed report surfaces:
     - `reports/*/nightly_soak_report.json -> maker_fireability`
   - refreshed per-run harvest stock:
     - `logs_exec/paper_universal/forge_masters_archive_run_f79550de-22f2-439a-844a-09fa27d5fc48`
     - `logs_exec/paper_universal/forge_masters_archive_run_8fabbe70-742a-4e74-b791-2f885a59a010`
     - `logs_exec/paper_universal/forge_masters_archive_run_3f1bc263-e209-4e05-a510-1218d63d70f4`
     - `logs_exec/paper_universal/forge_masters_archive_run_0cfbe40a-8a9c-4ff6-b094-bec6c200ef23`
   - refreshed baseline-only corpus cuts:
     - `logs_exec/paper_universal/forge_masters_archive_baseline_set_usa_pm_2026-04-27`
     - `logs_exec/paper_universal/fusion_core_profile_baseline_set_usa_pm_2026-04-27`
   - pinned truth:
     - all four baseline runs share `maker_window_low_price_viability_floor=0.04375`
     - only `f795...` and `3f1bc...` carry low-price impossible rows
     - baseline-only corpus totals:
       - `maker_window_viable_row_count_total=298`
       - `maker_window_impossible_row_count_total=19`
       - `maker_window_sizing_reject_count_total=16`
       - `maker_min_notional_max_shares_conflict_rows_total=24`
       - `maker_window_queue_depth_on_viable_targets_count_total=23`
       - `maker_window_queue_depth_on_impossible_targets_count_total=0`
       - `viable_target_total=30`
       - `impossible_target_total=1`
       - `mixed_target_total=1`
   - current interpretation:
     - low-price geometry conflict is a real but narrow family inside the
       baseline set, not a universal maker-law block
     - queue-depth friction in baseline stock is landing on viable targets, so
       the next real question is viable-target competitiveness rather than
       impossible-target cleanup
     - the broad historical `latest` corpus remains useful, but the
       baseline-only corpus is the right current truth surface for this lane
   - next likely lane after this closure:
     - choose the first post-shadow maker mutation family from:
       - low-price geometry / floor handling
       - viable-target queue-depth friction handling
     - do not rerun cadence first
     - under that drift-era runtime frame, do not widen the `50-60s` maker
       timing window first
     - do not broadly loosen quote-quality gates first
21. `HISTORICAL_ONLY`: Maker Queue-Competitiveness Packet A documents ancestry only. Counter names, branch names, and runtime results preserved below are archival vocabulary only and must not be read as current-live owner surfaces. Current code cut the queue-pressure runtime/config/report family on `2026-05-11`, keeps only legacy-config ignored compatibility plus historical replay lineage, and no longer treats this packet as live authority.
   - exact packet boundary:
     - maker-only
     - favored-side only
     - one-tick inside-spread conversion
     - only when the base quote fails specifically on queue depth
     - only when a simulated inside quote passes the existing quality gate
     - not a low-price geometry fix
     - not a `max_queue_ahead_size` relaxation
     - not a `min_expected_fill_prob` relaxation
   - historical implementation surfaces:
     - `prodesk/order_manager.py`
     - `prodesk/config.py`
     - `scripts/nightly_soak_report.py`
     - prior queue-packet profile residue has since been removed from the live
       workspace
   - historical report truth existed for:
     - `maker_queue_pressure_candidate_count`
     - `maker_queue_pressure_adopted_count`
     - `maker_queue_pressure_gate_conversion_count`
     - `maker_queue_pressure_replace_guard_blocked_count`
     - `maker_complete_correct_rate`
     - `maker_complete_incorrect_rate`
     - `maker_complete_neutral_rate`
   - historical focused validation passed before profile quarantine and later
     family cut:
     - queue-pressure execution-stack coverage
     - nightly soak report maker-competitiveness metric coverage
   - paired runtime proof completed:
     - control run: `5376484d-86f8-45b3-afe1-5316f616a922`
     - experiment run: `c9fba847-407e-4f68-9958-440b86199bf8`
   - paired runtime truth:
     - packet counters stayed cold:
       - `maker_queue_pressure_candidate_count = 0`
       - `maker_queue_pressure_adopted_count = 0`
       - `maker_queue_pressure_gate_conversion_count = 0`
       - `maker_queue_pressure_replace_guard_blocked_count = 0`
     - both runs still showed only `2` raw near-threshold queue-depth events and
       `0` hard-miss queue-depth events
     - geometry burden stayed flat:
       - `active_window_impossible_row_count = 10`
       - `active_window_sizing_reject_count = 0`
     - raw maker activity rose in the experiment:
       - submits `14 -> 24`
       - fill events `24 -> 32`
     - but decision-reference maker economics worsened:
       - net capture-minus-adverse `59.2722 -> 24.741366`
       - net/notional ratio `0.1095 -> 0.0490`
   - promotion result:
     - do **not** promote Packet A
     - fast-fail rule triggered because real queue-pressure candidate count stayed
       below `2` and gate conversions stayed `0`
   - interpretation boundary:
     - the experiment profile had isolated runtime/storage paths
     - the control profile reused the standard `paper_universal` lane, so raw
       submit/fill deltas are not clean packet attribution proof by themselves
     - that caveat does **not** change the core result because the queue-pressure
       packet itself never activated
   - next likely lane after this closure:
     - stop queue tuning here
     - move directly to a market-selection / skip-trash-windows packet unless a
       future clean replay is explicitly requested for a different question
22. `VERIFIED_IMPLEMENTED`: Maker Fight-Admission Shadow Packet is now wired as the main report-only selectivity tooling packet.
   - exact packet boundary:
     - runtime emits raw per-side maker fight rows
     - report layer computes the canonical selector rubric
     - `FMA` preserves summary truth plus normalized row-level archive export
     - no maker timing, queue, sizing, or gating semantics changed
   - implementation surfaces:
     - `prodesk/order_manager.py`
     - `executor.py`
     - `scripts/nightly_soak_report.py`
     - `scripts/bro_metric_harvest.py`
   - first-class support artifacts now exist:
     - `maker_market_snapshot.jsonl`
     - `maker_market_snapshot_summary.json`
     - `maker_market_snapshot_calibration_audit.json`
     - `maker_market_snapshot_rows.jsonl`
     - `maker_admission_target_side_summary.json`
   - focused validation passed:
     - `tests/test_execution_stack.py -k 'legacy_maker_queue_pressure_surface or maker_market_snapshot'`
     - `tests/test_nightly_soak_report.py -k 'maker_market_snapshot_support_artifacts or legacy_maker_market_snapshot'`
     - `tests/test_nightly_soak_report.py`
     - `tests/test_bro_metric_harvest.py`
   - short runtime health gate completed:
     - canonical paper run `256503c2-9345-4d11-8f9b-b8911af1fa79`
     - runtime stayed healthy
     - support artifacts were emitted
     - short run was `NON_PROMOTABLE_NO_PARTICIPATION`, so it is health proof,
       not selector proof
   - baseline truth gate completed on the named four-run stock set:
     - bundle:
       - `logs_exec/paper_universal/forge_masters_archive_baseline_set_usa_pm_2026-04-27_admission_shadow`
     - row truth:
       - `row_count=204`
       - `candidate=118`
       - `truth_thin=86`
       - `clean=64`
       - `borderline=28`
       - `trash=26`
       - all rows sourced from `legacy_quote_or_submit_backfill_v1`
     - mature-outcome join truth:
       - `clean_complete_joined=34`
       - `borderline_complete_joined=15`
       - `trash_complete_joined=0`
       - `clean_complete_bad_ratio=0.7352941176470589`
       - `borderline_complete_bad_ratio=0.7333333333333333`
       - `clean_multifill_incorrect_ratio=0.6190476190476191`
       - `borderline_multifill_incorrect_ratio=0.8`
     - dominant driver families:
       - `size_liquidity_pressure=52`
       - `repeat_target_side_pressure=40`
       - `fill_prob_margin_lt_neg_0p015=19`
       - `queue_delta_gt_50=7`
   - interpretation boundary:
     - the packet is useful for driver discovery and no-submit classification
     - the current selector does **not** yet separate submitted `clean` fights
       from submitted bad fights strongly enough to earn runtime gating
     - the current baseline recut does **not** earn the new `FM-2A1`
       `fight_admission_shadow` family yet
   - next likely lane after this closure:
     - keep runtime behavior unchanged
     - use the shadow outputs to build the first stricter market-selection /
       skip-trash-windows rubric
     - target `size_liquidity_pressure` and `repeat_target_side_pressure`
       before reopening queue tuning
     - fold in `docs/GALAXY_MEGA_MAKER_CANNON_DOCTRINE_PROPOSAL_2026-04-28.md` as
       external doctrine input for the first high-selectivity maker rubric
  - cannon-shadow extension now implemented on the same report-only lane:
    - runtime rows now preserve:
      - `cannon_window_class`
      - `session_regime_class`
      - `stack_pressure_class`
      - `secondary_oracle_status` / confirmation
      - `depth_multiple_vs_cannon_target`
    - targeted proof:
      - `tests/test_execution_stack.py -k maker_market_snapshot`
      - `tests/test_nightly_soak_report.py -k maker_market_snapshot`
      - `tests/test_bro_metric_harvest.py -k maker_market_snapshot`
  - short native runtime proof after the cannon extension:
    - run: `823cd68e-26e1-4b0d-a63c-52091e86c7fe`
    - runtime truth:
      - `VALID_ACTIVE`
      - no execution error
      - policy-only exit on `soak_hardening_gate` and `time_discipline_audit`
    - cannon-shadow artifact truth:
      - `row_count=18`
      - `snapshot_source_class_distribution={"runtime_raw":18}`
      - `candidate=18`
      - `clean=0`
      - `borderline=0`
      - `trash=18`
      - `cannon_window_class_distribution={"gt_20s":18}`
      - `stack_pressure_class_distribution={"below_soft_cap":18}`
      - `secondary_oracle_confirmation_distribution={"confirmed":18}`
      - `cannon_depth_requirement_counts={"met":18}`
      - dominant drivers:
        - `non_viable_geometry_or_sizing_conflict=9`
        - `queue_delta_gt_50=9`
    - interpretation:
      - the new cannon-shadow lane is healthy and emitting
      - the current maker runtime was still sampling the older drift-era
        `45-60s` maker lane, not true `15-20s` Galaxy-cannon candidates
      - current live evidence therefore supports a stricter skip-trash doctrine,
        but does **not** yet validate the late-window cannon itself
  - refined next lane after this proof:
    - keep runtime behavior unchanged
    - use the cannon-shadow surfaces to cut the first stricter
      skip-trash-windows rubric
    - if that still cannot separate real cannon candidates, add a bounded
      report-only late-window probe before any runtime maker timing shift
  - bounded late-window observational widening and cannon-probe sharpen now
    complete:
    - run: `310f728b-dd59-4216-8d4b-259e66ce0f7b`
    - runtime classification:
      - `VALID_ACTIVE`
      - no execution error
      - policy-only exit on `soak_hardening_gate` and `time_discipline_audit`
    - late-window probe truth:
      - `row_count=68`
      - `late_window_raw_row_count=68`
      - `candidate=36`
      - `truth_thin=32`
      - `full_cannon_candidate_count=0`
      - `runtime_maker_stage_allowed_distribution={"disallowed":68}`
      - `probe_visible_depth_fail_closed_zero_distribution={"imputed_zero":36,"reported_or_not_needed":32}`
      - `secondary_oracle_status_distribution={"confirmed":36,"unknown":32}`
      - `runtime_secondary_oracle_status_distribution={"unknown":68}`
      - `cannon_depth_requirement_counts={"not_met":36,"unknown":32}`
    - dominant candidate reject families:
      - `market_reference_not_authoritative=36`
      - `insufficient_depth_multiple=36`
      - `non_viable_geometry=18`
    - late-window topology truth:
      - historical market-reference distribution was dominated by one-sided fallback rows rather than midpoint-backed authoritative rows
      - `market_reference_source_side_distribution={"ask":27,"bid":27,"none":14}`
      - `market_probability_band_distribution={"ge_0p99":27,"le_0p01":27,"unknown":14}`
      - `favored_side_depth_class_distribution={"zero_imputed":36,"unknown":32}`
    - interpretation:
      - we now observe real `<=20s` cannon population without changing maker behavior
      - the probe is no longer blocked by runtime stage-disallow or midpoint-blind
        secondary-oracle truth
      - the current live late-window fights are predominantly one-sided
        bounded-touch extremes with no favored-side resting depth, plus a
        narrower low-price geometry wound
      - this run occurred in `transition_heuristic`, so it is not yet peak-session
        proof for the full Galaxy maker doctrine
    - net next move:
      - keep runtime unchanged
      - use the sharpened cannon probe to cut the first strict late-window
        skip-trash rubric
      - likely next sub-lane is peak-session cannon observation plus stronger
        authoritative late-window reference / depth truth, not a maker timing
        mutation
  - decontaminated historical cannon reread now complete on healthier maker
    samples:
    - representative runs:
      - peak: `6c3f1332-e080-4845-af87-e5a43ca8ff7a`
      - transition: `f79550de-22f2-439a-844a-09fa27d5fc48`
      - asia: `dff97a9a-9953-47e6-939f-f5f1f8814ed4`
    - representative sweep artifact:
      - `/tmp/maker_cannon_decontam_slice_2026-04-28/archive/maker_cannon_probe_session_sweep.json`
    - decontaminated truth:
      - all `341` late-window rows still land in `population_class=external_blocked`
        because of then-live `reduce_only_recovery_active` historical lineage
      - after stripping that posture out in the new latent-market read:
        - transition: `111` evaluable rows, `0` latent full cannon candidates
        - asia: `58` evaluable rows, `0` latent full cannon candidates
        - peak: `0` evaluable rows because the late-window truth is still too thin
      - dominant latent-market reject families on the evaluable transition/asia
        rows:
        - `insufficient_depth_multiple`
        - `secondary_oracle_unknown`
        - narrower `non_viable_geometry`
    - interpretation:
      - the healthier transition and asia slices are **not** hiding clean
        Galaxy-cannon fights under recovery posture; the underlying late-window
        terrain still fails
      - the remaining unresolved seam is peak-session truth quality, not more
        maker queue tuning
    - next packet:
      - keep runtime unchanged
      - harden peak-session late-window truth capture first
      - specifically improve favored-side depth and secondary-oracle truth in
        recovery / extreme-only late-window observations before any runtime
        maker timing shift
  - maker timing-ladder truth is now preserved as a separate shop cut:
    - bundle:
      - `logs_exec/paper_universal/forge_masters_archive_maker_timing_ladder_valid_active_2026-04-28`
    - corpus scope:
      - six valid-active maker runs with shadow artifacts on disk
      - row source is still `legacy_quote_or_submit_backfill_v1`, not native
        runtime-raw maker shadow
    - timing truth:
      - `candidate_count_by_timing_band={"45_to_60s":111,"unknown":45}`
      - `complete_joined_count_by_timing_band={"45_to_60s":50,"unknown":18}`
      - `complete_bad_ratio_by_timing_band={"45_to_60s":0.76,"unknown":0.6111111111111112}`
      - `multifill_incorrect_ratio_by_timing_band={"45_to_60s":0.7575757575757576,"unknown":0.6}`
      - no candidate rows were observed in `10-45s` on this maker-active corpus
    - meaning:
      - the historical bundle was dominated by a drift-era `45-60s` runtime
        band; that archival posture was not yet a clean high-win-rate fighting
        band and does not define current maker doctrine
      - the active maker mission is still the same:
        - zero the sights
        - identify the right battlefield time band
        - identify which fights deserve the `$350` cannon shot
      - `20-45s` is still a real unknown seam, not a disproven one
    - bounded `20-45s` observational packet is now complete:
      - six-run archive recut result:
        - `row_count=1248`
        - `population_class_counts={"external_blocked":1094,"truth_thin":154}`
        - `full_mid_window_candidate_count=0`
        - `latent_market_full_mid_window_candidate_count=0`
      - meaning:
        - the archived `20-45s` seam is dominated by posture-blocked or
          truth-thin rows and does **not** currently justify a mid-window maker
          timing shift
    - fresh native canonical transition specimen:
      - run: `ba5bfde7-01f1-4e34-9695-975b56661ad9`
      - canonical outcome:
        - `runtime_classification=NON_PROMOTABLE_NO_PARTICIPATION`
        - primary suppression:
          `active_targets_without_meaningful_participation`
      - native late-window probe truth:
        - `row_count=156`
        - `candidate=116`
        - `truth_thin=40`
        - `full_cannon_candidate_count=27`
        - `latent_market_full_cannon_candidate_count=27`
      - native mid-window probe truth:
        - `row_count=58`
        - `candidate=6`
        - `truth_thin=52`
        - `full_mid_window_candidate_count=1`
        - `latent_market_full_mid_window_candidate_count=1`
      - meaning:
        - current-code native truth now points much more strongly at the
          `<=20s` Galaxy-cannon lane than at `30-45s`
        - the live maker runtime still did not participate, so this remains
          observational truth only
    - next likely lane after this closure:
      - keep runtime unchanged
      - preserve the new native specimen in:
        - `logs_exec/paper_universal/forge_masters_archive_run_ba5bfde7-01f1-4e34-9695-975b56661ad9`
      - prioritize structured `20m` canonical harvests during the heavy
        `8am-2pm` working window
      - operator support prepared:
        - collection:
          `python3 scripts/maker_peak_session_harvest.py --session-count 30 --active-minutes 20 --wait-sec 25 --stop-before-local <YYYY-MM-DD>T14:00:00 --local-timezone America/Chicago`
        - exact-set harvest:
          `python3 scripts/bro_metric_harvest.py --report-root logs_exec/paper_universal/reports --run-id-file <sweep_dir>/run_ids.txt --out-dir <bundle_dir>`
      - use those peak-session specimens to decide whether a true late-window
        maker timing shift is earned
      - do **not** promote a `20-45s` mid-window maker lane from the current
        archive/native evidence
      - new ROI cut on the `2026-04-28` `13`-run keeper packet now exists:
        - tool:
          `scripts/maker_cannon_roi_setup.py`
        - artifacts:
          - `logs_exec/paper_universal/forge_masters_archive_maker_peak_session_keeper_set_2026-04-28/solar_slug_maker_cannon_roi_setup.json`
          - `logs_exec/paper_universal/forge_masters_archive_maker_peak_session_keeper_set_2026-04-28/solar_slug_maker_cannon_roi_setup.md`
        - verdict:
          - `partial_adopt_high_roi_components_only`
        - current highest-ROI cannon setup pieces:
          - `depth_requirement_1p5x`
          - `secondary_oracle_confirmation`
          - `repeat_target_side_calm`
        - current low-ROI or not-yet-earned pieces:
          - hard stack cap is a safety guard, not a primary selector
          - strict late-window timing shift still needs native runtime proof
          - `0.20` delta semantics still need formal BRO-side definition
        - launch-shape meaning:
          - if we bias toward a first safe `Barbatos` launch instead of a final
            perfect maker design, the evidence currently favors a conservative
            skip-trash selection packet over a broad runtime timing mutation
            packet
      - implementation status:
        - the bounded launch-safe maker selection packet lineage now exists as:
          - runtime gate:
            `lifecycle.selection` plus maker-lane lifecycle gates
        - historical experiment-profile residue previously existed on disk but
          has now been cut from the live workspace
        - the old noncanonical alternate paper lane is gone:
          - no second paper fingerprint
          - no supported noncanonical proving path outside canonical
            `paper_universal`
        - current packet enforces only the proven selectors:
          - `depth_requirement_1p5x`
          - `secondary_oracle_confirmation`
          - `repeat_target_side_calm`
        - canonical `paper_universal` remains unchanged
        - `Packet B` paper proof verdict:
          - harness passed end to end
          - maker/taker separation held
          - `0` submits
          - `0` fills
          - runtime classification:
            `NON_PROMOTABLE_NO_PARTICIPATION`
        - next missing proof is no longer harness viability
        - the zero-submit reconciliation audit packet now exists in the report
          layer and is the active truth surface for the current maker stall:
          - `maker_participation_waterfall.json`
          - `maker_quote_starvation_audit.jsonl`
          - `maker_quote_starvation_summary.json`
          - `maker_truth_reference_starvation_audit.jsonl`
          - `maker_truth_reference_starvation_summary.json`
          - `maker_quote_construction_audit.jsonl`
          - `maker_quote_construction_summary.json`
          - `maker_timing_band_diagnostic_matrix.json`
          - `maker_timing_band_decision.json`
          - `maker_zero_submit_root_cause_audit.json`
          - `maker_zero_submit_specimen_manifest.json`
          - `maker_zero_submit_specimen_comparison.json`
        - current proved audit truths:
          - `$350 Packet B`:
            - `0` shadow rows
            - `10` true `no_desired_quote` active-band quote-starvation rows
            - truth readiness is one-sided / non-authoritative
            - dominant truth deprivation:
              one-sided WS pair-truth with no authoritative maker reference
            - active-band rows still die on truth/depth/quoteability
          - `$250` caliber:
            - `20` true `no_desired_quote` active-band quote-starvation rows
            - `12` authoritative-complete active-band rows
            - `12` matched shadow selection rejects
            - `16` total shadow selection rejects
            - `4` full-cannon opportunities exist, but all are off-band
            - dominant one-sided / non-authoritative deprivation:
              one-sided WS pair-truth with no authoritative maker reference
          - current measurement state:
            - Packet 0 measurement contract is closed enough for
              `ready_for_truth_packet`
            - specimen pair is frozen and comparable side by side
        - next maker lane is no longer blind caliber stepping by default
        - chosen next move from the audit output:
          - truth/reference hardening first
        - current maker runtime repair is now implemented:
          - maker-only recent paired-touch backfill in `executor.py`
          - active one-sided `ws` books can now promote to
            `backfilled_paired_touch` when the complementary side was seen
            within the bounded repair delta
          - the same resolved top/reference now feeds:
            - maker competitiveness profiling
            - quote construction / manager step
            - maker edge-evaluation logging
          - taker remained untouched
          - no profile/setup-lock fingerprint churn was introduced for this
            packet
        - current next proof step:
          - run fresh Packet `B` and `$250` maker specimens on the repaired
            substrate
          - verify whether one-sided / non-authoritative active-band starvation converts into:
            - authoritative-complete rows
            - selection-depth rejects
            - or actual submits
        - repaired specimen truth now landed:
          - repaired `Packet B` shifted active-band choke away from pure
            quote starvation and toward selection-depth rejection, but still
            produced `0` submits / `0` fills
          - repaired `$250` produced two distinct late-window specimens in the
            same run:
            - `2026-04-29T06:59Z` maker window:
              - `10` maker rows blocked by
                `token_lag_not_verified_for_maker`
              - `market_reference_mode = missing`
              - no shadow rows
            - `2026-04-29T07:04Z` maker window:
              - `direct_midpoint` truth was available
              - `9` maker no-submit rows:
                - `6`
                  `launch_safe_selection_insufficient_depth_multiple`
                - `3` opposite-side rows later recognized as normal one-sided
                  prunes, not real blocker-family authority
              - `11` shadow rejects with
                `selection_gate_primary_reject_reason = insufficient_depth_multiple`
              - still `0` submits / `0` fills
          - current meaning:
            - the lane is no longer dominated by one single failure family
            - some maker windows still die on upstream truth/lag readiness
            - later windows can reach honest selection depth rejection once
              truth is good
            - quote-construction defect remains downgraded
        - do not jump straight to:
          - timing-band shift
          - quoteability-first mutation
          - or deeper caliber reduction
          until the repaired specimen truth lands

Explicit do-not-drift rules:
- do not reopen broad shop maintenance unless a new infrastructure symptom earns it
- do not broaden into a general maker review yet
- do not turn the active taker packet into a premature maker/recovery redesign packet
- do not treat the health run as edge proof
- do not mistake the cannon ROI tool for proof that the whole Grok blueprint is
  runtime-ready in one swing

Macro phase-line truth:
- the real goal line for `BRO` is not “perfect paper forever”
- the phase sequence is:
  - harden `Maker`
  - harden `Taker`
  - harden wallet authority
  - run the `predeployment` packet built with `Nova`
  - if predeployment passes:
    - clone `BRO`
    - promote that clone into `Barbatos`
    - go live with tiny short runs
    - tune upward carefully
    - expand until `Barbatos` runs through the full `8am-2pm` working window
- `Phase 1 mission success` means:
  - `Barbatos` is actively running each day in that window
  - live `PnL` is positive
  - the expansion is still controlled, not reckless

## Closed Historical Diagnostic Work
1. Verify this doc packet:
   - `scripts/doctrine_truth_audit.py`
   - `tests/test_doctrine_truth_audit.py`
   - Status: complete for targeted doctrine/report tests.
2. Inspect `stage_reduction_delta_accounting` logic:
   - Target: `scripts/nightly_soak_report.py`
   - Acceptance: prove whether mismatch is expected accounting overlap, primary-cause selection bug, or event taxonomy drift.
   - Status: complete. Current-code report output now marks event-row overlap explicitly.
3. Inspect `fair_probability_missing` path:
   - Target surfaces: fair-map construction, taker edge validation reuse, and edge truth taxonomy.
   - Acceptance: classify as expected unavailable, taxonomy gap, map-scope bug, or `UNKNOWN`.
   - Status: complete for code diagnosis, targeted tests, adjacent tests, and runtime proof. Current code separates maker-scoped and taker-scoped fair maps.
   - Runtime proof: canonical 10-minute paper run `7e0a7dcf-947a-4d88-9f0c-9a6790ed6b69` had `0` taker `fair_probability_missing` rows and `13` maker rows.
4. Inspect sizing feasibility:
   - Target surfaces: maker hard floor, maker max shares/notional, price feasibility, and sizing reject reporting.
   - Acceptance: prove whether config can deterministically request infeasible orders and whether pre-gating/reporting should be added.
   - Status: complete for diagnosis and report visibility. The residual is a fail-closed maker floor/cap feasibility constraint: `100.0` USDC floor with `800.0` max shares is infeasible below midpoint `0.125`.
   - Current-code proof: clean run replay `30` maker sizing rejects / `30` min-notional-max-shares conflicts; post-patch run replay `4` / `4`.
5. Inspect reduce-only recovery cap availability:
   - Target surfaces: recovery payload construction and flat/non-flat reject context.
   - Acceptance: prove whether latest run signals were flat/no-op churn or a true recovery-path weakness.
   - Status: complete. Current artifacts classify the signal as flat/wrong-side no-op local rejection, with non-flat/unknown count `0`.
   - Current-code proof: clean anchor `16/16`, post-patch runtime proof `2/2`, smoke run `1/1` flat-or-wrong-side local size-cap unavailable rows.
6. Inspect maker submit floor / participation finding:
   - Target surfaces: maker timing gate, sizing feasibility, quote quality, run-window length, and soak hardening threshold.
   - Acceptance: classify `soak_maker_submits_too_low` as expected run-window behavior, candidate tuning issue, report threshold mismatch, or `UNKNOWN`.
   - Status: complete. Packet 1 classified the finding as a maker-submit enforcement taxonomy gap and updated `ops/soak_budget.yaml` so maker-scope `fair_probability_missing` is non-actionable for this enforcement path.
   - Replay proof: current-code replays of `9d3c3225-13b6-4a12-8dd4-fb51a6d666e6` and `7e0a7dcf-947a-4d88-9f0c-9a6790ed6b69` pass with `ok=true` and `finding_count=0`.
   - Runtime proof: 5-minute smoke run `ec26dedd-84ee-4cc9-9f5f-d448ea834f9d` verified maker submit enforcement (`1` required, `1` observed), while correctly failing canonical validation because a 5-minute run is below the canonical 10-minute soak budget.

## Closed Historical Follow-Up
1. Inspect reduce-only recovery cap availability:
   - Target surfaces: local reject context, flat/non-flat exposure state, recovery payload construction, and report taxonomy.
   - Acceptance: classify as flat/wrong-side no-op churn, report-only noise, true recovery weakness, or `UNKNOWN`.
   - Status: complete. Report-only visibility added in `scripts/nightly_soak_report.py`.
2. Recheck required-book-feed disconnect context:
   - Target surfaces: first-status/bootstrap rows, active target state, order attempts around disconnected rows, websocket audit evidence.
   - Acceptance: classify as startup/bootstrap transient, recurring market-data defect, or `UNKNOWN`.
   - Status: complete. Current artifacts show first-status bootstrap rows only, with no order attempts/actions and websocket audits passing.
3. Recheck low-price maker floor/cap policy:
   - Target surfaces: maker hard floor, maker max shares, minimum feasible midpoint, no-submission visibility.
   - Acceptance: leave as explicit policy limitation unless strategy owners approve a separate config/strategy packet.
   - Status: complete for this packet. No config/strategy change; remains explicit future policy scope.
4. Superseded historical terminal handoff packet:
   - The old maker->taker handoff patch was later overruled by clarified taker doctrine.
   - Current canonical law does **not** allow maker-originated handoff into taker recovery for accepted normal taker.
   - Current-code proof runs `c0def...` and `3ab3...` surfaced:
     - `maker_to_taker_recovery_handoff_disabled` blocks
     - `waiting_for_maker_exit=0`
     - `normal_submits=0`
     - `lifecycle_residue_override_submits=0`
   - Interpretation: the old deadband question is no longer “should taker inherit earlier?” The current doctrine answer is “no, not for canonical taker.”

## Closeout Group
1. Regenerate checksums after documentation sync.
2. Run `git diff --check`.
3. Run doctrine truth audit.
4. Run a fresh canonical 10-minute paper session after each material smart group that changes runtime behavior.
5. Classify the fresh run strictly: all-green canonical pass, expected/policy-deferred non-green, or defect requiring another surgical packet.

## Post-Maintenance Re-Anchor
`VERIFIED`: the workshop-maintenance detour is complete and BRO stood back up healthy afterward.

Fresh operational-health anchor:
- canonical health run: `2348b33b-d2ba-4b4a-927e-267ddd3dd46e`
- classification: `VALID_ACTIVE`
- validator status: all validator exit codes `0`
- readiness result: highest passing stage `paper`, blocking stage `pilot_live`

Historical note from the health packet:
- a market-family gate was briefly added during runtime hardening and later
  judged to be a misunderstanding of maker/taker independence doctrine
- current Packet 1 runtime has removed that market-ownership / wait family

Important non-overclaim:
- this health packet proved BRO stood back up cleanly after maintenance
- it did not settle premium normal taker edge quality
- runtime activity before the `Masamune Energy Blade Set` pass leaned heavily on maker plus recovery-style taker behavior
- the new `Taker Katana` proof run shows the disabled-handoff law, not premium taker edge

## Recovery Lane Addendum
`VERIFIED`: recovery is not a PnL lane. It is a forced-risk-exit tool.

`VERIFIED`: clarified taker doctrine now states that canonical accepted normal taker should ride to outcome resolution and should not require taker-side recovery after entry.

`VERIFIED`: taker-side recovery authority is being unplugged in current code for canonical taker.

`INFERRED`: the remaining serious recovery packet is now maker/shared-lifecycle design, not “make taker recovery prettier.”

Current doctrine interpretation:
- taker live should not lean on recovery at all in normal accepted-shot lifecycle
- canonical normal taker should not rely on taker-side recovery at all once the shot is accepted
- maker may structurally need cleanup help more often than taker
- if recovery keeps dominating observed taker behavior, treat that as an end-state-shape defect signal or doctrine leak

Future recovery packet questions:
1. Why is any taker-side recovery being needed after accepted normal taker entry if canonical doctrine says the shot should ride to resolution?
2. Is the current recovery tuning too blunt for the intended contingency-only role?
3. Is live maker likely to need occasional recovery while live taker should effectively need none outside anomaly conditions?
4. What exact success metric should define “taker recovery absent except anomaly cases”?

## Explicit Non-Goals
- No live deployment.
- No wallet/risk/live semantics loosening.
- No strategy-aggression tuning.
- No global finance/session-regime module build in this packet.
- No broad refactor.

## Stop Conditions
- Evidence contradicts doctrine.
- A proposed patch touches capital authority without direct proof.
- A run artifact cannot be parsed or lineage cannot be established.
- Any candidate remains ambiguous after inspection; downgrade to `UNKNOWN` instead of patching.
