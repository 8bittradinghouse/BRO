# BRO G-Frame Packet 2: Spinal Cord / Canonical-Proving-Path Failure Chain

## Authority Lock
Current pickup point:
- original packet-local failure specimen:
  `8db2c7fc-630e-4cdb-a2fe-1ba14a93a204`
- clean paper-stage reconfirmation specimen:
  `d30b1c7c-ab05-494e-bfd1-3c5ac1205051`
- latest deep red-team contrast specimen:
  `c6b3bba3-a268-4e3e-9f53-3ae134689ca1`
- governing question:
  - did the original spinal-cord failure chain stay true after repair, or has
    current-code canonical paper-stage closure held?

Authority chain:
1. `docs/PROJECT_TRUTH_STATE.md`
2. `docs/BRO_GFRAME_CORE_FIGHTER_AUDIT_2026-05-01.md`
3. relevant BRO-local doctrine/runbook surfaces named in `Module Intake`
4. `docs/BRO_GFRAME_CORE_RESTORATION_PACKET_PROGRAM_2026-05-01.md`
5. `docs/CURRENT_BASELINE.md` as reference-only
6. packet-local prior blocker:
   - `docs/BRO_GFRAME_PACKET_1_BONES_RELEASE_TRUTH_LOCK_2026-05-01.md`
7. active run artifacts:
   - `canonical_paper_validation.json`
   - `validation_summary.json`
   - `readiness_gate.json`
   - `paper_harness_audit.json`
   - `maker_zero_submit_root_cause_audit.json`
   - `maker_timing_band_decision.json`
   - `maker_timing_band_diagnostic_matrix.json`
   - `maker_participation_waterfall.json`
   - `maker_cannon_late_window_probe_summary.json`
   - `maker_selection_authority_audit.json`

No-change list:
- no timing mutation
- no maker selector tuning
- no new profile or harness
- no report cleanup
- no “try a caliber tweak and see” shortcut

Current blocker being judged:
- whether Spinal Cord closure is still real on current code, not whether
  `8db2...` still fails

## Red-Team Recheck (2026-05-03)
Fresh live specimens:
- `run_id=d30b1c7c-ab05-494e-bfd1-3c5ac1205051`
- `session_id=30390767-37b8-4664-a6c3-d3535ddecf17`
- `run_id=c6b3bba3-a268-4e3e-9f53-3ae134689ca1`
- `session_id=5af6f365-e72c-483b-bdb8-855cdab3fce5`

Historical packet note:
- the original packet below preserves the real `8db2...` failure-chain map
- it should now be read as a historical anti-weapon-drift packet, not as the
  active unresolved owner

Red-team verdict:
- original Packet 2 diagnosis was real on `8db2...`
- current code no longer matches that no-participation / lag-verification choke
  as the stable owner of the lane
- the packet remains a valid anti-weapon-drift firewall, but it is no longer
  the active unresolved owner
- Packet 2 should not overclaim universal prereq cleanliness on current code;
  `c6b3...` proves paper-stage closure can coexist with mixed active-band
  truth-readiness on some target refs

Current recheck evidence:
- `canonical_paper_validation.json` on `d30...` says:
  - `status=pass`
  - `highest_passing_stage=paper`
  - `blocking_stage=pilot_live`
  - `runtime_classification=VALID_ACTIVE`
  - `reports_complete=true`
- live runtime tape in the doctrinal `15-20s` band shows:
  - `maker_allowed=true`
  - `maker_prereq_ok=true`
  - no current-run `token_lag_not_verified_for_maker`
- live actionability survived deep enough to produce:
  - `counter.order_submission_accepted_maker=1.0`
  - `counter.fills=5.0`
- current submit/reject shape is now above the old spinal choke:
  - `maker_selection_authority_audit.row_count=6`
  - `current_decision_distribution={"submit_rejected":5,"submitted":1}`
  - `blocked_count_by_canonical_reject_reason={}`
  - local live rejects are sizing-shaped, not lag-verification-shaped
- `soak_hardening_gate.json` remains `ok=true` with one non-blocking finding:
  - `soak_maker_submits_too_low:1.000000<min:50.000000`
- `canonical_paper_validation.json` on `c6b3...` says:
  - `status=policy_failed`
  - `highest_passing_stage=paper`
  - `blocking_stage=pilot_live`
  - `runtime_classification=VALID_ACTIVE`
  - `reports_complete=true`
- live/current-code contrast evidence on `c6b3...` sharpens the closure call:
  - live status tape does not stay uniformly prereq-clean:
    - `gauge.doctrine_maker_prereq_failure_count` intermittently rises to
      `2.0`
    - the same snapshots keep:
      - `gauge.latency_verifier_state=2.0`
      - tracked `leadlag_verified=1.0` token gauges on the visible active set
  - the completed report bundle does not re-prove the old lag blocker as the
    owner:
    - no persisted report artifact on `c6b3...` attributes the active-band seam
      to `token_lag_not_verified_for_maker`
  - late-window probe on the same run shows mixed active-band truth on
    different target refs:
    - one path survives to:
      - `order_submission_accepted_maker=1`
      - `fill_rows=4`
      - `full_cannon_candidate_count=1`
    - other `15-20s` rows still remain truth-thin with:
      - `market_reference_class=not_available`
      - `market_reference_mode=missing`
      - `secondary_oracle_status=unknown`
      - `block_reason=maker_requires_ws_book_source`
    - bounded but non-authoritative `15-20s` rows also persist with:
      - `market_reference_class=bounded_approximation`
      - `dominant_reject_reason=market_reference_not_authoritative`
  - current submit/reject shape on `c6b3...` is still above the old spinal
    choke:
    - `maker_selection_authority_audit.row_count=4`
    - `current_decision_distribution={"submit_rejected":3,"submitted":1}`
    - `blocked_count_by_canonical_reject_reason={}`
    - active-window local rejects are sizing-shaped or
      truth-reference-shaped, not lag-verification-shaped

## Historical Rehardening Gate
Mission frame:
- determine whether the active failure is core-frame or weapon-only

Doctrine frame:
- current runtime must be judged inside the intended `15-20s` maker doctrine,
  not the old `50-60s` drift posture

Authority frame:
- canonical validation, readiness, paper harness audit, and active maker
  diagnostic artifacts are the certifying surfaces for this question

Pathology frame:
- disease: active-band truth starvation before actionable maker participation
- symptoms:
  - `maker_submits=0`
  - `quote_uptime_ratio=0.0`
  - `NON_PROMOTABLE_NO_PARTICIPATION`

Semantic frame:
- this is not primarily a report-language problem
- current artifact bundle points to upstream runtime/proving starvation

Intervention frame:
- smallest correct-layer move is to classify the choke as a core-frame forensic
  packet and localize the starvation chain

Drift frame:
- high risk if zero submits are misread as generic maker-tuning justification

Proof frame:
- closure requires active-band prerequisite truth to survive deep enough to
  produce quoteable / meaningful participation rows

Failure-signature frame:
- current diagnostics show:
  - structurally healthy proving chain
  - honest non-promotable classification
  - zero active-band quoteability

Stop-the-line status:
- `TRIGGERED` against weapon-tuning promotion

Go / no-go:
- `GO` for core-frame forensic diagnosis
- `NO-GO` for weapon-primary tuning

Real problem:
- under restored doctrine timing, the fighter reaches the active maker band but
  does not develop actionable truth/quoteability inside it

Authoritative surface:
- `readiness_gate.json`
- `maker_participation_waterfall.json`
- `maker_timing_band_diagnostic_matrix.json`
- `maker_zero_submit_root_cause_audit.json`

Surface purpose:
- determine whether the active canonical-path failure is a fighter-core
  truth/readiness collapse or a downstream/weapon-side illusion

Disease vs symptom:
- disease: upstream truth starvation / prequote prerequisite failure
- symptom: zero quote uptime and zero maker submits

Authority owner:
- canonical path and current run truth ownership:
  - `canonical_paper_validation.json`
  - `validation_summary.json`
  - `readiness_gate.json`
  - `paper_harness_audit.json`
- release-truth closure limitation for this run remains owned by:
  - `docs/BRO_GFRAME_PACKET_1_BONES_RELEASE_TRUTH_LOCK_2026-05-01.md`

Smallest correct-layer move:
- keep this lane classified as core-restoration and push into per-token
  lag/discovery/confirmation truth, not weapon tuning

What proves closure:
- for spinal-cord closure specifically:
  - a current-code paper-stage pass on present doctrine
  - active-band rows that survive far enough to become truth-sufficient,
    quoteable, and meaningfully participatory on that same closure-grade run
- lesser signals such as non-zero quote uptime or non-zero participation count
  only as forensic progress, not closure

What this packet must not change:
- timing doctrine
- selector doctrine
- profile / harness topology

## Historical Module Intake
Authoritative sources:
- `docs/PROJECT_TRUTH_STATE.md`
- `canonical_paper_validation.json`
- `validation_summary.json`
- `readiness_gate.json`
- `paper_harness_audit.json`
- active maker zero-submit / timing-band diagnostics

Original failure-specimen evidence anchors:
- canonical validation:
  - `status=policy_failed`
  - `highest_passing_stage=none`
  - `blocking_stage=paper`
  - `runtime_classification=NON_PROMOTABLE_NO_PARTICIPATION`
  - full run lineage complete
- validation summary:
  - `overall_exit_code=2`
  - only `paper_harness_audit` and `soak_hardening_gate` fail
  - determinism is clean across edge and non-edge audits
- paper harness audit:
  - proving lineage complete
  - run integrity `ok=true`
  - runtime classification:
    `NON_PROMOTABLE_NO_PARTICIPATION`
  - `meaningful_participation=0.0`
  - `active_targets_seen=1.0`
- readiness gate:
  - `quote_uptime_ratio=0.0`
  - `runtime_meaningful_participation=0.0`
  - `maker_reference_direct_midpoint_activity=150.0`
  - `maker_reference_bounded_fallback_activity=26.0`
  - `maker_reference_direct_midpoint_action_activity=0.0`
  - `maker_reference_bounded_fallback_action_activity=0.0`
  - failure on paper-stage `min_quote_uptime_ratio`
- maker zero-submit root-cause audit:
  - `zero_submit_classification=upstream_starvation`
  - `decision_readiness=ready_for_truth_packet`
- maker timing band decision:
  - `recommended_timing_action=no_timing_change_truth_or_quoteability_dominant`
- maker timing-band diagnostic matrix:
  - in `15_to_20s`, `runtime_stage_allowed_count=20`
  - `authoritative_reference_count=12`
  - `geometry_viable_count=11`
  - `depth_met_count=0`
  - `desired_quote_present_count=0`
  - `dual_oracle_confirmed_count=0`
  - `full_cannon_candidate_count=0`
- participation waterfall:
  - `total_maker_rows=320`
  - `stage_band_allowed_rows=80`
  - `prequote_prereq_pass_rows=0`
  - terminal paths:
    - `stage_band_excluded=300`
    - `prequote_prereq_blocked=80`
- late-window probe summary:
  - `late_window_raw_row_count=80`
  - `full_cannon_candidate_count=0`
  - `runtime_secondary_oracle_confirmation_distribution={"not_confirmed":80}`
  - `latent_market_truth_class_counts={"truth_thin":80}`

Downstream consumers:
- readiness gate
- paper harness audit
- soak hardening gate
- next packet selection
- weapon authorization gate

Stale surfaces that must not dominate judgment:
- earlier passing maker runs under different timing posture
- old selector-choke narratives
- peak-hours economics questions

## Historical Contradiction Matrix
- canonical validator transport is mostly healthy
- current fighter still fails paper stage
- active maker band is reached
- active maker band still yields zero quoteable / zero participatory rows
- selector authority audit is empty
- therefore the main choke is upstream of submit/selector behavior

## Historical Pass 1
Top-down authority preservation findings:
- canonical path itself is intact:
  - reports complete
  - determinism consistent
  - validator chain mostly green
- active failure happens at paper-stage promotion, not at log integrity or audit
  transport
- the fighter reaches the doctrinal band:
  - `strategy.maker_competitiveness.timing_gate_min_sec_to_expiry=15.0`
  - `strategy.maker_competitiveness.timing_gate_max_sec_to_expiry=20.0`
  - `runtime.held_preexpiry_reduce_only_sec=15.0`
- active-band diagnostics show truth starvation:
  - no depth-met rows
  - no desired-quote-present rows
  - no dual-oracle-confirmed rows
  - no full-cannon candidates
- participation waterfall shows the maker path dies before prequote prereqs ever
  pass

Pass 1 verdict:
- the canonical spinal cord is structurally healthy
- the active failure is inside runtime actionability / truth readiness

## Historical Pass 2
Bottom-up reinterpretation findings:
- downstream consumers are largely honest on this packet:
  - soak hardening gate reports zero participation and quote uptime failure
  - readiness gate reports paper-stage failure on quote uptime
  - paper harness audit preserves bounded realism and non-promotable runtime
    classification
- maker selection authority audit shows `row_count=0`, so the current choke is
  not a selector-reject storm
- maker timing-band decision explicitly recommends:
  - `no_timing_change_truth_or_quoteability_dominant`

Pass 2 verdict:
- downstream reporting is not inventing a fake choke here
- the failure classification is runtime-core enough to keep this in the
  core-restoration lane

## Historical Dependency Review
Upstream dependency:
- Packet 1 limits what this run can certify
- this packet can diagnose present behavior, but not certify final closure

Downstream blast radius:
- high
- if misclassified as weapon work, later tuning could proceed beneath a weak
  fighter

False-closure risk:
- mistaking structurally healthy validator transport for a healthy fighter

No-shortcut zones:
- do not “fix” this by tuning maker caliber first
- do not mutate timing doctrine before truth starvation is understood
- do not treat selection logic as the choke without evidence
- do not treat report cleanup as the primary fix

## Historical Drift Register Delta
- `D-014 | Probe-to-weapon drift`
  - zero maker submits could be misread as weapon-tuning authorization
- `D-015 | Report/runtime drift`
  - structurally healthy validator outputs could be mistaken for fighter health
- `D-016 | Scope drift`
  - peak-hours economics or selector tuning could displace the core-frame choke
- `D-017 | Momentum drift`
  - “no timing change” could be ignored in favor of familiar tuning motion

## Historical Ambiguity Register Delta
- `canonical path healthy`
  - means validator/replay transport is functioning
  - forbidden misread:
    - the fighter is healthy
- `upstream starvation`
  - means rows die before quoteable/actionable participation
  - forbidden misread:
    - mere lack of aggression
- `core-frame maker issue`
  - means maker is exposing a frame/readiness defect
  - forbidden misread:
    - maker optimization is authorized

## Historical Packet Verdict
- `Needs Work`

Reason:
- the canonical path is healthy enough to expose the fighter honestly
- the active fighter still fails before meaningful participation inside the
  doctrinal maker band

## Historical Closure Matrix Update
What must become true:
- active-band rows must survive far enough to become:
  - truth-sufficient
  - quoteable
  - meaningfully participatory

Required proof artifact:
- current-code run on present doctrine showing:
  - paper-stage pass
  - active-band prerequisite pass rows
  - quoteable and meaningfully participatory active-band rows
  - explicit resolution of the per-token lag / discovery / confirmation choke

Progress indicators that do not certify closure:
- non-zero active-band quote uptime
- non-zero meaningful participation

Currently missing proof:
- any active-band row on `8db2...` that reaches prequote prerequisite pass
- any quoteable active-band row
- any meaningful participation on current doctrine

## Historical Packet 2 Call
`VERIFIED`:
- the active failure chain is not “selector weirdness”
- the active failure chain is not “report-only distortion”
- the active failure chain is a core-frame truth/readiness starvation problem on
  the canonical path

`INFERRED`:
- the next high-ROI forensic lane remains per-token lag-verification,
  discovery-refresh continuity, and confirmation truth inside the current
  doctrine-restored maker band
- this remains a core-restoration packet, not weapon tuning
- if a later run earns non-zero participation without a paper-stage pass, that
  is progress evidence only, not spinal-cord closure
- this packet is the active anti-weapon-drift firewall for the maker lane and
  must hand forward into core-frame restoration, not selector/tuning relapse

## Current Recheck Call
`VERIFIED`:
- the original `8db2...` packet diagnosis was accurate for that specimen
- the old spinal-cord no-participation failure chain no longer owns current code
- fresh live specimens `d30...` and `c6b3...` both keep the active frontier at
  `pilot_live` while passing the canonical paper stage
- `d30...` is a clean reconfirmation specimen:
  - live doctrine rows survive the maker band with:
    - `maker_allowed=true`
    - `maker_prereq_ok=true`
  - the old emitted blocker `token_lag_not_verified_for_maker` does not recur
    on that specimen
- `c6b3...` proves the stronger and more durable current truth:
  - the old lag blocker is no longer the stable owner of the lane
  - paper-stage closure can still coexist with intermittent prereq-failure
    spikes and truth-thin `15-20s` rows on some target refs
  - current code therefore is not universally prereq-clean across all
    maker-allowed rows
- current-code Packet 2 closure is stable-owner closure, not universal
  elimination of every active-band truth-readiness seam

`INFERRED`:
- current active limits now sit above Spinal Cord in submit scarcity / later
  pilot-live economics and in the still-open Brain / Nervous / Grip / Bones
  lanes
- current mixed-path truth-thin rows on `c6b3...` are better classified as
  bounded residual runtime truth-readiness seams than as a reopening of the old
  spinal owner
- reopening generic maker-surface spinal forensics now would be drift
