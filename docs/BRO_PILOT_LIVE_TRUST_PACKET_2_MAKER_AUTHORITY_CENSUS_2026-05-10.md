# BRO Pilot-Live Trust Packet 2: Maker Authority Census (2026-05-10)

## Purpose
- `VERIFIED`: this is the packet-local maker authority census for `Packet 2`.
- `VERIFIED`: it materializes the authority-census blank space named in the Packet 2 entry artifact.
- `VERIFIED`: it exists to say which surfaces currently own maker behavior, which only validate or summarize it, and which remain under challenge.

## Authority Boundary
- `VERIFIED`: this file is a census board, not a runtime root.
- `VERIFIED`: active Packet 2 truth still belongs first to:
  - `docs/BRO_PILOT_LIVE_TRUST_PACKET_2_MAKER_QUALIFICATION_2026-05-10.md`
  - `docs/BRO_PILOT_LIVE_TRUST_BOARD_SINK_2026-05-05.md`
  - `docs/BRO_PILOT_LIVE_TRUST_QUALIFICATION_PACKET_PROGRAM_2026-05-05.md`
  - `docs/OPEN_LIMITATIONS.md`
  - `docs/NEXT_PACKET_PLAN.md`
  - `docs/PROJECT_TRUTH_STATE.md` as broad repo truth only
- `VERIFIED`: this board may classify, fence, and challenge surfaces, but it does not authorize tuning or live arming.

## Root-Authority Census
| Surface | What it currently owns | Owner class | Packet 2 call | Notes |
| --- | --- | --- | --- | --- |
| `BRO_EDGE_DOCTRINE.txt` + `BRO_OUTCOME_TRUTH_DOCTRINE.txt` | canonical edge/outcome meaning and truth vocabulary | doctrine root | `KEEP` | stronger than packet-local interpretation |
| `executor.py` maker pre-eligibility path | `maker_requires_ws_book_source`, `market_reference_not_authoritative`, and authoritative market-reference filtering before maker eligibility | runtime root | `KEEP` | upstream truth-thin rejection is current steel, not historical story |
| `prodesk/order_manager.py` maker selection gate | timing window, secondary-oracle confirmation, repeat-target calm caps, depth comparison leaf, and cancel-on-gate-reject flow | runtime root | `KEEP NOW / LEAF-BY-LEAF REVIEW` | active behavior owner, but not every leaf is equally legitimate |
| `configs/profiles/paper_universal.yaml` maker competitiveness leaves | active paper maker runtime leaf values such as maker-open `15s`, taker-handoff `7s`, `$100`, `min_depth_multiple=1.5`, and calm caps | active profile leaf | `KEEP` | this is the current paper lane posture, not the whole doctrine universe |
| `prodesk/gateway.py` + `auth.enforce_post_only` | maker-safe `postOnly` transport enforcement | safety interlock | `KEEP` | valid safety steel, not optional flavor |
| `prodesk/risk.py` + risk config leaves | order-rate, exposure, open-order, and recovery-headroom interlocks | safety interlock | `KEEP` | economics cannot outrank capital/rate safety |
| `scripts/edge_truth_audit.py` + `scripts/order_lifecycle_audit.py` + `scripts/outcome_truth_audit.py` | validator readout of emitted decision, lifecycle, and outcome truth | validator surface | `KEEP` | stronger than wrappers, weaker than runtime roots |
| `scripts/nightly_soak_report.py` | downstream consumer/wrapper summary for operators and later tools | downstream consumer | `KEEP BUT FENCE` | useful readout, not owner-law |
| `scripts/bro_metric_harvest.py` / `FMA` | cross-run compression, labels, and reusable engineering shorthand | support-only shop tool | `KEEP BUT FENCE` | useful instrument, never runtime authority |
| `docs/SOLAR_SLUG_MAKER_CIRCUIT_SCHEMATIC.md` + `docs/SOLAR_SLUG_MAKER_TRUTH_SEMANTICS_MAP.json` | pathway legend, truth-population guardrails, and packet-local classification overlay | support-only atlas | `KEEP BUT FENCE` | descriptive map, not live owner steel |
| `docs/GALAXY_MEGA_MAKER_CANNON_DOCTRINE_PROPOSAL_2026-04-28.md` | design intent and challenge target for current maker posture | blueprint intent | `KEEP BUT FENCE` | may challenge runtime, may not replace it automatically |

## Immediate Packet 2 Calls
- `VERIFIED`: `market_reference_not_authoritative` and `maker_requires_ws_book_source` are present-tense runtime owners on truth-thin maker rows, not relics from the old lag story.
- `VERIFIED`: the current paper maker lane really does carry live runtime leaves for maker-open `15s`, taker-handoff `7s`, `$100`, secondary confirmation, and repeat-target calm.
- `VERIFIED`: `selection_gate.min_depth_multiple` is back in active paper alignment with the stronger maker cannon posture:
  - code fallback preserves `1.5`
  - blueprint lineage preserves `1.5`
  - current paper profile now carries the live leaf at `1.5`
  - Packet 2 may therefore treat the depth-multiple gate as current paper owner-law again
- `VERIFIED`: `FMA`, the admission shadow, and the schematic/map remain useful, but they only speak inside fences.

## Owner-Law Summary
- `VERIFIED`: current strongest runtime roots for maker Packet 2 are still runtime and active profile surfaces, not shop tools.
- `VERIFIED`: validator surfaces are essential for hostile rereads but do not outrank the emitting runtime roots.
- `VERIFIED`: support tools, atlases, and blueprints matter most when they are clearly fenced and clearly compared against stronger owners.
