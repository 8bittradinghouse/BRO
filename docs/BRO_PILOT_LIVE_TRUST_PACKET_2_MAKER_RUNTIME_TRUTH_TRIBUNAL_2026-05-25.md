# BRO Pilot-Live Packet 2: Maker Runtime-Truth Tribunal

## Classification
- `active packet owner`
- `pickup bridge`
- diagnosis-first maker packet opened on `2026-05-25`
- this is not a tuning packet and not a live-arming packet

## Supersession Note (2026-05-26)
- Packet 2B and Packet 2C materially supersede the older readiness-family
  semantics captured in this tribunal packet.
- references below to:
  - `latency_not_armed_for_maker`
  - `token_lag_not_verified_for_maker`
  - `token_score_below_maker_min`
  - `fair_probability_unavailable`
  - `latency_regime_change`
  - `armed` / `probation` / `disarmed`
  are historical pre-excision evidence only, not current owner-law.
- references below to:
  - `token_readiness_*`
  - `aggregate_signal_*`
  - `latency_signal_posture`
  - `latency_signal_degraded`
  - `signal_degraded_ratio`
  are also historical only, not current owner-law.
- current runtime/postrun/report truth no longer routes through readiness /
  latency family blocker or posture authority surfaces.

## Purpose
- prove whether the current maker patient is:
  - healthy selectivity
  - healthy but costly fail-closed readiness
  - real upstream starvation / readiness / geometry disease
  - support-schoolhouse residue impersonating authority
  - or some combination
- preserve runtime truth over schoolhouse truth
- preserve selective lawful execution over participation vanity

## Authority Lock
Current pickup point:
- phase:
  - `pilot_live`
- macro lane:
  - `live trust qualification`
- immediate implementation lane:
  - `Packet 2 maker runtime-truth tribunal`
- downstream lane after this packet:
  - `Packet 1 taker support-authority tribunal`
- wallet lane:
  - still downstream and fail-closed

Authority chain:
1. `docs/JIN_RELOCK_PACK_2026-05-12.md`
2. `docs/PROJECT_TRUTH_STATE.md` as broad repo truth only
3. `docs/NEXT_PACKET_PLAN.md`
4. `docs/OPEN_LIMITATIONS.md`
5. `docs/BRO_PILOT_LIVE_TRUST_BOARD_SINK_2026-05-05.md`
6. `docs/BRO_PILOT_LIVE_TRUST_QUALIFICATION_PACKET_PROGRAM_2026-05-05.md`
7. `docs/BRO_PILOT_LIVE_TRUST_PACKET_2_MAKER_QUALIFICATION_2026-05-10.md`
8. `docs/BRO_PILOT_LIVE_TRUST_PACKET_2_MAKER_AUTHORITY_CENSUS_2026-05-10.md`
9. runtime owners:
   - `executor.py`
   - `prodesk/order_manager.py`
   - `prodesk/gateway.py`
   - `prodesk/risk.py`
   - `configs/profiles/paper_universal.yaml`
10. consumer and support surfaces:
   - `scripts/nightly_soak_report.py`
   - `maker_blocker_ledger.json`
   - `maker_participation_waterfall.json`
   - `maker_zero_submit_root_cause_audit.json`
   - `maker_quote_integrity_summary.json`

## No-Change Steel
- no generic maker tuning
- no timing-window mutation by instinct
- no depth / queue / fill-probability loosening by frustration
- no live arming
- no wallet-lane substitution
- no reopening already-closed lifecycle / WS-transplant / queue-pressure cuts
  unless new runtime proof beats the active packet docs

## Keep-Now Steel
- lifecycle ownership-entry / timing collision closure
- selector-owned one-sided extinction
- official Polymarket WS/CLOB substrate transplant
- maker phase law:
  - maker opens at `15s`
  - taker handoff at `7s`
- selection depth multiple as active maker law
- queue-depth quote quality as active maker law
- maker submit / partial-fill / reservation / settlement receiving ends

## Packet 2A Implementation Status
- `VERIFIED`: the ordered maker authority contract is now implemented in current
  code without changing maker timing, cannon target, depth multiple,
  queue-depth quality law, fill-probability law, or wallet posture.
- `VERIFIED`: existing runtime-first maker emitter paths now carry canonical
  gate semantics:
  - `maker_gate_stage`
  - `maker_gate_reason`
  - `maker_gate_owner_family`
  - `maker_gate_terminal`
- `VERIFIED`: the active ordered gate chain is now:
  1. `phase_gate`
  2. `truth_reference_gate`
  3. `lane_readiness_gate`
  4. `selection_gate`
  5. `feasibility_gate`
  6. `shared_safety_lifecycle_gate`
  7. `quote_quality_gate`
  8. `submit_path_gate`
  9. `submitted`
  10. `filled`
- `VERIFIED`: support-schoolhouse consumers are now explicitly fenced behind
  canonical blocker truth:
  - `maker_blocker_ledger.json` remains owner
  - `maker_participation_waterfall.json` is support-only
  - `maker_zero_submit_root_cause_audit.json` is support-only
  - `maker_quote_integrity_summary.json` is support-only
  - `readiness_gate.json` and `soak_hardening_gate.json` are posture/policy
    surfaces only
- `VERIFIED`: the postrun gates now emit explicit non-owner markers in their
  JSON (`authoritative_for_runtime_blocker_truth=false`,
  `authoritative_for_execution_lane_blocker_truth=false`,
  `execution_lane_blocker_owner_artifact=maker_blocker_ledger.json`) so they
  cannot quietly pose as execution-lane blocker owners in later audits.
- `VERIFIED`: `secondary_oracle_not_confirmed` is now treated as a later
  `selection_gate` leaf in the canonical maker contract and is no longer legal
  upstream readiness storytelling.

## Evidence Base
Primary runtime set used in this tribunal:
- participating-maker contrast:
  - `f4d16884-844b-4cf6-bdf2-b596bb3ccb43`
  - `1e35a29a-9fba-4563-9e95-b9a38ad360c6`
  - `0b4932f5-d599-4a2f-990e-a4341d2771d0`
- zero-submit / low-submit contrast:
  - `bb592ab0-a5c6-48be-abda-87dcbe78cc52`
- fresh watched maker-runtime specimen:
  - `78a1bab4-da42-4a01-bc2f-7ba1fbe9dcaa`
- Packet 2A closeout watched specimen:
  - `bac209a8-a1d4-42ef-8ada-3950266decf3`
- inert watched contrasts:
  - `17fe07e3-32ec-43e7-950f-2481ac05b9ff`
  - `feb4f9cb-6d95-44c3-9968-b13c15bb666f`

Evidence order used:
1. raw `events_*.jsonl`
2. raw `status_*.jsonl`
3. wallet / receiving-end runtime events
4. canonical report bundle artifacts
5. descriptive-only support artifacts

## Runtime-First Blocker Census
### Participating-maker contrasts
- `f4d16884-844b-4cf6-bdf2-b596bb3ccb43`
  - maker snapshots: `12`
  - maker decision mix:
    - `submitted=4`
    - `submit_rejected=2`
    - `viability_rejected=6`
  - raw maker edge blocker mix:
    - `token_lag_not_verified_for_maker=54`
    - `latency_not_armed_for_maker=16`
    - `maker_no_submission=6`
- `1e35a29a-9fba-4563-9e95-b9a38ad360c6`
  - maker snapshots: `13`
  - maker decision mix:
    - `submitted=1`
    - `viability_rejected=12`
  - raw maker edge blocker mix:
    - `token_lag_not_verified_for_maker=54`
    - `maker_no_submission=8`
- `0b4932f5-d599-4a2f-990e-a4341d2771d0`
  - maker snapshots: `4`
  - maker decision mix:
    - `submitted=1`
    - `viability_rejected=3`
  - raw maker edge blocker mix:
    - `token_lag_not_verified_for_maker=60`
    - `maker_no_submission=3`

### Zero-submit contrast
- `bb592ab0-a5c6-48be-abda-87dcbe78cc52`
  - maker snapshots: `18`
  - maker decision mix:
    - `viability_rejected=18`
  - raw maker edge blocker mix:
    - `token_lag_not_verified_for_maker=54`
    - `maker_no_submission=10`

### Fresh watched specimen
- `78a1bab4-da42-4a01-bc2f-7ba1fbe9dcaa`
  - runtime classification:
    - `VALID_ACTIVE`
  - postrun policy state:
    - `policy_failed` only because
      `performance_latency_inactive_cycles_too_high:88>max:60`
  - maker runtime reached real active-owned markets twice:
    - target admission at `00:13:48Z`
    - target invalidation / standdown at `00:15:01Z`
    - second target admission at `00:18:32Z`
    - second target invalidation / standdown at `00:20:01Z`
  - raw maker edge family on the watched tape:
    - `phase_disallow_maker=104`
    - `token_lag_not_verified_for_maker=32`
    - no maker submit
  - raw taker contrast on the same watched tape:
    - `edge_below_min=72`
    - taker submitted / filled `2`
  - canonical current blocker owner surfaces on the same run:
    - `maker_blocker_ledger.json`
      - `dominant_runtime_blocker_bucket=selection_gate`
      - `dominant_runtime_blocker_reason=launch_safe_selection_insufficient_depth_multiple`
      - `runtime_bucket_distribution.selection_gate=6`
    - `maker_selection_authority_audit.json`
      - `row_count=6`
      - all `6` canonical selection rows blocked for
        `insufficient_depth_multiple`
  - tribunal call:
    - not substrate-inert
    - legal watched-maker proof
    - strengthens mixed-readiness diagnosis and geometry / feasibility
      diagnosis simultaneously

## Runtime-First Blocker Owner Statement
- `VERIFIED`: the canonical current blocker owner remains
  `maker_blocker_ledger.json`, not the descriptive probes.
- `VERIFIED`: on both `bb592...` and `1e35...`, the canonical current artifact
  says:
  - `authoritative_for_runtime_blocker_truth=true`
  - `dominant_runtime_blocker_bucket=selection_gate`
  - `dominant_runtime_blocker_reason=launch_safe_selection_insufficient_depth_multiple`
- `VERIFIED`: raw maker runtime still emits upstream readiness blockers such as
  `token_lag_not_verified_for_maker` and `latency_not_armed_for_maker`.
- `INFERRED`: the truthful read is not "lag blocker is gone."
- `INFERRED`: the truthful read is:
  - raw runtime still carries readiness-family scars
  - current canonical blocker owner demotes the dominant live patient to
    selection / feasibility truth on the rows that actually made it to the
    later-stage selection path

## Suspect-Family Verdict Table
| Family | Verdict | Tribunal call |
| --- | --- | --- |
| lifecycle ownership-entry / timing collision closure | `KEEP AS STEEL` | current packet docs and current runtime do not re-prove the old timing-owner collision |
| selector-owned one-sided extinction | `KEEP AS STEEL` | no current evidence beats the already-landed cut |
| official WS/CLOB substrate transplant | `KEEP AS STEEL` | no current maker truth says the body reverted to the old substrate patient |
| selection depth multiple | `KEEP AS STEEL` | current live math remains honest; do not loosen it by mood |
| queue-depth quote quality | `REOPEN / HOSTILE REGRADE` | later hostile tribunal says this family is suspicious live downstream authority and must prove blueprint legitimacy before staying runtime law |
| maker readiness authority chain | `OPEN NEXT SURGERY FAMILY` | current owner chain is mixed across truth-reference legality, aggregate arming, per-token lag verification, and later prereq semantics |
| timing-band mismatch story | `DEMOTE TO SUPPORT-ONLY` | off-band "better opportunity" story is descriptive-only until runtime-authoritative proof beats current maker-window doctrine |
| geometry / feasibility truth | `OPEN NEXT SURGERY FAMILY` | current surviving runtime patient is accessory geometry / sizing / feasibility truth, not generic inactivity |
| `maker_participation_waterfall.json` | `DEMOTE TO SUPPORT-ONLY` | explicitly subordinate to `maker_blocker_ledger.json` |
| `maker_zero_submit_root_cause_audit.json` | `DEMOTE TO SUPPORT-ONLY` | useful descriptive probe only; must step aside on submit runs |
| stale specimen-local schoolhouse wording in `maker_quote_integrity_summary.json` | `RETIRE / FIRE` | current-truth artifact must not keep teaching old specimen-local packet language |
| target-refresh / WS-unusable churn as primary patient | `DEMOTE TO SUPPORT-ONLY` | still weather / readiness context, not current primary owner by itself |
| downstream maker submit / fill / settlement machinery | `KEEP AS STEEL` | participating-maker runs still reconcile coherently downstream |

## Readiness Authority Tribunal
### What is real
- `VERIFIED`: current maker readiness is not owned by one clean concept.
- `VERIFIED`: raw runtime still shows a maker prereq chain that can fail before
  selection rows matter:
  - `maker_requires_ws_book_source`
  - `market_reference_not_authoritative`
  - `latency_not_armed_for_maker`
  - `token_lag_not_verified_for_maker`
  - maker prereq / oracle support semantics
- `VERIFIED`: current config still makes maker sensitive to:
  - `latency_verifier.min_samples=80`
  - `latency_verifier.score_min_for_maker=0.35`
  - `lifecycle.selection.require_secondary_oracle_confirmation=true`
- `VERIFIED`: the fresh watched run `78a1...` re-proved the live ordering of
  that chain twice:
  1. `targets_updated`
  2. `book_feed_watch_tokens_updated`
  3. `target_refresh_forced_ws_missing_or_unusable`
  4. `latency_regime_change -> probation`
  5. `latency_regime_change -> armed`
  6. `maker_window`
  7. raw `token_lag_not_verified_for_maker`
  8. `owned_market_invalidated`
  9. `targets_standdown reason=no_valid_targets_discovered`
  10. `latency_regime_change reason=no_samples`
- `VERIFIED`: that sequence repeated on two consecutive owned-market epochs in
  the same watched specimen.

### What is not legal anymore
- `VERIFIED`: it is no longer legal to let
  `secondary_oracle_not_confirmed` impersonate the dominant upstream runtime
  boss when raw runtime-first rows are dominated earlier by lag/readiness
  surfaces.

### Readiness owner statement
- `VERIFIED`: current maker readiness is mixed, not singular.
- `VERIFIED`: truth-reference availability and WS-book legality are earlier
  owners than later quote-quality math.
- `VERIFIED`: raw runtime still carries aggregate-arming and per-token
  lag-verification behavior as maker admission prerequisites.
- `INFERRED`: mixed readiness semantics are formally declared the first real
  maker surgery family if runtime mutation is reopened.

## Timing-Band Fit Tribunal
### Current evidence
- `VERIFIED`: current maker doctrine is still:
  - maker gate opens at `15s`
  - taker handoff at `7s`
- `VERIFIED`: `maker_zero_submit_root_cause_audit.json` on `bb592...` reports:
  - `classification_factors=['upstream_starvation','timing_band_mismatch']`
  - `off_band_full_cannon_candidate_count=18`
- `VERIFIED`: that artifact also declares itself:
  - `authoritative_for_canonical_selection=false`
  - `current_owner_artifact=maker_blocker_ledger.json`

### Timing-band legality statement
- `VERIFIED`: current off-band "better opportunity" claims are descriptive-only.
- `VERIFIED`: no runtime-authoritative off-band proof currently beats the
  active maker-window doctrine.
- `VERIFIED`: the fresh watched run `78a1...` still leaves the off-band probe
  in support-only status:
  - `classification_factors=['upstream_starvation','timing_band_mismatch']`
  - `off_band_full_cannon_candidate_count=11`
  - but no runtime-owned off-band row family outranked the active maker band
- `VERIFIED`: current timing mutation is `NO-GO`.
- tribunal decision:
  - `KEEP CURRENT BAND`

## Geometry / Feasibility Tribunal
### What current runtime actually says
- `VERIFIED`: current active maker rows that survive readiness can still fail
  honestly on selection / feasibility math.
- `VERIFIED`: live current-code profile posture includes:
  - `cannon_target_notional_usd=100.0`
  - `maker_min_depth_multiple_band=[1.45,1.5]`
  - `maker_min_depth_multiple=1.45`
  - `max_queue_ahead_size=300.0`
  - `min_expected_fill_prob=0.045`
- `VERIFIED`: the fresh zero-submit contrast `bb592...` and the earlier active
  maker runs all support the narrower patient family:
  - depth multiple
  - queue burden
  - fill-probability realism
  - `non_actionable_geometry`
  - low-price / share-cap / min-notional feasibility
- `VERIFIED`: the fresh watched run `78a1...` materially strengthens that same
  call:
  - canonical selection battle size:
    - `6` rows
  - all `6` blocked for:
    - `launch_safe_selection_insufficient_depth_multiple`
  - market reference truth on those rows:
    - `maker_truth_reference_starvation_summary.primary_truth_starvation_cause=none`
    - `truth_readiness_state_distribution.authoritative_complete=6`
  - snapshot geometry summary:
    - `depth_multiple_vs_cannon_target_summary.max=0.6413778`
    - `cannon_depth_requirement_counts.not_met=6`
    - `secondary_oracle_confirmation_distribution.confirmed=6`

### Geometry owner statement
- `VERIFIED`: the surviving active maker runtime patient is geometry /
  feasibility truth, not generic lane inactivity.
- `VERIFIED`: depth and queue law remain seated and should not be softened
  just because the geometry patient is still open.
- tribunal decision:
  - `OPEN NEXT SURGERY FAMILY`

## Support-Schoolhouse Deauth Tribunal
### Current disposition map
- `maker_blocker_ledger.json`
  - `KEEP AS STEEL`
  - canonical current blocker owner artifact
- `maker_participation_waterfall.json`
  - `DEMOTE TO SUPPORT-ONLY`
  - descriptive-only surface; may not speak current owner law
  - fresh re-proved fake-badge seam on `78a1...`:
    - reports `prequote_prereq_pass_rows.loss_reason_distribution.secondary_oracle_not_confirmed=58`
    - while raw runtime-first watched tape and canonical edge-truth surfaces
      show the real active upstream boss was
      `token_lag_not_verified_for_maker=58`
- `maker_zero_submit_root_cause_audit.json`
  - `DEMOTE TO SUPPORT-ONLY`
  - useful for descriptive hypotheses only
  - must step aside on submit runs
- `maker_quote_integrity_summary.json`
  - logic-pathology and quote-integrity findings remain useful
  - stale specimen-local wording such as:
    - `next_repair_lane="D. Peak-hours confirmation specimen"`
  - tribunal verdict:
    - stale current-truth teaching residue = `RETIRE / FIRE`

### Support-artifact trust verdict
- `VERIFIED`: support artifacts remain useful only inside fences.
- `VERIFIED`: none may outrank raw runtime or `maker_blocker_ledger.json`.

## Receiving-End Revalidation
- `VERIFIED`: participating-maker runs still show coherent downstream truth:
  - one submit leads to one order lifecycle
  - partial-fill chunking preserves remaining size correctly
  - wallet open-order state and reservation move with accepted orders
  - settlement and report closeout remain materially coherent
- `VERIFIED`: no new maker equivalent of the old taker overspend disease was
  re-proved in the receiving ends.
- downstream verdict:
  - `KEEP AS STEEL`

## Fresh Watched Specimen Verdict
- fresh watched behavioral run:
  - `78a1bab4-da42-4a01-bc2f-7ba1fbe9dcaa`
- result:
  - two real owned-market epochs
  - maker reached `maker_window` twice
  - raw runtime died first on readiness (`token_lag_not_verified_for_maker`)
  - surviving canonical selection rows then died on
    `launch_safe_selection_insufficient_depth_multiple`
  - maker submitted `0`
  - taker submitted / filled `2`
  - policy failed only on postrun hardening due
    `latency_sampling_inactive_cycles_max=88 > 60`
- operational meaning:
  - do not call this a substrate-inert maker specimen
  - do call it a real runtime-truth maker specimen that re-proves:
    - mixed readiness authority remains active
    - geometry / depth feasibility remains the dominant surviving
      submit-side patient
    - support-schoolhouse prereq storytelling is still overstating
      `secondary_oracle_not_confirmed`
- inert watched contrasts remain useful only as:
  - substrate-noise comparisons
  - not current maker runtime proof

## Packet 2A Closeout Watched Verdict
- fresh watched closeout run:
  - `bac209a8-a1d4-42ef-8ada-3950266decf3`
- result:
  - `canonical_paper_validation.json`
    - `status=pass`
    - `runtime_classification=VALID_ACTIVE`
    - `promotion_eligible=true`
    - `highest_passing_stage=paper`
    - `blocking_stage=pilot_live`
  - real maker behavior occurred on the watched tape:
    - maker submits=`1`
    - maker fills=`1`
    - taker submits=`1`
    - taker fills=`1`
  - canonical ordered maker gate distribution on the same run:
    - `phase_gate=190`
    - `lane_readiness_gate=60`
    - `selection_gate=1`
    - `feasibility_gate=1`
    - `quote_quality_gate=1`
    - `shared_safety_lifecycle_gate=16`
    - `submitted=1`
  - maker-window-only distribution:
    - `lane_readiness_gate=60`
    - `selection_gate=1`
    - `feasibility_gate=1`
    - `quote_quality_gate=1`
    - `submitted=1`
  - maker-window dominant earliest blocker:
    - `token_lag_not_verified_for_maker`
  - later-stage submit-side diagnostics still show bounded later patients:
    - `launch_safe_selection_insufficient_depth_multiple`
    - `non_actionable_geometry`
    - `quote_quality_skip_queue_depth`
  - support-artifact posture on the same run:
    - `maker_blocker_ledger.json` remains canonical owner
    - `maker_participation_waterfall.json` now reports
      `prequote_prereq_pass_rows.loss_reason_distribution.token_lag_not_verified_for_maker=60`
      and no longer invents upstream `secondary_oracle_not_confirmed`
    - `maker_zero_submit_root_cause_audit.json` steps aside with
      `applicability=not_applicable_submit_run`
    - `maker_quote_integrity_summary.json` stays fenced behind
      `current_owner_artifact=maker_blocker_ledger.json`
- operational meaning:
  - Packet 2A succeeded in exposing the real earliest live maker-window loss
    owner
  - the dominant current live maker patient is still lane-readiness reality,
    specifically per-token lag verification
  - geometry / sizing / feasibility remains the next bounded later-stage
    patient, not the first one

## Completion Outputs
Updated maker suspect-family table:
- `KEEP AS STEEL`
  - lifecycle ownership-entry / timing closure
  - selector-owned one-sided extinction
  - official WS/CLOB substrate transplant
  - selection depth multiple
  - queue-depth quote quality
  - downstream maker receiving ends
- `DEMOTE TO SUPPORT-ONLY`
  - timing-band mismatch probe story
  - `maker_participation_waterfall.json`
  - `maker_zero_submit_root_cause_audit.json`
  - target-refresh / WS-unusable churn as primary owner story
- `RETIRE / FIRE`
  - stale specimen-local schoolhouse wording inside current-truth artifacts
- `OPEN NEXT SURGERY FAMILY`
  - maker readiness authority collapse
  - maker accessory geometry / sizing / feasibility truth

Canonical runtime-first blocker owner statement:
- `maker_blocker_ledger.json` remains the canonical current blocker owner.
- fresh watched strengthening:
  - on `78a1...`, the canonical owner still seats the later-stage selection
    diagnostics at
    `launch_safe_selection_insufficient_depth_multiple` even while the raw
    watched tape preserves an earlier readiness-family choke on
    `token_lag_not_verified_for_maker`
  - on `bac209...`, the canonical owner now exposes both layers cleanly:
    - whole-lane earliest blocker remains `phase_disallow_maker`
    - maker-window earliest blocker is `token_lag_not_verified_for_maker`
    - later-stage selection diagnostics remain
      `launch_safe_selection_insufficient_depth_multiple`

Readiness owner statement:
- current maker readiness is mixed and is not yet one clean owner chain.
- fresh watched strengthening:
  - current runtime can admit targets, bootstrap WS watch, arm aggregate
    latency, and still fail maker fire on per-token lag readiness before
    selection law gets its turn

Timing-band legality statement:
- `KEEP CURRENT BAND`

Geometry / feasibility owner statement:
- accessory geometry / sizing / feasibility is the surviving bounded
  later-stage maker runtime patient after lane-readiness losses are exposed.

Support-schoolhouse disposition map:
- support probes stay subordinate
- stale specimen-local packet teaching must be retired

Downstream receiving-end verdict:
- maker submit / fill / reservation / settlement machinery remains steel on
  participating-maker runs

## Recommendation
- `VERIFIED`: a true maker surgery packet should reopen only after this
  runtime-truth tribunal is accepted as the current diagnostic map.
- Packet 2C shared latency-authority family excision is now closed on watched
  proof `4ee0cf47-dec6-44b9-bdd2-764abf2c3b47`:
  - no current maker row dies on readiness / latency-family semantics
  - maker submitted `3` orders and produced `4` fill events
  - taker did not reopen as a hidden readiness patient in the same specimen
- hostile downstream-maker reread is now closed through:
  - `docs/BRO_PILOT_LIVE_TRUST_PACKET_2_DOWNSTREAM_MAKER_FILTER_HOSTILE_TRIBUNAL_2026-05-26.md`
- lifecycle pair verdict:
  - `open_order_cleanup_required`
  - `settlement_hold_required`
  - remain explicit resolve-phase lifecycle/state truth
  - are demoted from `next active maker patient` authority
- downstream maker filter verdict:
  - `launch_safe_selection_insufficient_depth_multiple` = provisional
    blueprint steel
  - `non_actionable_geometry` = blueprint-aligned early actionability truth
    pending leaf-specific reread
  - `quote_quality_skip_queue_depth` = first suspicious live downstream
    authority
  - `quote_quality_skip_fill_probability` = same hostile family even when not
    dominant in the watched Packet 2C specimen
- recommended first runtime surgery family:
  - `maker quote-quality authority tribunal / surgery`
  - scope:
    - `quote_quality_skip_queue_depth`
    - `quote_quality_skip_fill_probability`
    - first decide whether the queue/fill-probability family belongs as live
      runtime law or should be demoted back to shadow / observability
    - do not reopen shared readiness-authority surgery unless fresh runtime
      proof beats Packet 2C
- recommended second runtime surgery family:
  - `maker accessory geometry / sizing / feasibility truth`
- taker interpretation:
  - current watched Packet 2C specimen shows taker blocked honestly on
    `edge_below_min`
  - do not treat taker as the active patient again without stronger live proof
- support-schoolhouse cleanup may proceed as bounded hygiene work without being
  mistaken for runtime closure.
