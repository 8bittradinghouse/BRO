# BRO Pilot-Live Trust Packet 2: Maker Small-Loss + Scar-Tissue Board (2026-05-10)

## Classification
- `historical-only`
- this is a Packet 2 wound-lineage board sink
- any old stage-family language here is lineage evidence only

## Purpose
- `VERIFIED`: this board materializes the Packet 2 small-loss anatomy trace and scar-tissue family trace.
- `VERIFIED`: it exists to keep the recurring small negative maker dollars and the compensator-family question explicit in one place.
- `VERIFIED`: current-code canon proof now says the old recovery-tied
  small-loss family is not an active Packet 2 patient unless its exact
  repeated tiny-bleed pattern returns.

## Small-Loss Owner Candidates
| Candidate owner population | Surface family | Why it matters | Current Packet 2 call |
| --- | --- | --- | --- |
| decision-cycle truth | no-submit overlays, quote-quality skips, and selection-gate rejects | explains abstain geometry and participation pain, but not recurring negative maker dollars by itself | `SUBORDINATE / TRACE` |
| submit truth | submit population and incomplete accumulation | can hide whether maker pain is in participation, maturation, or lifecycle geometry | `SECONDARY TRACE` |
| lifecycle truth | submit/fill/cancel linkage and gap classes | loss shape can be faked if lifecycle identity is weak | `SECONDARY TRACE` |
| fill-event truth | execution capture vs adverse selection | execution economics matter, but favorable fills can still coexist with bad completed decisions | `SUPPORTING ONLY` |
| complete-outcome truth | completion bias, multi-fill wound geometry, same-target repeat clusters, complement-pair clusters | strongest current direct candidate owner of recurring small negative maker dollars | `PRIMARY CURRENT OWNER CANDIDATE / SURGERY-PILE CANDIDATE` |
| market-truth substrate | authoritative vs bounded-low market reference basis | upstream truth-thin rows can poison every downstream economic reading | `FOUNDATION TRACE` |

## Specimen-Backed Small-Loss Read
- `VERIFIED`: current Packet 2 specimens point hardest at `complete-outcome` maker truth, not pure fill-event execution, as the current candidate owner of the recurring small negative maker dollars.
- `VERIFIED`: the accessible historical Packet 2 small-loss specimens `8bfb...`,
  `e675...`, and `ed184...` all came from dirty-tree commit `519f6ed...` under
  config fingerprint `bc887339...` with packet-era timing posture `50-60 / 50`;
  they are ancestry and wound-lineage truth, not current doctrine.
- `VERIFIED`: watched specimen `8bfb70eb-c942-48eb-87ff-b9628b3098c7` showed:
  - maker total rows: `61`
  - maker complete rows: `35`
  - maker incomplete rows: `26`
  - maker complete incorrect rows: `28`
  - maker complete incorrect rows with favorable execution: `28`
  - maker complete multifill rows: `22`
  - maker complete multifill incorrect rows: `20`
- `VERIFIED`: watched specimen `ed184f61-c453-4511-a5e5-3fa24271c191` showed:
  - maker complete rows: `5`
  - maker complete incorrect rows: `3`
  - maker complete incorrect rows with favorable execution: `3`
- `VERIFIED`: watched specimen `e675467e-368a-49db-bc03-f35c96aebba8` showed:
  - maker complete rows: `8`
  - maker complete incorrect rows: `8`
  - maker complete incorrect rows with favorable execution: `8`
- `VERIFIED`: across those three historical small-loss specimens, every
  outcome record still used:
  - `decision_reference_basis=direct_book_midpoint`
  - `eval_reference_basis=edge_market_midpoint_series`
  so historical one-sided fallback / missing-reference substrate is not the
  proximate owner of that wound family even though `market-truth substrate`
  remains the foundation trace for current Packet 2.
- `VERIFIED`: same-target repeat geometry is not noise inside that population:
  - multiple repeated `target_ref` clusters in `8bfb...` carried multi-fill pain and meaningful negative decision-debt sums
- `VERIFIED`: multi-fill persistence sharply worsened the wound:
  - in `8bfb...`, complete rows with `fill_count >= 3` were `14/14 incorrect`
  - in `e675...`, complete rows were `8/8 incorrect` across both `fill_count=1`
    and `fill_count=2`
  - in `ed184...`, single-fill completes were the only healthy complete subset,
    while all `fill_count >= 2` completes were incorrect
- `VERIFIED`: low-price / sizing-conflict families are contributory in some
  packet-era specimens but not universal:
  - `8bfb...` and `e675...` still lost across low, middle, and high price bands
  - so the recurring small-loss wound is not reducible to low-price stock alone
- `INFERRED`: the strongest current small-loss story is:
  - participation friction lives upstream in quote-quality / queue-depth / replace-guard truth
  - the recurring negative maker dollars themselves most likely crystallize
    later, inside complete-outcome geometry
  - completion bias, multi-fill persistence, and repeat-target /
    complement-pair clustering are the main wound multipliers inside that
    population

## Support-Surface Seam
- `VERIFIED`: canonical outcome-owner truth lives first in emitted runtime truth plus raw `outcome_truth_records.jsonl` / canonical `outcome_truth_audit` populations.
- `VERIFIED`: `scripts/bro_metric_harvest.py` compresses maker wound summaries after the fact; those ratios are useful but are not themselves the highest owner-law surface.
- `VERIFIED`: `FMA` and archive summaries are shop compression layers and may vary by archive class; they must not outrank the raw outcome population they summarize.
- `VERIFIED`: Packet 2 answer order for this lane is:
  1. emitted runtime + raw outcome records
  2. canonical outcome-truth audit populations
  3. compressed maker wound summaries
  4. operator-facing shop tools
- `VERIFIED`: if those layers disagree, the stronger lower layer wins and the upper layer becomes a support-surface drift signal.

## Scar-Tissue / Bug-Chasing Families
| Family | Current touchpoint | Why it is under challenge | Current call |
| --- | --- | --- | --- |
| `dust` family | descriptive `position_dust_*` plus formerly authoritative `held_dust_*` / hard-degraded exemption flow | dust shadow / enforcement authority and dust-based severity suppression were not justified as intended maker-cannon steel | `CURRENT-CODE CUT / WATCHED PROOF PENDING` |
| maker recovery family | historical recovery-lineage truth plus formerly maker-authoritative relaxations | maker recovery privilege cluster did not belong in intended maker-cannon doctrine; current owner cut is landed and the remaining tail is archaeology / compat only | `CUT / HISTORICAL-COMPAT LINEAGE` |
| queue-pressure family | historical queue-mutation lineage only; current code removed the runtime/config/report family and keeps only legacy-config ignored compatibility plus archive replay awareness | present authority collapsed cleanly without harming core maker steel, and historical packet lineage still remains readable | `CUT / HISTORICAL-ONLY LINEAGE` |
| support-shadow / probe family | `maker_fight_admission_shadow*`, late/mid-window probes, zero-submit root-cause, quote-starvation / quote-integrity / selection-authority counterfactual report artifacts | recent watched specimens show tiny live maker participation under much larger synthetic/support populations, so this family is narrating far more than it owns | `SURGERY-PILE CANDIDATE / REPORT-SIDE FIRST` |
| accessory competitiveness bundle | live `edge_scale_*`, `size_mult_max`, `spread_mult_min`, `requote_delta_mult_min` maker shaping leaves | house-added quote/size/requote shaping bundle exceeds the clean cannon doctrine and is not yet runtime-proven as must-keep steel | `SURGERY-PILE CANDIDATE / RUNTIME TRIBUNAL` |
| accessory maker sizing scaler bundle | `maker_depth_target_*` plus `maker_liquidity_tod_*` live shapers | fixed-shot + `1.5x` depth doctrine does not automatically justify extra depth-target or overnight liquidity scaling; these leaves can distort runtime interpretation | `SURGERY-PILE CANDIDATE / RUNTIME TRIBUNAL` |
| duplicate selection-authority seams | selection-gate `allowed_stages` fallback plus dormant selection-gate timing fields | the stronger timing/authority owners already exist, so extra gate carriers risk reintroducing duplicate authority by shape alone | `FENCE / LATER TRIBUNAL` |
| same-target calm caps | repeat-target and repeat-side submit caps in the selection gate | looks more like intentional cannon selectivity than bug-chasing residue, but still needs outcome-owner proof | `KEEP` |
| post-only cross guard | post-only clamp/reject path | valid safety interlock unless it starts carrying claims it was not built to own | `KEEP` |

## Current Specimen-Backed Family Read
- `VERIFIED`: queue-depth and fill-probability friction are loud in current specimens:
  - `8bfb...`: `quote_quality_skip_queue_depth=35`, `quote_quality_skip_fill_probability=18`, `replace_guard_min_rest=91`
  - `ed184...`: `quote_quality_skip_queue_depth=8`, `quote_quality_skip_fill_probability=11`, `replace_guard_min_rest=4`
  - `e675...`: `quote_quality_skip_fill_probability=2`, `replace_guard_min_rest=16`
- `VERIFIED`: post-cut watched specimen
  `06080654-41ca-4913-be4b-80cebb42ac74` proves the queue-pressure family is
  gone from current/live surfaces:
  - no `maker_queue_pressure_*` counters remain in `nightly_soak_report.json`
  - no `maker_queue_pressure_adjustment` event family remains in current/live
    report outputs
  - no queue-pressure quote-mutation fields or `material_queue_pressure_only`
    class remain in current/live quote-integrity outputs
  - current runtime classification still returned `VALID_ACTIVE`
  - `reduce_only_recovery.accepted_or_reserved_recovery_rows=0`
  - `recovery_cost_benefit.recovery_submit_count=0`
  - `recovery_cost_benefit.recovery_fill_event_count=0`
  - current-live dust shadow / enforcement fields are absent after the cut
  - descriptive dust / held-unpriceable truth remains visible without dust authority
- `VERIFIED`: pre-cut Packet 2 specimens showed the maker recovery family quiet:
  - `recovery_submit_count=0`
  - `recovery_fill_event_count=0`
  - `accepted_or_reserved_recovery_rows=0`
- `VERIFIED`: current code no longer emits dust shadow / enforcement authority
  as current-live truth surfaces.
- `VERIFIED`: descriptive dust / held-unpriceable truth still remains, but the
  body now separates meaningful escalation from non-defect retired-market residue.
- `VERIFIED`: recent watched current-paper specimens show the support family is
  much louder than live maker participation:
  - `321d...`: `20` active-window rows, `2` maker submits, `17`
    admission-shadow candidates, `73` late-window probe candidates
  - `a35b...`: `20` active-window rows, `0` maker submits, `24`
    admission-shadow candidates, `96` late-window probe candidates
  - `50f...`: `20` active-window rows, `3` maker submits, `31`
    admission-shadow candidates, `50` late-window probe candidates
- `INFERRED`: that runtime ratio makes the support-shadow / probe family read
  like descriptive overhead first, not keep-now maker steel.
- `VERIFIED`: current code no longer materializes
  `maker_competitiveness.queue_pressure` as live runtime authority.
- `VERIFIED`: legacy configs that still carry the old
  `maker_competitiveness.queue_pressure` key now load through an explicit
  ignored compatibility seam with warning, not through live maker power.
- `VERIFIED`: historical packet-era logs containing
  `maker_queue_pressure_adjustment` remain readable only as archive lineage;
  they do not restore present-tense queue-pressure owner authority.
- `VERIFIED`: maker recovery is real cleanup code, not a
  generic maker-aggression branch:
  - `_reduce_only_recovery_payload()` only activates for held exposure inside
    preexpiry reduce-only or expired-grace windows
  - quote-quality relaxation only applies when the intent is pure
    risk-reducing
- `VERIFIED`: dust is real held-exposure valuation / degradation code, not
  a maker-selection owner:
  - it classifies held exposure under fail-closed valuation pressure
  - it can exempt dust-only hard-degraded held tokens from escalation
  - it does not decide normal maker fight admission
- `INFERRED`: against current blueprint and intended maker doctrine, queue
  pressure, dust, and maker recovery now read more like false-authority /
  bug-chasing residue families than keep-now maker steel.
- `INFERRED`: dust and maker recovery are best treated as one coupled surgery
  patient because both are cleanup/residue machinery tied to held-exposure and
  lifecycle mess that cleaner maker doctrine was supposed to avoid producing.
- `INFERRED`: the next likely slag family after dust/recovery closeout is the
  support-shadow / probe family, because it is report-side first and has the
  smallest blast radius relative to runtime owner-law.
- `INFERRED`: the next likely runtime family after that is the accessory maker
  tuning bundle:
  - `edge_scale_*` / `size_mult_max` / `spread_mult_min` /
    `requote_delta_mult_min`
  - then `maker_depth_target_*` / `maker_liquidity_tod_*`

## Residuals Still Open
- `VERIFIED`: the final strongest small-loss owner is not yet closed; the current answer is still a strongest candidate owner, not a final verdict.
- `VERIFIED`: the next dollar-owner proof must isolate:
  - authoritative vs truth-thin market-reference populations
  - complete vs incomplete maker populations
  - multifill vs single-fill complete rows
  - repeated-target vs non-cluster rows
- `VERIFIED`: quiet recovery and dust in the current specimen set does not
  save them from surgery-candidate status; later live specimens could still
  prove a tiny residue handler must survive elsewhere.
- `VERIFIED`: the present-tense family classification is now:
  - queue pressure is cut from current/live authority and quarantined as
    historical-only lineage
  - dust + maker recovery read like a coupled cleanup / fake-authority cluster
  - support-shadow / probe mass reads like report-side slag until it proves it
    still buys real reasoning density
  - accessory edge/depth/liquidity shaping reads like runtime tribunal
    material, not settled doctrine steel
  - same-target calm caps and post-only cross guard remain keep-now steel

## Current Disposition
- `VERIFIED`: this board remains valid as wound-lineage evidence.
- `VERIFIED`: current-code canon proof does not show the historical
  recovery-tied small-loss family alive:
  - current canon losses are discrete order outcomes
  - lifecycle residue linkage is `0`
- `INFERRED`: the active Packet 2 posture is to treat the old family as cut
  unless future watched runtime reproduces its exact repeated tiny-bleed shape.
- `UNKNOWN`: whether a different later economics patient exists; that would be
  a new patient, not automatic resurrection of this historical one.

## Current Surgery Patients
1. `queue pressure`
   - `VERIFIED`: current runtime/config/report family is cut
   - `VERIFIED`: only legacy-config ignored compatibility and archive replay
     lineage remain
   - current posture = `cut complete / historical-only lineage`
2. `dust + maker recovery`
   - `VERIFIED`: coupled cleanup / false-authority cluster
   - `INFERRED`: likely compensator machinery for held-exposure / lifecycle
     residue that cleaner maker doctrine was supposed to avoid
   - current posture = `watched-proof / cleanup closeout first`
3. `support-shadow / probe family`
   - `VERIFIED`: runtime-observed maker participation is much smaller than the
     emitted support populations in the fresh watched specimens
   - `VERIFIED`: this family is report-side first:
     - `maker_fight_admission_shadow*`
     - late/mid-window probes
     - zero-submit root-cause
     - quote-starvation / quote-integrity / selection-authority counterfactual
   - current posture = `next low-blast-radius de-fat packet`
4. `accessory competitiveness bundle`
   - `VERIFIED`: live paper still carries edge-shaping leaves beyond the clean
     cannon doctrine
   - `INFERRED`: remove or fence report-side slag before cutting this runtime
     family so owner truth stays legible
   - current posture = `runtime tribunal after support-family de-fat`
5. `accessory maker sizing scaler bundle`
   - `VERIFIED`: live paper still carries depth-target and overnight liquidity
     scaling beyond the fixed-shot + `1.5x` depth doctrine
   - `INFERRED`: this is separable from the edge-scale family and should be cut
     only after the simpler support-family cleanup
   - current posture = `runtime tribunal after edge-scale verdict`
6. `small-loss wound family`
   - `VERIFIED`: strongest current owner candidate remains
     `complete-outcome truth`
   - `VERIFIED`: current disposition = `not trusted steel / surgery-pile
     candidate`
   - current posture = `remeasure after earlier family cuts and then decide the
     next legal cut`

## Current Packet 2 Read
- `VERIFIED`: Packet 2 still does not know the final strongest owner of the recurring small negative maker dollars.
- `VERIFIED`: Packet 2 now has a stronger current owner candidate:
  - `complete-outcome truth`, with `market-truth substrate` held as the foundation trace that can still poison or validate that answer
- `VERIFIED`: Packet 2 also now knows that the answer is unlikely to live in one wrapper metric or one fill-event summary.
- `VERIFIED`: Packet 2 now has a stronger wound picture inside that owner:
  - completion bias
  - multi-fill persistence
  - repeat-target clustering
  - mirrored complement-pair debt
- `VERIFIED`: queue pressure is no longer a live Packet 2 patient; the
  current/live family is cut and historical-only lineage is quarantined.
- `INFERRED`: maker recovery and dust no longer read like keep-now maker
  doctrine steel; they now read stronger as a coupled cleanup / false-authority
  family that belongs in surgery planning.
- `VERIFIED`: current operator packet posture is:
  - not trusted steel
  - surgery-pile candidate
  - no approval to carry this exact loss shape into live by default
