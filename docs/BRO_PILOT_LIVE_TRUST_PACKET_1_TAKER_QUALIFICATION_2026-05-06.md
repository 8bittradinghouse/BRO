# BRO Pilot-Live Packet 1: Taker Live Trust Qualification

## Authority Lock
Current pickup point:
- active phase:
  - `pilot_live`
- active macro lane:
  - `live trust qualification`
- current watched closeout proof specimen:
  - `run_id=6957087b-488e-4bbb-b8b9-1f215b5e33d0`
- historical short seam specimen:
  - `run_id=4243e263-7f5c-4bbe-bf13-68163a04f7e9`
- historical timing-clean healthy specimen:
  - `run_id=069f4459-6804-44ac-a8eb-95f511d9f787`
- historical ancestry-only taker closeout:
  - `BRO_HANDOFF_20260405T051017Z_SNIPER_TAKER_PACKET_CLOSEOUT.md`
- governing question:
  - is current `taker` one coherent doctrine/runtime brain, or does inherited
    sniper-era lineage still leave duplicate meaning, dead relays, and
    compensator fat in the lane?

Authority chain:
1. `docs/PROJECT_TRUTH_STATE.md`
2. `docs/BRO_GFRAME_CORE_FIGHTER_AUDIT_2026-05-01.md`
3. `docs/NEXT_PACKET_PLAN.md`
4. `docs/BRO_PILOT_LIVE_TRUST_BOARD_SINK_2026-05-05.md`
5. doctrine roots:
   - `docs/DOCTRINE_RUNBOOK.md`
   - `BRO_CANONICAL_DOCTRINE.txt`
   - `docs/EDGE_TRUTH_RUNBOOK.md`
   - `prodesk/edge_truth_contract.py`
6. runtime owners:
   - `executor.py`
   - `prodesk/config.py`
   - `configs/profiles/paper_universal.yaml`
   - `prodesk/taker_competitiveness.py`
   - `prodesk/gateway.py`
   - `prodesk/risk.py`
7. consumer/report owners:
   - `scripts/nightly_soak_report.py`
   - `scripts/time_discipline_audit.py`
   - `scripts/edge_truth_audit.py`
8. historical closeout and handoff artifacts as reference only

No-change list:
- no generic weapon tuning
- no threshold loosening
- no broad frame surgery
- no unrelated multi-lane runtime mutation
- no live arming
- no wallet packet substitution
- no board claim beyond packet-local qualification truth
- no clone-safe frame surgery before authorization

Current blocker being judged:
- whether the taker lane still contains runtime/report split-brain relay
  ancestry and compensator fat severe enough to contaminate live-trust claims

## Current Execution Status
- packet status:
  - `closed / bounded-live-test ready`
- packet disease statement:
  - taker lane had been carrying shared late-window owner-law, shared
    maker/taker interference, and stale sniper-era
    consumer/config/report/test residue that could let one lane step on the
    other or let weaker surfaces impersonate stronger ones
- as-executed restoration ladder:
  1. replace shared late-window owner-law with explicit authority truth
  2. cut maker/taker runtime interference from normal taker authority
  3. preserve the canonical taker owner path while quarantining sniper-era
     compatibility residue
  4. realign consumer / test / doc truth to the repaired owner chain
  5. reprove Packet 1 on watched runtime and close honestly or keep it open

## Packet-Local Lock Card
Mission lock:
- determine whether taker is safe enough to arm in principle, or prove
  that it is not

Doctrine lock:
- restoration before tuning
- one concept / one term / one owner
- no weaker surface replaces a stronger one
- canonical live taker authority is narrower than raw stage naming

Authority lock:
- current board owners outrank older packet/handoff convenience language
- runtime emitters outrank report summaries
- historical closeout explains ancestry only; it does not certify current trust

Pathology lock:
- likely disease is higher-order semantic and authority drift inside the
  taker lane, not generic “taker is lazy”

Semantic lock:
- no stale sniper-root owner claim
- no duplicate owner terms
- no treating stage bucket names as the whole authority story

Timing lock:
- host -> runtime -> decision -> submit -> report path remains one combat
  timing chain

Economic-meaning lock:
- semantic cleanliness alone is insufficient
- firing/abstention integrity and economics both matter

False-closure list:
- “strict doctrine” as a hand-wave for unexplained no-fire
- “historical taker packet passed” as current live-trust proof
- “EXTREME_ONLY row exists” as proof that live authority is clean
- “green wrapper / pass status” as proof that the lane is one brain

Go / no-go:
- `GO` for doctrine/runtime mapping, watched analysis, and implementation
  planning
- `NO-GO` for generic tuning, live arming, or broad runtime mutation

## Packet 1 Restoration Method Lock
Authority boundary:
- this section preserves the method that actually closed Packet 1
- it does not replace doctrine roots or authorize later packets to inherit
  taker-specific answers without re-proof

Big-picture lock:
- this packet succeeded by restoring one-brain taker truth before tuning
- the job was to kill false owners, quarantine history, preserve valid
  interlocks, and prove the live lane on watched runtime
- the job was not to make taker look busy or profitable by momentum

As-executed method:
1. reload current board owners and doctrine roots before trusting historical
   packet language
2. trace the taker river from doctrine core through:
   - stage/window authority
   - runtime emitters and controller identity
   - timing truth
   - market-reference truth
   - gate / guardian surfaces
   - recovery / handoff seams
   - report / audit / operator consumers
   - and then back into the strongest current owner
3. classify each touching surface as:
   - owner-law
   - valid interlock
   - compatibility bridge
   - historical-only
   - scar tissue / purge candidate
4. preserve an explicit no-change list so semantic cleanup cannot smuggle in
   tuning
5. land one seam or one tightly coupled authority set per slice
6. reprove against watched runtime and inspect raw event, status, timing,
   wallet, lifecycle, and outcome truth under the wrapper
7. close only after contradiction compression, negative-proof, and explicit
   residual carry

Carry-forward law:
- future `pilot_live` packets may inherit this restoration method
- future packets must restate their own mission lock, owner map, no-change list,
  contradiction matrix, and falsifier list before implementation begins
- history stays history unless current code and watched proof make it current
  again

## Proof Cohort
Current doctrine/static owner set:
- `docs/DOCTRINE_RUNBOOK.md`
- `BRO_CANONICAL_DOCTRINE.txt`
- `docs/EDGE_TRUTH_RUNBOOK.md`
- `prodesk/edge_truth_contract.py`

Current runtime proof specimens:
- short cold-start taker seam specimen:
  - `4243e263-7f5c-4bbe-bf13-68163a04f7e9`
  - primary use:
    - `market_probability_missing`
    - `stage_disallow_taker`
    - short-specimen runtime truth without timing failure
- timing-clean healthy paper specimen:
  - `069f4459-6804-44ac-a8eb-95f511d9f787`
  - primary use:
    - healthy timing
    - clean maker runtime
    - taker non-fire under cleaner overall conditions

Historical ancestry-only reference:
- `BRO_HANDOFF_20260405T051017Z_SNIPER_TAKER_PACKET_CLOSEOUT.md`
- closeout runs:
  - `b9651b49-9760-4995-957d-621b5732dd70`
  - `60b95781-929b-49e2-b7d7-0ea08ad474f0`

Allowed answers from historical closeout:
- why specific taker inherited-lineage surfaces were added
- what packet-era success criteria existed
- what historical disease or design intent a relay was built to protect

Forbidden answers from historical closeout:
- whether current taker is live-trustworthy
- whether current doctrine roots are still aligned
- whether current no-fire behavior is acceptable
- whether current split-brain debt is already closed

## Doctrine-Root Contradiction Verdict
| Concept | Conflicting owners | Current canonical owner | Demoted historical owner | Consequence for runtime mapping |
| --- | --- | --- | --- | --- |
| `SNIPER_PRIMARY` live authority | historical `BRO_CANONICAL_DOCTRINE.txt` taker-only wording had conflicted with `DOCTRINE_RUNBOOK.md`, `edge_truth_contract.py`, and active board owners | `DOCTRINE_RUNBOOK.md` + `prodesk/edge_truth_contract.py` + synchronized `BRO_CANONICAL_DOCTRINE.txt` + active board owners | pre-`2026-05-06` taker-only wording in `BRO_CANONICAL_DOCTRINE.txt` | any runtime/config/report surface treating `SNIPER_PRIMARY` as current canonical live taker authority is compatibility legacy, not closure authority |
| raw `EXTREME_ONLY` stage vs true live-fire window | runtime stage bucket maps `0-20s`; doctrine says canonical live taker fire window is hard `<=7s` | `DOCTRINE_RUNBOOK.md` + current config final window + active board owners | raw stage bucket by itself | `EXTREME_ONLY` remains a compatibility bucket; actual live authority still depends on tighter window and downstream gates |
| stage-local taker windows outside `<=7s` | older doctrine/config mechanisms allow stage-local windows; current doctrine says broader windows are diagnostic-only | `DOCTRINE_RUNBOOK.md` + current empty `stage_final_window_sec_by_stage` + active board owners | older stage-local live-authority assumptions | stage-local noncanonical windows are audit/diagnostic material, not present live authority |

Current doctrine-root verdict:
- doctrine-root `SNIPER_PRIMARY` wording is now source-corrected.
- `SNIPER_PRIMARY` is not current canonical live taker authority.
- canonical live taker authority is the narrow `EXTREME_ONLY <=7s` commitment
  lane.
- any stage-local or naming surfaces that imply broader live authority are
  compatibility legacy until explicitly repaired.

## Canonical River Map
1. Doctrine roots:
   - current live taker doctrine lives in `docs/DOCTRINE_RUNBOOK.md`
   - stage-permission contract lives in `prodesk/edge_truth_contract.py`
2. Stage bucket production:
   - `executor._stage_name_for_sec_to_expiry()`
   - outputs `SNIPER_PRIMARY` for `20-30s`, `EXTREME_ONLY` for `0-20s`
3. Stage permission owner:
   - `edge_stage_policy()` / `CANONICAL_EDGE_STAGE_POLICY`
   - forbids taker in `SNIPER_PRIMARY` and in raw `EXTREME_ONLY`
   - current live late-window authority instead comes from the explicit runtime
     authority fields:
     `maker_new_risk_allowed`, `normal_taker_allowed`,
     `reduce_only_recovery_allowed`, and
     `preexpiry_emergency_taker_allowed`
4. Semantic live/dead checker:
   - `executor._emit_taker_stage_window_semantic_check()`
   - already warns when a stage/window combination is dead by construction
5. Main taker controller:
   - `executor._run_taker()`
   - consumes stage info, latency state, books, fair probabilities, mode, and
     runtime controls
6. Early stop relays:
   - canonical `taker_enabled`
   - mode-state gates
   - `latency_not_armed`
   - `token_lag_not_verified`
   - `normal_taker_authority_closed`
   - historical compatibility rows may still carry `stage_disallow_taker`
7. Market-truth relays:
   - oracle freshness
   - ws-only book source rule
   - `market_probability_missing`
   - complement-route mapping
   - `edge_below_min`
8. Risk/wallet relays:
   - target USD resolution
   - size caps
   - multi-oracle cap
   - submit feasibility / risk validation
9. Emitted runtime truth:
   - `edge_evaluation`
   - `taker_decision`
   - `taker_submit`
   - `taker_stage_window_semantic_check`
10. Consumer/report interpretation:
   - `nightly_soak_report.py`
   - `time_discipline_audit.py`
   - `edge_truth_audit.py`
11. Return path to owner:
   - board/operator claims must route back through emitted runtime truth and
     doctrine roots, not through consumer-only mirrors

## Stage/Config Dead-By-Construction Census
| Surface | Current role | Current status | Can influence live runtime? | Later disposition |
| --- | --- | --- | --- | --- |
| `executor._stage_name_for_sec_to_expiry()` | raw stage-bucket producer | canonical bucket producer with compatibility legacy naming burden | yes | keep, but do not let bucket name outrank tighter doctrine |
| `executor._emit_taker_stage_window_semantic_check()` | semantic self-audit | canonical emitted self-audit with legacy bridge alias | yes | keep now |
| `taker.min_edge_by_stage` non-`EXTREME_ONLY` keys | stage-local taker threshold | source-collapsed; noncanonical taker stages are now rejected by config and ignored by runtime init | no for newly validated configs | guarded |
| `taker.per_token_cooldown_sec_by_stage` non-`EXTREME_ONLY` keys | stage-local taker cooldown | source-collapsed; noncanonical taker stages are rejected by config and the stage-local branch is now ignored by canonical runtime | no | guarded |
| `_run_taker().stage_priority_rank` privileging non-`EXTREME_ONLY` stages | candidate ordering | source-collapsed; canonical runtime no longer grants stage-priority authority to taker ordering | no | guarded |
| `stage_final_window_sec_by_stage` mechanism | stage-local window override | diagnostic shim; canonical runtime now ignores it for live taker authority | no | compatibility-only residue until later purge |

## Split-Brain Inventory
| Surface | Purpose | Initial class | Duplicate-risk | Notes |
| --- | --- | --- | --- | --- |
| `SNIPER_PRIMARY` | raw late-stage bucket name | compatibility legacy | high | conflicts with current doctrine-root meaning |
| `EXTREME_ONLY` | strictest raw taker bucket | canonical bucket with narrower live-fire authority layered on top | medium | must not be mistaken for “all `0-20s` are live-fire eligible” |
| `taker_enabled` | canonical taker enablement view | canonical runtime owner | low | current runtime owner is source-collapsed onto effective `taker_enabled` |
| `_run_taker` | main taker controller | canonical runtime owner | low | current code has no live sniper wrapper sibling |
| `_taker_context` | canonical taker context owner | canonical runtime owner | low | current code has no live sniper context wrapper sibling |
| `taker_decision` | emitted taker decision event | canonical emitted event | low | current code emits canonical taker events only |
| `taker_submit` | emitted taker submit event | canonical emitted event | low | current code emits canonical taker events only |
| `taker_stage_window_semantic_check` | emitted taker stage semantic self-audit | canonical emitted event | low | current code emits canonical taker events only |
| stage-priority logic | candidate ordering overlay | compatibility legacy | high | still references noncanonical stage semantics |
| taker competitiveness config names under `taker.*` | config namespace | canonical runtime housing | low | current config root is canonical taker ownership |
| report-side `\"sniper_taker\" in reason` classification | downstream taker recognition | consumer-only mirror / compatibility bridge | high | classification by legacy string matching is drift-prone |

## Compensator-Fat / Scar-Tissue Census
| Candidate surface | Original protection target | Disease still real? | Higher owner now covers it? | Initial class | Current cost |
| --- | --- | --- | --- | --- | --- |
| `SNIPER_PRIMARY` stage-local threshold/cooldown config | stage-specific taker competitiveness tuning | partially historical only | yes, current doctrine narrows live authority harder | compatibility bridge | semantic fog, false-bug risk |
| `stage_priority_rank` privileging `SNIPER_PRIMARY` | older taker-stage ordering logic | historical only in active runtime | yes | compatibility bridge | reduced runtime blast radius, remaining semantic fog |
| `stage_final_window_sec_by_stage` override mechanism | per-stage window experimentation | no in canonical runtime | yes | diagnostic shim | extra compatibility mass |
| historical sniper-root enable bridge | removed runtime ancestry | no longer active on current code | no | historical-only residue | archived artifacts may still mention it |
| maker-only `bounded_single_side_touch` while taker fail-closes | maker recoverability on one-sided books | explicit current runtime policy asymmetry: maker bounded fallback allowed, taker bounded fallback unauthorized and fail-closed on missing midpoint | no | valid interlock / shared-frame defer | taker starvation suspicion, keep-now doctrine/runtime verdict, shared-frame redesign deferred |
| `maker_to_taker_recovery_handoff_disabled` | prevent taker from inheriting maker baggage as live authority | yes | no | valid interlock / dead-power guard | chatter, operator confusion |
| `taker_requires_ws_book_source` | prevent taker from acting on weaker book sources | yes | no | valid interlock | low direct cost |
| consumer string matching on `sniper_taker` in reports | keep downstream metrics coherent with older taker artifacts | partly historical only; current code now normalizes through canonical helper contract | mostly | consumer-only mirror | reduced drift risk, remaining translation fat for historical compatibility |
| historical handoff-era taker closeout references | preserve ancestry and prior proof | yes as history only | current board owners cover current truth | compatibility bridge | stale authority risk if overread |

Keep now:
- fail-closed `market_probability_missing`
- current taker no-bounded-fallback market-reference policy
- `taker_requires_ws_book_source`
- `maker_to_taker_recovery_handoff_disabled` until proven redundant
- `executor._emit_taker_stage_window_semantic_check()`

Keep until clone-safe surgery:
- raw stage lineage names `SNIPER_PRIMARY` / `EXTREME_ONLY`
- consumer/report mirrors kept only for historical artifact compatibility
- stage-local override mechanisms that are currently dormant but still part of
  compatibility shape

Safe later removal candidates:
- `SNIPER_PRIMARY` live-authority semantics in config once doctrine/runtime
  naming is collapsed
- duplicate enablement layering if one higher-authority gate can own the lane
- stage-priority legacy ordering that depends on noncanonical live semantics

Likely root-fog generators:
- conflicting doctrine roots on `SNIPER_PRIMARY`
- raw stage bucket name vs real live-fire authority window
- legacy compatibility mirrors that still expose `sniper_taker` for old
  artifacts
- maker/taker fallback asymmetry living across a shared-frame boundary

## Sniper-Derived Taker Surface Map
Presumption law for this map:
- sniper-derived taker surfaces are presumed `non-steel` until they earn a keep
  call
- a surface earns keep only if it proves at least one of:
  - an independent current doctrine owner still requires it
  - live/runtime safety proof shows it is a real fail-closed interlock
  - current compatibility load still requires it until clone-safe surgery
- active use alone is not proof of doctrine steel

| Surface / family | Current job | Presumed status | Keep proof required / current proof | Current call |
| --- | --- | --- | --- | --- |
| raw stage buckets `SNIPER_PRIMARY` / `EXTREME_ONLY` in `_stage_name_for_sec_to_expiry()` | produce raw time-bucket lineage before any remap | presumed non-steel compatibility naming | current runtime still needs raw bucket lineage to explain remap versus authority; bucket names still do not own live authority | keep now as lineage only; rename/collapse later if remap model changes |
| `taker.*` namespace and `TakerCompetitivenessConfig` container | hold active taker config under canonical taker naming | keep-now canonical housing | current loaders, validators, and runtime owners depend on this tree shape | keep now |
| retired stage-local taker threshold leaves | older taker fire-threshold housing | retired current authority | canonical live taker threshold now lives only on top-level `taker.min_edge`; stage-local threshold leaves must not re-arm current fire permission | historical quarantine only |
| `stage_final_window_sec_by_stage` | per-stage taker window override mechanism | presumed non-steel dormant add-on | canonical taker engine now ignores it for live authority; no keep proof remains beyond compatibility parsing | compatibility-only purge candidate |
| `aggressive_window_enabled` / `aggressive_window_sec` | optional late-window aggression overlay | presumed non-steel dormant add-on | canonical taker engine now ignores it for live authority; no keep proof remains beyond compatibility parsing | compatibility-only purge candidate |
| retired stage-local price/size aggression overlays | older per-stage taker price/size overlay housing | retired current authority | canonical live taker no longer accepts stage-local price/size aggression authority; any surviving payload is compatibility/readback residue only | historical quarantine only |
| `stage_priority_enabled` | stage-ordering overlay for taker candidate selection | presumed non-steel low-value residual | canonical taker runtime no longer grants it authority; remaining value is compatibility shape only | compatibility-only purge candidate |
| `allow_complement_buy_route` | allow live-compatible opposite-token buy instead of same-token short behavior | presumed non-steel until proven | independent live-parity doctrine still requires complement-token expression when the current token is overpriced | proven keep-now doctrine guardrail |
| `preexpiry_emergency_taker_window_sec` + `preexpiry_emergency_taker_unwind` | emergency safety path for stuck/unintended baggage near expiry | presumed non-steel safety add-on | current runtime and watched specimens show this path is active safety machinery, not normal taker alpha | proven keep-now safety-only path |
| `maker_to_taker_recovery_handoff_disabled` | keep maker baggage from silently promoting into taker authority | presumed non-steel interlock | live/runtime proof shows a real dead-power interlock preventing inherited maker authority from reopening taker | proven keep-now safety interlock |
| `taker_requires_ws_book_source` | keep taker from acting on weaker than websocket book truth | presumed non-steel interlock | canonical taker market-truth still requires websocket book source and current runtime enforces it fail-closed | proven keep-now safety interlock |
| removed sniper-family wrappers/events (`_run_sniper_taker()`, `_sniper_context()`, `_emit_sniper_stage_window_semantic_check()`, `sniper_taker_*`) | older compatibility bridge family | removed from current runtime | historical docs/tests/artifacts may still mention them | quarantine as history only |
| support / replay surfaces in tests, historical closeouts, and artifact readers that still speak `SNIPER_PRIMARY` or `sniper_taker_*` | preserve ancestry and replay old artifacts honestly | presumed non-steel historical carryover | replay truth still requires them, but only as fenced compatibility/readback surfaces | keep bounded and fenced |

Current call on the sniper-derived subtree:
- `VERIFIED`: the main disease is not one bad `EXTREME_ONLY` string by itself.
- `VERIFIED`: the main disease is an inherited taker subtree whose active
  owner surfaces, safety interlocks, and old sniper-era scaffolding still live
  in the same shape.
- `VERIFIED`: Packet 1 now treats the whole sniper-derived subtree as
  presumptively non-steel until a surface earns keep through doctrine,
  runtime-safety, or compatibility proof.
- `VERIFIED`: no sniper-derived surface is granted permanent doctrine-steel
  status merely because it is active today.
- `VERIFIED`: strongest likely weak / later-surgery candidates are:
  - `stage_final_window_sec_by_stage`
  - `aggressive_window_enabled` / `aggressive_window_sec`
  - retired stage-local taker threshold leaves
  - retired stage-local taker price/size aggression overlays
  - `stage_priority_enabled`
  - inherited taker helper/config alias mass
- `VERIFIED`: surfaces that have currently earned keep are:
  - raw bucket lineage surfaces, but only as lineage and not as live authority
  - `allow_complement_buy_route`
  - `preexpiry_emergency_taker_window_sec` / `preexpiry_emergency_taker_unwind`
    as safety-only machinery
  - `taker_requires_ws_book_source`
  - `maker_to_taker_recovery_handoff_disabled`
  - bounded compatibility bridges still required for current loaders,
    historical artifacts, and helper continuity
- `INFERRED`: a meaningful share of current taker complexity is not “taker
  logic” in the pure sense; it is compatibility and safety mass accreted
  around the older sniper-separated model.

## Preserve List / Muscle Map
Preserve list:
- current doctrine-owner chain
- `CANONICAL_EDGE_STAGE_POLICY`
- fail-closed missing market-probability behavior
- combat timing chain and `taker_decision` timing fields
- wallet/risk gating authority
- complement-route guardrails
- taker commitment doctrine and no same-token risk-increasing short

Target muscle-state:
- one current doctrine-root language
- one stage/window authority story
- one live taker lane name
- one enablement authority surface
- one consumer/report language following runtime emitters instead of second
  dialects
- only the safety interlocks that still protect real disease

## Gate / Relay Schematic
| Relay family | First relays | Owner | Allowed decision | Current status |
| --- | --- | --- | --- | --- |
| Stage authority | `_stage_name_for_sec_to_expiry`, `edge_stage_policy`, `normal_taker_authority_closed` | runtime bucket producer + explicit late-window authority contract | bucket selection plus canonical no-fire authority closure only | active; raw-stage legacy wording is now compatibility-only |
| Timing | host timing, latency verifier, `latency_not_armed`, `token_lag_not_verified`, final-window check | combat timing doctrine + runtime timing gates | may block taker on timing readiness only | active and doctrinal |
| Market truth | oracle freshness, ws book-source rule, `market_probability_missing`, complement-route mapping | runtime decision path under doctrine | may block taker on market-truth insufficiency | active; main live seam |
| Gating | mode state, recovery handoff, edge threshold, token score | runtime lane controller | may block taker on lane policy and risk posture | active |
| Wallet/risk | target USD resolution, size caps, multi-oracle cap, submit feasibility | wallet/risk owners | may cap or reject notional and submit feasibility | active |
| Consumer/report | `taker_decision`, `taker_submit`, `taker_stage_window_semantic_check`, nightly soak and timing audits | runtime emitters + downstream consumers | may classify and summarize only | active with legacy compatibility mirrors, but canonical owner is now taker-named |

## Per-Relay Leaf Map
| Relay | Owner | Surfaces needed | Why | Current verdict | Cleanup call |
| --- | --- | --- | --- | --- | --- |
| `_stage_name_for_sec_to_expiry()` | raw bucket producer | `stage_bucket` only | maps time-to-expiry into raw bucket lineage before any remap occurs | keep-now correct-sized single-surface relay | none |
| `_token_stage_info()` | stage authority assembler | `effective_stage` + `stage_bucket` | computes raw bucket first, then may remap live authority through maker timing override, observe-hold, expired grace, or recovery posture | keep-now correct-sized dual-surface relay | preserve both surfaces |
| `_stage_policy()` / `edge_stage_policy()` | canonical stage permission | `effective_stage` only | maker/taker authority must follow the remapped live authority stage, not stale raw bucket lineage | keep-now correct-sized single-surface relay | keep bucket out of permission ownership |
| `stage_transition` | operator-visible authority transition | `effective_stage` only | transition event is about current live authority movement, not raw bucket history | keep-now correct-sized single-surface relay | possible alias-fat cleanup later |
| `doctrine_decision` | doctrine/operator truth | `effective_stage` + `stage_bucket` | operator truth needs both the current authority call and the pre-remap lineage that produced it | keep-now correct-sized dual-surface relay | preserve both; aliases stay compatibility-only |
| `edge_evaluation` | runtime action/block truth | `effective_stage` + `stage_bucket` | action/block semantics depend on remapped authority stage while Packet 1 diagnostics still need original bucket lineage | keep-now correct-sized dual-surface relay | preserve both; continue shrinking alias drift |
| `taker_decision` | submit-path decision truth | `effective_stage` + `stage_bucket` | submit-path events need actual decision stage plus original bucket lineage for complement-route, cooldown, and audit reasoning | keep-now correct-sized dual-surface relay | preserve both |
| lifecycle context payloads | posture/recovery truth | `effective_stage` + `stage_bucket` | recovery, maker timing override, and posture diagnostics need current authority plus raw bucket lineage | keep-now correct-sized dual-surface relay | preserve both through Packet 1 closeout and Packet 2 handoff |
| telemetry `doctrine_stage_count.*` | authority-stage observability | `effective_stage` only | counts current live authority distribution | keep-now correct-sized single-surface relay | none |
| telemetry `doctrine_stage_bucket_count.*` | raw-lineage observability | `stage_bucket` only | counts raw time-bucket lineage independent of remap | keep-now correct-sized single-surface relay | none |
| `maker_fight_admission_shadow` and nightly-soak row normalization | consumer/report support artifacts | `effective_stage` + `stage_bucket` | support artifacts need both stage truths so audits and backfills do not flatten remap lineage | keep-now correct-sized dual-surface consumer bridge | preserve both; shrink outward alias clutter later |
| legacy `stage` / `raw_stage` field names | compatibility mirrors only | alias view onto both | historical consumers still read them, but they no longer own meaning | compatibility fat, not owner-law | later alias-fat reduction after helper/config ancestry verdict |

## Current Watched Runtime Findings
Artifact anchors:
- short seam specimen:
  - `/home/odah/bro/base/logs_exec/paper_universal/events_2026-05-06.jsonl`
  - `/home/odah/bro/base/logs_exec/paper_universal/sessions/3076fdd1-a90b-4cfa-9875-fe91991c503f/slices/status_slice.jsonl`
  - `/home/odah/bro/base/logs_exec/paper_universal/reports/4243e263-7f5c-4bbe-bf13-68163a04f7e9/readiness_gate.json`
- timing-clean comparison specimen:
  - `/home/odah/bro/base/logs_exec/paper_universal/sessions/36196558-53cd-48ee-897b-ba889af784ed/slices/events_slice.jsonl`
  - `/home/odah/bro/base/logs_exec/paper_universal/reports/069f4459-6804-44ac-a8eb-95f511d9f787/readiness_gate.json`

### Finding 1: public stage and raw stage are different live surfaces
- `VERIFIED`: both fresh specimens carry the same raw stage skeleton:
  - `MAKER_TAKER_SELECTIVE -> SNIPER_PRIMARY` at about `29.xs`
  - `SNIPER_PRIMARY -> MAKER_TAKER_SELECTIVE` at about `19.xs`
  - `MAKER_TAKER_SELECTIVE -> EXTREME_ONLY` at about `14.xs`
- `VERIFIED`: that pattern appears in:
  - seam specimen `4243e263-7f5c-4bbe-bf13-68163a04f7e9`
  - comparison specimen `069f4459-6804-44ac-a8eb-95f511d9f787`
- `VERIFIED`: `executor._token_stage_info()` computes `raw_stage` first, then may remap the published `stage` through maker timing override, observe hold, or recovery logic before `stage_transition` and `doctrine_decision` are emitted.
- `VERIFIED`: current code now names that split explicitly on the active Packet 1
  path:
  - `effective_stage` = remapped live authority stage
  - `stage_bucket` = raw time-bucket lineage
  - legacy `stage` / `raw_stage` remain as bounded compatibility aliases only
  - missing raw bucket lineage now stays explicit as `UNKNOWN` instead of being silently borrowed from `effective_stage`
- `VERIFIED`: the doctrine stream itself proves this split:
  - `4243` includes `2` `stage=MAKER_TAKER_SELECTIVE / raw_stage=EXTREME_ONLY`
  - `069f` includes `8` `stage=MAKER_TAKER_SELECTIVE / raw_stage=EXTREME_ONLY`
- `VERIFIED`: the current owner verdict is **keep-now explicit two-surface contract**, not “unresolved stage split-brain.”
  - `effective_stage` is the live authority / action surface
  - `stage_bucket` is the raw timing-lineage / diagnostic surface
  - collapsing them to one field on current runtime would either hide real remap behavior or destroy diagnostic lineage
- `VERIFIED`: the relays that genuinely need both surfaces today are:
  - `_token_stage_info()` because it computes raw bucket lineage before observe-hold, maker timing override, or recovery remap
  - `doctrine_decision` because operator truth needs both current authority and raw lineage
  - `edge_evaluation` because action/block semantics can depend on the remapped authority stage while Packet 1 still needs the original bucket lineage
  - `taker_decision` and lifecycle context payloads because submit-path and recovery/posture artifacts must preserve the authority/lineage distinction
  - telemetry stage counters because `doctrine_stage_count.*` and `doctrine_stage_bucket_count.*` answer different questions
- `INFERRED`: the fat here is no longer “two surfaces exist.” The fat is legacy alias clutter and any consumer that still treats `stage` / `raw_stage` as owner terms instead of bounded compatibility names.

### Finding 2: fresh watched taker consumer artifacts showed a raw-stage blind spot, and the current emitter slice now fences it
- `VERIFIED`: the fresh watched Packet 1 specimens were captured before the current emitter fix, so their taker `edge_evaluation` rows expose `stage`, `block_reason`, and market-truth fields but not `raw_stage`.
- `VERIFIED`: downstream counts in those watched artifacts therefore see only the post-override published stage, for example:
  - `4243`: `1956` `OBSERVE/stage_disallow_taker`, `140` `MAKER_TAKER_SELECTIVE/stage_disallow_taker`, `40` `SNIPER_PRIMARY/stage_disallow_taker`
  - `069f`: `3808` `OBSERVE/stage_disallow_taker`, `292` `MAKER_TAKER_SELECTIVE/stage_disallow_taker`, `68` `SNIPER_PRIMARY/stage_disallow_taker`
- `VERIFIED`: current runtime code now preserves `raw_stage` on `edge_evaluation` for both maker and taker rows.
- `VERIFIED`: current runtime/report code now carries the explicit canonical
  stage-surface contract across the active Packet 1 path:
  - `_token_stage_info()`
  - `doctrine_decision`
  - `edge_evaluation`
  - lifecycle context payloads
  - maker admission-shadow events
  - nightly-soak report row normalization and audit/support artifacts
- `VERIFIED`: targeted doctrine-gating proof now shows remapped public stage and raw stage can coexist on emitted edge rows:
  - maker edge row: `stage=MAKER_TAKER_SELECTIVE`, `raw_stage=EXTREME_ONLY`
  - taker edge row: `stage=MAKER_TAKER_SELECTIVE`, `raw_stage=EXTREME_ONLY`
- `VERIFIED`: the owner language is now materially tighter on current code, and
  Packet 1 no longer owes a yes/no verdict on whether the two-surface model
  exists. That verdict is now `keep-now`.
- `INFERRED`: the remaining work on this seam is compatibility-fat cleanup and
  later alias reduction, not forcing a fake one-surface story.

### Finding 2b: the main taker controller and emitted taker identity are now canonical on current code
- `VERIFIED`: current runtime code now uses:
  - `executor._run_taker()` as the canonical controller
  - `taker_decision`
  - `taker_submit`
  - `taker_stage_window_semantic_check`
- `VERIFIED`: sniper-family runtime bridges were removed from current code.
- `VERIFIED`: current Packet 1 proof now rides only the canonical taker
  controller and canonical taker event names.
- `INFERRED`: the runtime/controller identity seam is materially reduced; the remaining split-brain mass is now stage duality, doc/history residue, and any still-open helper/config ancestry.

### Finding 2c: dual runtime enablement and active-mode telemetry are now source-collapsed onto canonical taker ownership
- `VERIFIED`: current runtime code now resolves one effective lane enable owner:
  - `taker_enabled`
  - current code reads canonical `taker_enabled` directly; historical
    sniper-root enablement is no longer a live runtime owner
- `VERIFIED`: `_run_taker()` now fail-closes only on canonical taker gate
  language:
  - `taker_disabled`
  - `ramp_taker_disabled`
- `VERIFIED`: current runtime status/event emission now uses canonical taker
  mode language:
  - `taker_mode_active`
  - `taker_token_count`
  - `taker_lag_verified_token_count`
  - `taker_near_token_count`
  - `taker_sec_to_expiry_min`
  - `taker_mode_transition`
- `VERIFIED`: historical bridges no longer survive in current runtime code.
- `VERIFIED`: current operator/report surfaces are canonical taker-first; any
  surviving sniper-family references are historical artifact residue, not live
  owners.
- `VERIFIED`: active outward-facing consumer/report surfaces that belonged to
  this packet are now canonical taker-only:
  - `time_discipline_audit.py` emits only `taker_*` critical timing evidence
    keys
  - `nightly_soak_report.py` emits a canonical top-level `taker` summary and
    canonical `taker_lag_gate` posture
  - `ops_snapshot.py`, `ops_brief.py`, and `paper_live_parity.py` now consume
    and emit canonical `taker_*` public metrics while preserving historical
    fallback reads only where required for old artifacts
  - `fusion_core_profile.py` no longer publishes a separate `sniper` readiness
    lane
- `VERIFIED`: the remaining enablement seam is no longer runtime dual
  authority. It is now a bounded config-bridge / helper-compatibility seam.
- `VERIFIED`: the current owner verdict on that seam is `defer`, not `open`:
  - active taker ownership is already canonical
  - remaining `sniper.*` config names and helper aliases still carry config,
    test, and historical-artifact continuity load
  - removal belongs to later clone-safe cleanup, not Packet 1-local trust
    repair

### Finding 3: late-window maker/taker fallback asymmetry is concretely real
- `VERIFIED`: in seam specimen `4243`, taker logs `38` `EXTREME_ONLY/market_probability_missing` rows while maker logs `48` `EXTREME_ONLY/bounded_single_side_touch` rows.
- `VERIFIED`: in comparison specimen `069f`, taker logs `20` `EXTREME_ONLY/market_probability_missing` rows while maker logs `52` `EXTREME_ONLY/bounded_single_side_touch` rows.
- `VERIFIED`: the asymmetry hits the same target refs on the same late-window tape.
  - `4243`: taker fail-closed rows for `d7b727f583ae73c5` and `a091d2491a7b31c4` from about `13.61s` down through `10.20s`, while maker keeps publishing `bounded_single_side_touch` on those same refs.
  - `069f`: taker fail-closed rows for `a24e89e12cd9e236` from about `14.48s` down through `10.22s`, while maker keeps publishing `bounded_single_side_touch` on that same ref and later on `39e55ce1e1774017` / `b8b6cc4e22f26f84`.
- `VERIFIED`: taker fail-closed rows are `observed_live`, `book_source=ws`, `market_reference_class=not_available`, `market_reference_mode=missing`, `market_probability=null`.
- `VERIFIED`: maker on the same thin tape can still emit `market_reference_class=bounded_approximation`, `market_reference_mode=bounded_single_side_touch`, and non-null `market_probability`.
- `VERIFIED`: current runtime now names this split as two explicit owner contracts rather than one explicit maker policy plus ad hoc taker fallthrough:
  - maker market-reference policy may emit `bounded_single_side_touch` as `bounded_approximation`
  - taker market-reference policy has no bounded fallback and remains explicit `missing -> fail_closed_market_probability_missing`
- `VERIFIED`: current doctrine and current runtime now agree on the same keep-now owner call:
  - maker bounded single-side fallback remains an explicitly labeled maker-only bounded approximation path
  - taker no-bounded-fallback remains a fail-closed blind-fire interlock, not a Packet 1-local bug to relax
- `VERIFIED`: any future attempt to let taker inherit maker fallback would be shared-frame behavior surgery and is out of scope for Packet 1 unless doctrine-root authority changes first
- `INFERRED`: this asymmetry is not theoretical and not purely archival. It is current-code late-window behavior in both fresh Packet 1 specimens.

Current status:
- `VERIFIED`: this finding is now historical rather than current owner-law.
- `VERIFIED`: Packet 1 follow-on surgery removed the taker midpoint-only
  fallback asymmetry on canonical ws one-sided books.
- `VERIFIED`: current taker market-reference policy now allows
  `bounded_single_side_touch` on the same narrow ws one-sided condition where
  maker already used it.
- `VERIFIED`: `market_probability_missing` now remains only for fully missing ws
  market-reference truth, not for ordinary one-sided ws late-window tape.

### Finding 4: recovery/handoff chatter currently reads more like a real dead-power interlock than random noise
- `VERIFIED`: seam specimen `4243` produced `5` taker rows with `block_reason=maker_to_taker_recovery_handoff_disabled`.
- `VERIFIED`: comparison specimen `069f` produced `25` such rows.
- `VERIFIED`: all sampled rows are:
  - `stage=EXTREME_ONLY`
  - `reduce_only_recovery_active=true`
  - `reduce_only_recovery_reason=preexpiry_reduce_only_window_active`
- `VERIFIED`: some of those rows still have clean observed-live authority inputs:
  - `book_source=ws`
  - `decision_input_type=observed_live`
  - `market_reference_class=authoritative`
  - direct midpoint probabilities present
- `VERIFIED`: current runtime and focused regression proof now classify this seam
  as a keep-now `valid interlock / dead-power guard`, not generic chatter:
  - maker-originated recovery handoff stays dead on the taker side
  - timing-gate handoff override does not restore taker recovery authority
  - expired-grace / preexpiry-recovery contact remains safety-only and
    non-promoting
  - missing midpoint or weaker market-reference truth does not reopen the lane
    through recovery handoff
- `VERIFIED`: latest watched specimen `ff709f61-6e5a-4da4-b164-86739d461f47`
  strengthened this classification and exposed a doctrine-text drift:
  - runtime emitted `preexpiry_emergency_taker_unwind` rows with
    `timing_gate_handoff_override_active=true`
  - those rows were outside the nominal `<=7s` emergency window
    (`sec_to_expiry≈14.05s` and `≈13.14s`)
  - `maker_no_submission_reason=maker_timing_gate_closed`
  - corresponding taker edge rows still showed clean live substrate on some
    contacts:
    - `book_source=ws`
    - `market_reference_class=authoritative`
    - `market_reference_mode=direct_midpoint`
  - even there, the owner block remained
    `maker_to_taker_recovery_handoff_disabled`
- `VERIFIED`: current runtime therefore has two distinct recovery-unwind emit
  paths:
  - true `<=7s` emergency-window path
  - timing-gate handoff override path before that window
- `VERIFIED`: both paths currently remain dead-power on the taker side; neither
  is a hidden live-fire authorization path
- `VERIFIED`: recovery/handoff owner-classification hardening is now closure-grade
  on current owner language:
  - the live specimen, doctrine root, and focused regression ring now agree on
    the same dead-power call
  - remaining Packet 1 work should not reopen this seam casually unless fresh
    contradictory runtime evidence appears
- `VERIFIED`: any future change here would be shared-frame behavior surgery or
  telemetry/report cleanup, not a Packet 1-local live-authority loosening.

## External Blueprint Audit: Taker Sword Blueprint
Audit anchor:
- `docs/TAKER_SWORD_BLUEPRINT_2026-05-07.md`
- external Grok challenge input only; not an active doctrine owner
- burden rule for this audit:
  - no sniper-derived taker surface inherits doctrine steel from age, activity,
    or superficial alignment with the external blueprint
  - every current taker gate must still earn its keep from current doctrine,
    runtime safety proof, or bounded compatibility necessity

| Blueprint demand | Current taker lane evidence | Match status | Packet 1 call |
| --- | --- | --- | --- |
| Chainlink primary plus Pyth secondary are non-negotiable | `secondary_oracle.pyth.enabled=true`; runtime computes same-direction confirmation between primary and secondary edges, but current secondary oracle is confirmation/boost context rather than unconditional fire prerequisite | partial | keep under challenge; current BRO uses the second oracle, but not with the same hard-owner semantics as the blueprint |
| Official Polymarket CLOB API with signed orders | `LiveClobGateway` uses `py_clob_client.ClobClient` against `https://clob.polymarket.com` with `signature_type=1`; live taker submit path resolves through `place_taker_order_with_outcome()` | aligned | proven current live execution surface; this does not by itself justify inherited sniper-era stage doctrine |
| IOC taker execution | `place_taker_order_with_outcome()` hard-codes `tif="IOC"` and `post_only=False` for taker submits | aligned | proven keep-now runtime behavior |
| Final `8-12s` sniper window | current canonical taker doctrine hard-locks `<=7s`; active config sets `final_window_sec: 7.0`; `stage_final_window_sec_by_stage` is fenced as diagnostic-only | mismatch | current `<=7s` / `EXTREME_ONLY` window does not earn steel from the external blueprint and stays under keep-proof burden |
| Hard dual-oracle delta threshold `>=0.20` before fire | current top-level taker fire threshold is now canonical `taker.min_edge=0.11`; `multi_oracle_edge_threshold_abs` remains optional boost eligibility rather than unconditional fire permission | partial | current threshold owner is cleaner, but dual-oracle confirmation still does not own fire permission the way the blueprint demands |
| Liquidity must be `1.5x` order size at aggressive price | current taker lane checks `visible_fill_ratio >= min_visible_fill_ratio`; active profile uses `min_visible_fill_ratio: 0.5`, which is not the same contract as explicit `1.5x` depth | mismatch | current visibility/depth rule is real runtime behavior but not blueprint-equivalent; keep under challenge |
| Peak-window / regime whitelist | no explicit taker-only `allowed_peak_windows` gate was found; current lane has operating-mode health gates plus time-of-day scalers for paper liquidity and dynamic risk sizing, not a hard taker peak-hours whitelist | mismatch | current taker regime gating does not inherit steel from the external blueprint and stays challenge-open |
| Fixed `$150` taker shot | external blueprint still calls for fixed `$150`, but the current paper packet is temporarily detuned to `target_usd: 20.0`, `hard_min_target_usd: 20.0`, and `dynamic_size_target_usd_cap: 20.0` so taker can prove fireability before size is retightened | mismatch | temporary paper proving detune only; do not confuse with settled blueprint shot-size law |
| Fail-closed on unknown state | current lane fail-closes on fully missing ws market reference (`market_probability_missing`), weak book source (`taker_requires_ws_book_source`), timing disarm, operating-mode disarm, and unknown secondary-oracle state for boost | aligned / stronger | proven keep-now safety posture |
| Full observability for every fire / skip | canonical runtime emits `taker_decision`, `edge_evaluation`, `taker_submit`, `taker_stage_window_semantic_check`; Packet 1 report path now preserves gate-only no-submit posture honestly | aligned / stronger | proven keep-now observability posture |

Current audit call:
- `VERIFIED`: the external blueprint strengthens the case that the real open
  Packet 1 pressure is upstream taker fire-condition doctrine, not hidden
  runtime sickness.
- `VERIFIED`: BRO currently matches the external blueprint best on:
  - live CLOB/IOC execution path
  - external fixed `$150` taker shot doctrine, with current paper packet
    temporarily detuned to fixed `$20`
  - fail-closed safety posture
  - observability richness
- `VERIFIED`: BRO currently diverges from the external blueprint most on the
  same surfaces that look sniper-era or add-on heavy:
  - hard `<=7s` / `EXTREME_ONLY` live window
  - multi-oracle confirmation still acting as boost logic rather than a hard
    fire prerequisite
  - `multi_oracle_edge_threshold_abs` being boost logic instead of a hard fire
    prerequisite
  - `min_visible_fill_ratio` as the active liquidity gate instead of explicit
    `1.5x` aggressive depth
  - absence of a hard taker-only peak-window whitelist
- `INFERRED`: this external audit does not prove the blueprint itself is
  canonical law, but it does reinforce the inversion rule:
  - current `EXTREME_ONLY`-subtree fire-condition surfaces are not presumed
    steel
  - they must prove they need to survive through current doctrine and accepted
    runtime evidence

## Null-Hypothesis Subtree Removal Verdict
Default-removal law for the inherited taker subtree:
- assume the subtree should **not** survive in its current housing
- a surface only earns survival if one of two things is true:
  - the exact behavior is still required by current doctrine or runtime safety
  - and no simpler canonical taker / market-truth / recovery owner can absorb it
- active behavior alone is not proof that the old subtree deserves to remain

Most important Packet 1 distinction:
- `VERIFIED`: several current protections look real and necessary
- `VERIFIED`: that does **not** mean the inherited sniper-derived container,
  naming, stage semantics, or helper stack deserves to survive
- `VERIFIED`: the strongest current evidence supports:
  - delete the weak subtree surfaces outright where they are dormant or
    redundant
  - re-home any proven safety or live-parity behavior into canonical owners
  - remove the inherited sniper/taker housing after those re-homes land

### Remove Now Candidates
These surfaces currently show little or no keep proof in their present form and
should be planned for deletion first:
- `stage_final_window_sec_by_stage`
  - canonical taker runtime now ignores it
  - doctrine already fences it to diagnostic-only
- `aggressive_window_enabled` / `aggressive_window_sec`
  - canonical taker runtime now ignores it
  - doctrine already says it must not broaden canonical live taker
- `stage_priority_enabled`
  - canonical taker runtime no longer grants it ordering authority
  - remaining presence is compatibility/config residue rather than doctrine
    owner logic

### Replace-Then-Remove Candidates
These surfaces may represent real behavior pressure, but current evidence does
not justify preserving the old subtree form:
- raw bucket names `SNIPER_PRIMARY` / `EXTREME_ONLY`
  - the lineage concept may still be needed while remap behavior exists
  - the inherited sniper-flavored names do not earn keep as canonical owner
    language
- retired stage-local taker threshold leaves
  - top-level `taker.min_edge` now owns current fire permission directly
  - remaining stage-local threshold references are historical residue only
- `final_window_enabled` / `final_window_sec`
  - current live doctrine still uses a hard taker fire window and the active
    profile sets it to `true` / `7.0`
  - that proves live influence, not that sniper competitiveness housing is the
    right owner for the final taker fire-time contract
- retired stage-local price/size aggression overlays
  - canonical taker runtime no longer accepts stage-local aggression authority
  - any surviving payload is compatibility/readback residue, not live owner law
- `taker.*` config namespace and `TakerCompetitivenessConfig`
  - current loaders/validators depend on this canonical taker shape
  - that is current runtime housing proof, not blanket doctrine proof for every leaf
- `multi_oracle_boost_window_sec` / `multi_oracle_target_usd_cap`
  - both are active subtree leaves
  - in the current profile they effectively collapse to the same live bounds as
    canonical taker shot size / final window (`7s`, `$20`)
  - that makes them likely subtree mass until they prove a distinct current
    doctrine job
- `multi_oracle_boost_enabled` / `multi_oracle_edge_threshold_abs`
  - current boost path remains live-configurable, but the threshold acts as
    optional boost eligibility rather than canonical fire permission
  - if multi-oracle confirmation survives, it should survive under a canonical
    taker confirmation owner, not sniper-era boost housing
- `min_visible_fill_ratio`
  - current taker liquidity gate is active runtime behavior
  - it still does **not** match the external blueprint's explicit `1.5x` depth
    contract, so it stays under removal/re-home burden rather than earning
    steel from current use
- `taker_min_edge` / `taker_extreme_edge_mult`
  - top-level `taker_min_edge` is now the sole current live fire-threshold
    owner
  - `taker_extreme_edge_mult` is no longer part of current fire permission and
    now reads as legacy fallback residue rather than live owner steel
- `per_token_cooldown_sec_by_stage`
  - stage-scoped cooldown branch no longer owns live runtime behavior
  - current profile does not prove it is needed beyond compatibility shape
  - treat as compatibility-only residue pending purge

### Re-Home-Then-Delete Generic Active Leaves
These leaves are not the obvious `EXTREME_ONLY` / sniper-stage surfaces, but
they still live inside the inherited subtree and can change taker behavior if
cut carelessly:
- `taker_target_usd` + `hard_min_target_usd`
  - current strongest keep proof is economic doctrine:
    - temporary fixed `$20` paper proving shot
    - hard floor against under-sized fire
  - current proof does **not** justify keeping them inside inherited sniper
    housing
- `taker_max_orders_per_cycle`
  - active pacing cap in `_run_taker()`
  - if it survives, it should survive as canonical taker pacing owner, not as
    subtree baggage
- `taker_per_token_cooldown_sec`
  - active generic pacing guard
  - same re-home rule as `taker_max_orders_per_cycle`
- `dynamic_size_enabled` + `dynamic_size_edge_start_abs` /
  `dynamic_size_edge_full_abs` / `dynamic_size_target_usd_cap`
  - active taker sizing curve and its subtree enable toggle
  - if canonical taker still needs dynamic sizing, move it into canonical
    taker economics rather than preserving subtree housing
- `hard_min_enforcement`
  - currently hard-locked to `skip_if_unachievable`
  - if the hard minimum shot floor survives, the enforcement rule should live
    with canonical taker shot geometry rather than inside sniper competitiveness
- `conviction_model` / `edge_weight` / `latency_score_weight`
  - active taker scoring-model cluster
  - if these ranking weights survive, they should survive as canonical taker
    decision-model owners rather than inherited subtree scoring baggage
- `price_aggress_bps_max`
  - active aggressiveness ceiling
  - may be real guardrail, but it should not survive only because sniper-era
    config already exists
- `multi_oracle_capital_pct_cap`
  - active wallet-derived cap source for boosted taker size
  - if kept, it belongs with taker wallet/risk authority rather than inherited
    subtree competitiveness mass
- `taker_order_size`
  - current paper profile inherits `sizing.mode=\"notional\"`, so this share-size
    leaf is dormant in the active profile
  - that makes it a safer purge candidate later, but watch non-notional
    profiles before deletion
- `dynamic_preview_enabled`
  - active advisory-preview toggle on the taker competitiveness path
  - if preview survives, it should re-home to a canonical taker observability
    owner instead of remaining bundled with sniper-era fire-condition housing

### Re-Home-Then-Delete Candidates
These behaviors have stronger keep proof, but the proof supports the behavior,
not the inherited subtree:
- `normal_side_policy=buy_expected_winner_only`
  - keep behavior: canonical no same-token risk-increasing short doctrine
  - delete target: inherited sniper competitiveness housing for side policy
  - re-home target: canonical taker live-parity / side-expression owner
- `allow_complement_buy_route`
  - keep behavior: live-parity canonical expression for overpriced current
    token
  - delete target: inherited sniper competitiveness housing
  - re-home target: canonical taker live-parity owner
- `taker_requires_ws_book_source`
  - keep behavior: fail-closed market-truth interlock
  - delete target: subtree ownership under sniper competitiveness mass
  - re-home target: canonical taker market-truth substrate
- `maker_to_taker_recovery_handoff_disabled`
  - keep behavior: dead-power recovery boundary
  - delete target: mixed subtree framing that makes it look like taker
    competitiveness logic
  - re-home target: maker/taker recovery authority boundary
- `preexpiry_emergency_taker_window_sec` / `preexpiry_emergency_taker_unwind`
  - keep behavior: emergency safety-only path
  - delete target: treating it like part of taker alpha doctrine
  - re-home target: shared runtime safety / terminal risk doctrine

### Compatibility Purge Candidates
These surfaces do not have doctrine value and should disappear after consumer
and loader continuity is migrated:
- `_run_sniper_taker()`
- `_sniper_context()`
- `_emit_sniper_stage_window_semantic_check()`
- legacy `sniper_taker_*` runtime/event aliases
- support/replay readers that still require old `sniper_taker` naming

Current planning verdict:
- `VERIFIED`: the current evidence does **not** justify preserving the
  inherited taker subtree as a permanent subsystem
- `VERIFIED`: several behaviors inside it still look necessary, but they have
  to earn survival by re-homing into simpler canonical owners
- `VERIFIED`: the original removal map was incomplete until the latent subtree
  leaves and remaining live competitiveness toggles/scoring leaves were called
  out explicitly:
  - `taker_min_edge`
  - `taker_extreme_edge_mult`
  - `per_token_cooldown_sec_by_stage`
  - `multi_oracle_boost_window_sec`
  - `multi_oracle_boost_enabled`
  - `multi_oracle_edge_threshold_abs`
  - `multi_oracle_target_usd_cap`
  - `min_visible_fill_ratio`
  - `normal_side_policy`
  - `final_window_enabled` / `final_window_sec`
  - `dynamic_size_enabled`
  - `hard_min_enforcement`
  - `conviction_model`
  - `edge_weight` / `latency_score_weight`
  - `dynamic_preview_enabled`
- `INFERRED`: your belief is plausible:
  - cleaning out the old subtree may unlock taker
  - but the highest-confidence route is delete-or-re-home by class, not one
    blind rip-out

Removal options:
1. Conservative:
   - remove only dormant stage overlays first
   - `stage_final_window_sec_by_stage`
   - `aggressive_window_enabled/aggressive_window_sec`
   - `stage_priority_enabled`
   - `per_token_cooldown_sec_by_stage`
2. Balanced:
   - remove dormant overlays
   - then re-home canonical keep behaviors:
     - `normal_side_policy`
     - `allow_complement_buy_route`
     - `taker_requires_ws_book_source`
     - `maker_to_taker_recovery_handoff_disabled`
     - emergency taker safety path
   - then collapse threshold/boost residue:
     - `min_edge_by_stage.EXTREME_ONLY`
     - `taker_min_edge`
     - `taker_extreme_edge_mult`
     - `multi_oracle_*` subtree leaves
     - `min_visible_fill_ratio`
     - `final_window_enabled` / `final_window_sec`
     - scoring/preview residue:
       - `conviction_model`
       - `edge_weight` / `latency_score_weight`
       - `dynamic_preview_enabled`
3. Aggressive:
   - rip out the whole subtree housing first, then repair runtime/tests around
     the blast crater
   - highest speed, worst truth control, highest regression risk

Packet 1 recommendation:
- `VERIFIED`: choose the balanced path
- `INFERRED`: it is the highest-ROI option because it can remove real subtree
  mass while preserving the behaviors most likely to be genuine taker steel
  once re-homed
- `VERIFIED`: the first live behavior slice is now tighter than the original
  balanced-path sketch:
  - keep `multi_oracle_boost_*` as keep-now dual-oracle lock help
  - keep `min_visible_fill_ratio` under challenge, but untouched in the first
    packet
  - cut dynamic-size authority, conviction-owned ranking authority, and dead
    stage-overlay semantics first

Implemented strangler slices now landed in current code:
- `VERIFIED`: canonical internal owner path now exists as
  `build_taker_competitiveness_policy(...)` +
  `TakerCompetitivenessEngine`
- `VERIFIED`: executor runtime now reads the canonical taker policy/engine
  owners through `TakerCompetitivenessConfig` and
  `TakerCompetitivenessEngine`; the sniper-family compatibility façades are no
  longer part of current runtime code
- `VERIFIED`: normalized taker competitiveness validation now routes through
  the canonical policy builder in `prodesk/config.py`, reducing parser/runtime
  drift risk
- `VERIFIED`: dormant overlay authority already removed from canonical taker
  runtime:
  - `stage_final_window_sec_by_stage`
  - `aggressive_window_enabled` / `aggressive_window_sec`
  - `stage_priority_enabled`
  - `per_token_cooldown_sec_by_stage`
- `VERIFIED`: active report/runtime truth remains canonical:
  - `taker_*`
  - `effective_stage`
  - `stage_bucket`
- `VERIFIED`: legacy sniper-era façades remain accepted for continuity only;
  they are no longer the active taker runtime owner

Critical removal-order hazards:
- keep top-level `taker.min_edge` as the sole current fire-threshold owner; do
  not reintroduce stage-local threshold relays or fallback multiplier authority
- deleting `final_window_enabled` or `final_window_sec` without an explicit
  replacement owner changes the live fire-time contract directly, not just
  config placement
- deleting `taker_max_orders_per_cycle` or `taker_per_token_cooldown_sec`
  without a replacement owner changes pacing/fire-burst behavior, not just
  naming
- deleting `multi_oracle_target_usd_cap` or `multi_oracle_capital_pct_cap`
  without an explicit replacement changes boosted-size authority, not just
  config shape
- deleting `min_visible_fill_ratio` without a replacement owner changes the
  live liquidity / partial-fill guard directly, not just subtree shape
- deleting `conviction_model` or `edge_weight` / `latency_score_weight`
  without a replacement owner changes taker edge-ranking behavior, not just
  implementation style
- deleting `taker_target_usd` / `hard_min_target_usd` without a replacement
  owner changes canonical shot geometry directly

## Seam Calls
### Seam 1: Stage / Window Authority
Real disease:
- raw stage buckets, older doctrine text, and current live-fire doctrine are
  not yet one language

Likely false-bug interpretation to reject:
- “taker is broken because `SNIPER_PRIMARY` doesn’t fire”

Preserve while fixing:
- edge stage policy fail-closed authority
- canonical hard `<=7s` live-fire doctrine
- semantic self-check guard

Candidate slice order:
1. doctrine-root contradiction cleanup
2. stage/window vocabulary collapse
3. stage-local dead-by-construction config cleanup

### Seam 2: Market-Probability / Fallback Asymmetry
Real disease:
- taker late-window truth can fail closed on missing market probability while a
  shared-frame maker path may still operate on bounded single-side reference

Likely false-bug interpretation to reject:
- “taker should just inherit maker fallback”

Preserve while fixing:
- fail-closed blind-fire protection
- ws-only book-source guard
- market-reference class truth

Candidate slice order:
1. preserve the keep-now taker no-bounded-fallback verdict and prevent drift
2. map shared-frame contact points
3. revisit only if doctrine-root authority or shared-frame architecture
   changes

### Seam 3: Recovery / Handoff
Real disease:
- shared code still exposes maker-to-taker recovery contact even though current
  doctrine treats canonical taker-side recovery as dead power for normal taker

Likely false-bug interpretation to reject:
- “recovery chatter means taker itself is unhealthy”

Preserve while fixing:
- safety guard against inherited maker baggage
- no new-risk recovery doctrine

Candidate slice order:
1. preserve the keep-now dead-power / no-new-risk owner call and prevent drift
2. demote report chatter if needed
3. cut or simplify only after doctrine and runtime owner collapse

## Historical Closure Quarantine
Consulted historical ancestry surfaces:
- `BRO_HANDOFF_20260405T051017Z_SNIPER_TAKER_PACKET_CLOSEOUT.md`
- referenced closeout proof artifacts in `/home/odah/bro/base/exports/`

Allowed to answer:
- what packet-era taker steel was intentionally added
- why specific competitiveness and event surfaces exist
- which historical packet goals were previously considered sufficient

Explicitly forbidden to answer:
- whether current taker is one brain
- whether current doctrine roots are aligned
- whether current no-fire behavior is acceptable
- whether current live-trust Packet 1 can close

## Bounded Implementation Ladder
| Slice | Exact seam | Why it exists | Must not change | Owner files | Expected proof | Regression risk | Type |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | doctrine-root contradiction cleanup | resolve current owner for `SNIPER_PRIMARY` and live taker authority | runtime behavior until authority story is settled | `docs/DOCTRINE_RUNBOOK.md`, `BRO_CANONICAL_DOCTRINE.txt`, board owners | aligned doctrine + doc guard | medium | semantic cleanup only |
| 2 | vocabulary / owner collapse | remove duplicate live-lane meanings and shadow owners | taker safety interlocks | runtime names, config names, report labels, board docs | one-language map | medium | semantic cleanup only |
| 3 | stage/window authority collapse | remove dead-by-construction stage-local live semantics | hard `<=7s` live doctrine and stage fail-closed | `executor.py`, `paper_universal.yaml`, doctrine docs | timing-clean watched run + semantic check outputs | medium | config/contract change |
| 4 | fallback / market-probability verdict lock and shared-frame defer map | preserve the current keep-now taker no-bounded-fallback interlock and isolate any later shared-frame redesign | fail-closed blind-fire protection | market-reference formation and taker validation path | watched seam run plus doctrine/runtime verdict lock | medium | semantic cleanup only / defer |
| 5 | recovery / handoff collapse | simplify taker dead-power / maker baggage seams | safety posture and no-new-risk doctrine | recovery relays, report consumers | reduced chatter with preserved doctrine | medium | runtime/report mixed |
| 6 | consumer/report truth cleanup | remove second-dialect taker consumer drift | runtime event ownership | `nightly_soak_report.py`, audits, docs | consumer outputs match runtime owner language | low | report/audit consumer change |
| 7 | compensator-fat safe reductions | cut compatibility or dormant shims proven unnecessary before clone-ready surgery | preserve list and board truth | packet-local identified surfaces only | smaller relay count, no doctrine loss | medium | mixed, bounded |
| 8 | shared-frame contact defer slices | tag maker/taker/shared frame cleanup for later | do not cut frame steel early | later frame packet owners | explicit defer map | low now | defer |
| 9 | final runtime behavior repair only if still needed | only after higher-order cleanup | preserve all owner-law and safety interlocks | exact seam files from slices 1-8 | watched runtime closure proof | high | runtime behavior change |

## Current Packet Call
Current verdict:
- `bounded-live-test ready`

Verdict basis:
- `VERIFIED`: Packet 1 has materially collapsed the highest-value
  taker inherited-lineage split-brain owner seams on current code:
  - doctrine-root `SNIPER_PRIMARY` contradiction is source-corrected
  - canonical taker runtime/controller identity is source-collapsed
  - canonical taker runtime enablement and active-mode telemetry are
    source-collapsed
  - the two-surface `effective_stage` / `stage_bucket` contract is explicit and
    guarded
  - the maker/taker fallback asymmetry is explicit, guarded, and classified as
    keep-now interlock / shared-frame defer
- `VERIFIED`: fresh watched Packet 1 proof now earns `bounded-live-test ready`:
  - stored canonical validator bundle is clean with all validator and
    replay-validator exits `0`
  - maker and taker both submitted and filled on the stored bundle
  - accepted taker live-fire trust behavior is present on the stored tape at
    canonical `required_min_edge=0.11`
  - repaired nightly valuation truth no longer flattens event-level degrade
    windows into false `none` report truth
  - compatibility bridges remain correctly deferred without retaining current
    fire authority

Current strongest findings:
- doctrine-root contradiction on `SNIPER_PRIMARY` is source-corrected and now
  guarded at the doctrine-root layer
- remaining runtime/config/report residue is bounded compatibility legacy, not
  a live doctrine-root contradiction
- canonical taker stage config surfaces are now source-collapsed to
  `EXTREME_ONLY`:
  - config validation rejects noncanonical taker stage keys
  - runtime init ignores noncanonical stage-local taker edge/cooldown rows
  - default/profile taker stage aggressiveness and stage-local edge/cooldown
    rows no longer reauthorize `SNIPER_PRIMARY` or `MAKER_TAKER_SELECTIVE`
- raw stage bucket and public stage are `VERIFIED` as separate live runtime
  surfaces in both fresh Packet 1 specimens, and current code now names them
  explicitly as:
  - `effective_stage`
  - `stage_bucket`
  while preserving `stage` / `raw_stage` only as compatibility aliases
- the current owner verdict on that seam is now `VERIFIED` keep-now:
  - `effective_stage` remains the authority surface
  - `stage_bucket` remains the lineage surface
  - alias clutter, not the dual contract itself, is the remaining cleanup mass
- fresh watched Packet 1 artifacts still show the older taker consumer blind
  spot, but current runtime emission now preserves `raw_stage` on
  `edge_evaluation` for new maker and taker rows, and the active Packet 1
  report/audit path now normalizes the explicit `effective_stage` /
  `stage_bucket` contract for current-code artifacts
- canonical taker runtime/controller identity is now source-collapsed on
  current code:
  - controller owner is `_run_taker()`
  - emitted taker owner events are `taker_decision`, `taker_submit`, and
    `taker_stage_window_semantic_check`
  - legacy `sniper_taker` and `sniper_stage_window` forms remain as
    compatibility bridges, not live owner surfaces
- canonical taker runtime enablement and active-mode telemetry are now
  source-collapsed on current code:
  - effective lane enable owner is `taker_enabled`
  - runtime disable reasons are `taker_disabled` and `ramp_taker_disabled`
  - active mode/status emission is `taker_*`, not `sniper_*`
  - historical sniper-family config and helper bridges are removed from current
    code; archived artifacts may still mention them, but they no longer own or
    mirror live taker behavior
- active report/profile outputs touched by Packet 1 now stay canonical:
  - `time_discipline_audit` exposes `taker_*` critical timing evidence only
  - `nightly_soak_report` exposes canonical top-level `taker` summary and
    `taker_lag_gate` posture keys
  - `ops_snapshot`, `ops_brief`, and `paper_live_parity` expose canonical
    `taker_*` operator metrics
  - `fusion_core_profile` no longer advertises a separate `sniper` readiness
    lane
- Packet 1 report-side taker gate posture truth is now source-corrected:
  - raw-root replay of watched run `ff709f61-6e5a-4da4-b164-86739d461f47`
    no longer lies with `no_taker_activity_observed`
  - current report truth now classifies that watched specimen as
    `mixed_normal_and_recovery_taker_gate_activity_observed_no_submit`
  - fresh watched rerun `9cad4c71-a317-40e2-ac87-c54234a06aa7` now proves
    end-to-end live `required_min_edge` carry-through on gate-only taker rows:
    - the pre-surgery `EXTREME_ONLY` normal taker rows carried
      `required_min_edge=0.25`
    - the pre-surgery `EXTREME_ONLY` recovery rows carried
      `required_min_edge=0.25`
    - canonical report truth now records
      `required_min_edge_by_intent_stage.normal_competitiveness.EXTREME_ONLY`
      and `required_min_edge_by_intent_stage.recovery_override.EXTREME_ONLY`
  - the active `EXTREME_ONLY` surgery packet now adds:
    - packet-entry hardening through
      `docs/EXTREME_ONLY_SELF_HARDENING_PACK_2026-05-08.md`
    - canonical paper fixed-shot behavior with no dynamic-size authority
    - no conviction-owned candidate ranking authority on the canonical taker
      path
    - current canonical paper top-level `taker.min_edge=0.11`
- late-window maker/taker fallback asymmetry is `VERIFIED` on current code,
  not just suspected
- late-window maker/taker fallback asymmetry now carries a `VERIFIED`
  keep-now verdict:
  - maker-only bounded single-side fallback remains authorized
  - taker remains explicit fail-closed on missing midpoint
  - any redesign is shared-frame surgery later, not Packet 1-local loosening
- recovery/handoff now carries a `VERIFIED` keep-now owner classification:
  - maker-originated handoff into taker recovery remains dead power
  - timing-gate handoff override remains dead power
  - the seam reads as valid safety interlock, not generic taker sickness
- Packet 1 now has an explicit sniper-derived taker surface map:
  - real current owner steel
  - bounded compatibility bridge mass
  - shared-frame safety / tactical add-ons
  - likely weak legacy carryover for later surgery
  - strongest likely later-surgery targets are:
    - `stage_final_window_sec_by_stage`
    - `aggressive_window_enabled` / `aggressive_window_sec`
    - `stage_priority_enabled`
    - inherited taker helper/config alias mass
- Packet 1 now explicitly audits the external `Taker Sword Blueprint` against
  current BRO taker truth:
  - the current lane matches best on CLOB/IOC execution, fail-closed posture,
    and observability; current paper shot size is temporarily detuned to `$20`
  - the strongest challenge pressure lands on the inherited taker fire-condition
    subtree:
    - hard `<=7s` / `EXTREME_ONLY` window
    - canonical top-level `taker.min_edge=0.11`
    - `multi_oracle_edge_threshold_abs` as boost-only logic
    - `min_visible_fill_ratio`
    - lack of a hard taker-only peak-window whitelist
  - active use or partial Grok overlap does not grant those surfaces doctrine
    steel automatically
- Packet 1 now carries a default-removal verdict on the inherited taker
  subtree:
  - the subtree is not presumed to survive in current housing
  - dormant or redundant stage overlays are first-cut removal candidates
  - real safety/live-parity behaviors must be re-homed if they survive
  - compatibility helpers/aliases are purge targets after migration
- fresh watched rerun `9cad4c71-a317-40e2-ac87-c54234a06aa7` strengthens the
  live abstain-legitimacy call:
  - canonical validation stayed `pass`
  - runtime classification stayed `VALID_ACTIVE`
  - taker gate posture stayed
    `mixed_normal_and_recovery_taker_gate_activity_observed_no_submit`
  - late `EXTREME_ONLY` taker contact remained coherent:
    - `edge_below_min=28`
    - `market_probability_missing=15`
    - `maker_to_taker_recovery_handoff_disabled=45`
  - taker still recorded `0` submits / `0` fills
  - maker recorded `4` submitted orders / `4` filled orders with
    `net_pnl_usd=-498.2233843609022`
  - current live center of gravity is now taker trust economics / threshold
    reality plus market-truth availability, not hidden owner-language drift
- per-relay leaf-map completion is now `VERIFIED` on the active Packet 1 path:
  - only six relays currently need both `effective_stage` and `stage_bucket`
  - stage permission, stage transitions, and the split stage-count gauges each
    need only one side of the contract
  - the remaining fat on this seam is alias baggage, not unresolved relay
    ownership
- config-bridge / helper ancestry now carries a `VERIFIED` defer verdict:
  - active taker ownership is already canonical
  - remaining `sniper.*` config names and helper aliases are bounded
    compatibility bridges
  - removal belongs to later clone-safe cleanup, not Packet 1 closure work

Current lane status:
- latest watched 10-minute current-tree specimen
  `6957087b-488e-4bbb-b8b9-1f215b5e33d0` produced a stored-bundle clean
  closeout pass:
  - `validation_summary.json`: `ok=true`, `overall_exit_code=0`
  - maker `1 submit / 2 fills`
  - taker `1 submit / 1 fill`
  - action-row source purity clean on both lanes
- repaired nightly valuation truth now carries event-level degrade windows
  through current report truth instead of flattening them to `none`.
- Packet 1 is now authorized to close `bounded-live-test ready` and hand the
  next implementation lane to `Packet 2 Maker-Live`.

## Maker/Taker Independence Blocker Map
- `VERIFIED`: current BRO runtime does **not** treat maker and taker as
  independent concurrent authorities. The active owner chain still includes
  multiple shared shells where one lane can subordinate or suppress the other.
- `VERIFIED`: the first coupling surface is the stage-policy root itself:
  - current stage owner law is:
    - `MAKER_POSITION`: maker only
    - `MAKER_TAKER_SELECTIVE`: maker only
    - `SNIPER_PRIMARY`: neither
    - `EXTREME_ONLY`: neither at the raw stage-policy layer
  - late-window authority still remains shared higher up through runtime
    translation and sequencing shells, but the raw stage-policy root no longer
    grants concurrent maker+taker fire authority by itself
- `VERIFIED`: `_token_stage_info()` adds a stronger shared-shell layer above raw
  stage timing:
  - historical watched run `815ae355-e18d-44bf-9f80-6679ec11fd03` proved a
    real maker-first late-window theft surface:
    - `raw_stage=EXTREME_ONLY`
    - `effective_stage=MAKER_TAKER_SELECTIVE`
    - `maker_allowed=true`
    - `taker_allowed=false`
    - clustered around about `19.6s -> 15.6s` remaining
  - current code no longer uses `maker_timing_stage_override_active` to steal
    late `EXTREME_ONLY` authority back into `MAKER_TAKER_SELECTIVE`
  - remaining effective-stage remap owners are now:
    - `observe_hold_active`
    - `expired_reduce_only_grace_active`
    - `reduce_only_recovery_active`
- `VERIFIED`: `_taker_context()` is a separate pre-stage taker authority shell:
  - near-token selection now keeps all non-expired tokens inside the broad
    `sniper_arming_horizon_sec` shell
  - current defaults still come from sniper-era housing:
    - `arming_horizon_sec=86400`
    - `execution_cutoff_sec=10`
  - `execution_cutoff_sec` no longer suppresses true late-window taker tokens
    before stage truth is consulted; it is now bounded compatibility mass, not
    active taker reachability authority
- `VERIFIED`: taker activation and taker execution no longer diverge on
  token-set ownership:
  - canonical taker runtime now derives one stage-window token set from the
    near-token window intersected with current taker-stage eligibility
  - `taker_active` and `_run_taker()` both consume that same stage-window set
  - non-taker-stage near-window tokens remain diagnostic-only through doctrine
    and stage surfaces; they no longer silently shape taker-active posture or
    taker runtime input
- `VERIFIED`: this closes the old activation-vs-execution split where
  `candidate_taker_tokens` drove `taker_active` while `_run_taker()` still
  consumed broader `near_tokens`
- `INFERRED`: remaining independence debt is now centered on shared
  orchestration, cleanup, and market-family/posture contracts, not on a second
  hidden taker reachability shell
- `VERIFIED`: `_resolve_financial_posture_class()` is a whole-fighter authority
  layer, not a lane-local one:
  - if any token trips reduce-only recovery, the entire posture can promote to
    `PREEXPIRY_REDUCE_ONLY`
  - if terminal unwind conditions are met, posture can promote to
    `HALT_NEW_RISK`
  - that posture then participates in maker/taker suppression through
    operating-mode and lifecycle decisions
- `VERIFIED`: shared manager orchestration is another live coupling layer above
  local edge truth:
  - Packet 1 removed the old shared `OrderManager` soft-rate budget and
    action-budget mutation from `taker_active`
  - normal taker still shortens its own cycle cadence in the final-fire lane
    so the weapon is not dulled inside the canonical `<=7s` window
  - maker cleanup/orchestration still runs first on the shared manager, so
    sequencing interference remains an active patient even after the shared
    budget shell was cut
- `VERIFIED`: maker cleanup still runs first on the shared manager / shared
  `tx_manager` before taker evaluates:
  - canonical maker path builds `tracked_token_cancel_reason_by_token` across
    the tracked token universe
  - `manager.step(...)` then executes `tracked_token_cleanup` /
    `orphan_token_order` cleanup before `_run_taker()` is called
  - taker later submits through the same `OrderManager.place_taker_order...`
    surface
- `INFERRED`: that shared cleanup-first sequencing is another likely false
  authority shell because maker-side cleanup ownership is still structurally
  upstream of taker execution ownership
- `VERIFIED`: `_run_taker()` also remains subordinate to whole-cycle runtime
  shells beyond stage truth:
  - `operating_mode_maker_only`
  - `operating_mode_safe_stop`
  - `operating_mode_non_normal`
  - `latency_not_armed`
  - `ramp_taker_disabled`
  - `maker_to_taker_recovery_handoff_disabled`
- `VERIFIED`: the misunderstood market-ownership / wait family has been
  removed from current Packet 1 runtime:
  - maker and taker no longer wait on each other through same-market
    commitment, sibling-market conflict, or post-fill family cleanup shells
  - each lane now relies on its own timing gates plus its own direct truth
    prerequisites instead of market-base exclusivity logic
- `VERIFIED`: this means the current maker/taker chain contains two different
  kinds of couplers:
  - likely false-authority / legacy-shell couplers:
    - stage mutual-exclusion root
    - raw-stage remap into maker-owned effective stage
    - sniper-era taker context cutoff shell
    - shared manager budget / cleanup-first orchestration shell
    - whole-fighter posture promotion above lane-local authority
  - likely real safety interlocks that still intentionally couple the lanes:
    - recovery dead-power / handoff disabled path
- `VERIFIED`: current BRO therefore does **not** satisfy the requirement
  `maker and taker operate with total independence from each other`
- `INFERRED`: the strongest upstream repair target is no longer just
  `EXTREME_ONLY` naming. It is the shared authority architecture itself:
  - replace sniper-era taker context shells with canonical taker arming logic
  - remove shared manager budget / cleanup-first orchestration as a false
    cross-lane owner where possible
  - separate raw time bucket from lane authority
  - remove maker-first remap ownership from taker live authority
  - decide which safety interlocks stay as explicit cross-lane contracts and
    which false-authority shells get deleted

Immediate next packet work:
1. lock this Packet 1 closeout truth against the watched specimen
   `6957087b-488e-4bbb-b8b9-1f215b5e33d0`
2. keep the doctrine-root, two-surface stage contract, fallback verdict, and
   compatibility-bridge defer calls fixed as closed Packet 1 truth
3. open `Packet 2 Maker-Live / Economic Trust Qualification` as the next
   bounded `pilot_live` packet through:
   - `docs/BRO_PILOT_LIVE_TRUST_PACKET_2_MAKER_QUALIFICATION_2026-05-10.md`
