# BRO Pilot-Live Packet 2: Downstream Maker Filter Hostile Tribunal (2026-05-26)

## Classification
- `active packet owner`
- `pickup bridge`
- `read-only hostile tribunal closeout`

## Purpose
- `VERIFIED`: this tribunal exists to decide whether the first surviving
  post-Packet-2C maker families are:
  - real blueprint steel,
  - real but blueprint-wrong runtime authority,
  - or refined slag still acting as owner-law.
- `VERIFIED`: it also exists to decide whether the current next-packet routing
  through lifecycle residue is truthful or drifted.

## Authority Boundary
- `VERIFIED`: this file is a packet-local hostile tribunal owner.
- `VERIFIED`: it does not authorize runtime mutation by itself.
- `VERIFIED`: it may reroute the next bounded packet when stronger runtime +
  blueprint truth beats the prior pickup story.

Primary owner chain:
1. `docs/DOCTRINE_RUNBOOK.md`
2. `docs/BRO_MARKET_LIFECYCLE_BLUEPRINT_2026-05-16.md`
3. `docs/GALAXY_MEGA_MAKER_CANNON_DOCTRINE_PROPOSAL_2026-04-28.md`
4. `executor.py`
5. `prodesk/order_manager.py`
6. watched Packet 2C proof `4ee0cf47-dec6-44b9-bdd2-764abf2c3b47`
7. `docs/NEXT_PACKET_PLAN.md`
8. `docs/PROJECT_TRUTH_STATE.md`

## No-Change Guard
- no readiness-family rebuild
- no timing-band mutation
- no cannon-target tuning
- no queue/fill threshold loosening by mood
- no geometry retune by frustration
- no lifecycle alias cleanup presented as runtime surgery

## Evidence Anchors
- watched Packet 2C proof:
  - `4ee0cf47-dec6-44b9-bdd2-764abf2c3b47`
  - `runtime_classification=VALID_ACTIVE`
  - maker submitted `3` orders and produced `4` fill events
- lifecycle residue derivation:
  - `executor.py` `_token_lifecycle_obligation_flags()`
  - `open_order_cleanup_required = open_order_present`
  - `settlement_hold_required = abs(net_shares) > 0`
- maker filter runtime roots:
  - `prodesk/order_manager.py` selection, feasibility, and quote-quality
    reject leaves
- blueprint/doctrine inputs:
  - lifecycle blueprint says non-actionable execution geometry should be
    rejected early
  - maker doctrine proposal explicitly requires `1.5x` maker depth relative to
    canonical maker size
  - maker doctrine proposal treats `queue_ahead_size` and
    `expected_fill_prob` as observability / shadow inputs first, not automatic
    runtime owner-law

## Runtime Findings
### 1. Lifecycle pair
- `VERIFIED`: `open_order_cleanup_required` and
  `settlement_hold_required` are currently derived from state facts, not
  opportunity law:
  - open order exists
  - accepted exposure exists
- `VERIFIED`: in watched proof `4ee0cf47...`, those two reasons appeared only
  under lifecycle phase `resolve`, never `maker_window`.
- `INFERRED`: they are real lifecycle/state descriptors, but the prior call
  that elevated them into the next maker surgery headline was wrong.

### 2. Selection depth leaf
- `VERIFIED`: `launch_safe_selection_insufficient_depth_multiple` is current
  runtime selection authority.
- `VERIFIED`: watched proof showed `18` rows on this leaf, all inside
  `maker_window`.
- `VERIFIED`: the lifecycle blueprint + maker doctrine target both support a
  real depth-vs-cannon owner.
- `VERIFIED`: this family was deliberately re-seated during Packet 2A and is
  not a mere report leftover.

### 3. Queue-depth quote-quality leaf
- `VERIFIED`: `quote_quality_skip_queue_depth` is current runtime quote-quality
  authority.
- `VERIFIED`: watched proof showed `4` rows on this leaf, all inside
  `maker_window`.
- `VERIFIED`: those rows carried extreme queue / fillability weakness.
- `VERIFIED`: the maker doctrine proposal names `queue_ahead_size` and
  `expected_fill_prob` as report/shadow ingredients first, not automatically as
  live trigger-law.
- `INFERRED`: this is the most suspicious downstream maker family now alive on
  current code.

### 4. Geometry leaf
- `VERIFIED`: `non_actionable_geometry` is current runtime feasibility
  authority.
- `VERIFIED`: watched proof showed `2` rows on this leaf, both inside
  `maker_window`.
- `VERIFIED`: the lifecycle blueprint explicitly supports early rejection of
  non-actionable execution geometry.
- `INFERRED`: the concept is blueprint-aligned even if future doctrine may
  later change the exact fixed-shot handling.

## Tribunal Verdict Table
| Family | Runtime reality | Blueprint alignment | Tribunal call |
| --- | --- | --- | --- |
| `open_order_cleanup_required` | real resolve-phase state descriptor | lifecycle residue only, not entry/selection owner | `DEMOTE / EXTINCT FROM NEXT-PATIENT AUTHORITY` |
| `settlement_hold_required` | real resolve-phase state descriptor | lifecycle residue only, not entry/selection owner | `DEMOTE / EXTINCT FROM NEXT-PATIENT AUTHORITY` |
| `launch_safe_selection_insufficient_depth_multiple` | real maker-window filter | aligned with `1.5x` maker depth doctrine | `KEEP AS PROVISIONAL STEEL` |
| `quote_quality_skip_queue_depth` | real maker-window filter | suspicious; blueprint supports it as observability first, not proven runtime steel | `ACTIVE PATIENT / REGRADE HARD` |
| `non_actionable_geometry` | real maker-window filter | aligned with early non-actionable rejection law | `KEEP / LEAF-SPECIFIC REVIEW` |

## Routing Verdict
- `VERIFIED`: the prior next-packet routing to `maker post-fill lifecycle
  residue / cleanup surgery` loses this hostile reread.
- `VERIFIED`: the lifecycle pair remain explicit state truths, but they no
  longer own the next bounded packet.
- `VERIFIED`: the downstream maker filter family wins the reroute.
- `VERIFIED`: inside that family, `quote_quality_skip_queue_depth` is the
  first suspicious live authority.
- `INFERRED`: `quote_quality_skip_fill_probability` belongs in the same hostile
  family even though it did not dominate the watched Packet 2C specimen.
- `VERIFIED`: Packet 2D now treats both leaves as active excision patients
  rather than provisional maker steel.

## Recommendation
- first bounded next packet:
  - `Packet 2D maker quote-quality authority excision and threshold-semantics purge`
  - scope:
    - `quote_quality_skip_queue_depth`
    - `quote_quality_skip_fill_probability`
    - paired queue/fill-probability runtime-owner semantics
  - rule:
    - no threshold tuning first
    - cut maker runtime/report/doc authority for this family without
      backfilling a successor owner
- second bounded packet if still needed:
  - geometry / sizing leaf-specific reread
- lifecycle pair disposition:
  - keep as explicit lifecycle/state truth
  - stop treating them as the current maker-patient headline

## No-Fake-Closure Note
- `VERIFIED`: Packet 2D rejects `quote_quality_skip_queue_depth` and
  `quote_quality_skip_fill_probability` as accepted active-path maker law
- `VERIFIED`: this tribunal is strong enough to reroute the packet order
- `VERIFIED`: this tribunal was not yet the surgery itself when first written
- `VERIFIED`: watched proof for the resulting surgery is now closed on
  `ef021ded-99b3-4b56-ac55-7f12592a67e0`
  - maker produced `2` viability rows, `2` snapshots, and `2` submits
  - zero current runtime rows emitted the excised quote-quality family
  - zero stealth `no_desired_quote` relapse appeared in the watched specimen
  - later downstream runtime truth now reads:
    - `launch_safe_selection_insufficient_depth_multiple` as the dominant
      surviving downstream filter
    - `non_actionable_geometry` as the secondary downstream leaf
