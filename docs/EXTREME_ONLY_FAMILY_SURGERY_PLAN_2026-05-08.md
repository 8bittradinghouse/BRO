# EXTREME_ONLY Family Surgery Plan (2026-05-08)

## Classification
- `historical-only`
- this plan belongs to the pre-lifecycle-surgery stage-family packet
- old stage-family language here is ancestry only

## Purpose
- `VERIFIED`: this is the surgery map for the remaining `EXTREME_ONLY` family.
- `VERIFIED`: it is a planning and classification aid for the next bounded taker cleanup packet.
- `VERIFIED`: it is not itself proof that the family is already closed.
- `VERIFIED`: packet entry now begins through
  `docs/EXTREME_ONLY_SELF_HARDENING_PACK_2026-05-08.md`.

## Current Doctrine Call
- `VERIFIED`: raw `EXTREME_ONLY` is now a late-window lineage bucket, not live owner-law by itself.
- `VERIFIED`: current live late-window authority is expressed through explicit runtime fields:
  - `maker_new_risk_allowed`
  - `normal_taker_allowed`
  - `reduce_only_recovery_allowed`
  - `preexpiry_emergency_taker_allowed`
- `VERIFIED`: the family is still not fully cleaned because raw bucket naming, threshold leaves, accessory leaves, compatibility language, and report semantics still cluster around `EXTREME_ONLY`.

## Family Map
### Keep now
- raw lineage bucket production in `_stage_name_for_sec_to_expiry()`
  - keep only as lineage/readback while the two-surface contract still exists
- explicit late-window authority fields
  - these are the real current owners
- `stage_bucket` plus `effective_stage`
  - keep as explicit dual-surface truth until later alias surgery is safe

### Under challenge
- `sniper.taker.min_edge_by_stage.EXTREME_ONLY`
  - active threshold leaf
  - strongest likely over-tight sheath candidate
- `stage_aggressiveness.EXTREME_ONLY`
  - likely accessory mass if fixed shot geometry remains the real law
- `multi_oracle_*` boost leaves when they act as accessory overlays instead of direct taker truth
- `min_visible_fill_ratio`
  - likely real concern, but may still be over-tight relative to the blueprint

### Likely slag / cut candidates
- dormant stage-local overlays already fenced from canonical authority:
  - `stage_final_window_sec_by_stage`
  - noncanonical stage-key threshold/cooldown mass
- stale report/doc semantics that still let raw `EXTREME_ONLY` language sound like owner-law
- sniper-era alias bridges once current canonical taker owner path is fully stable

## Surgery Order
1. keep explicit authority fields fixed
2. keep raw bucket as lineage only
3. simplify accessory leaves around `EXTREME_ONLY`
4. detune threshold only in bounded paper testing
5. watched paper specimen
6. only then consider later bucket/alias purge

## First Bounded Packet
### Goal
Reduce the chance that `EXTREME_ONLY` accessory mass keeps taker permanently sheathed.

### Candidate moves
- disable live dynamic-size authority on the canonical taker path
- remove conviction-owned candidate ranking authority from the canonical taker path
- lower `EXTREME_ONLY` threshold to `0.18` in bounded paper test
- leave real fail-closed truth gates intact
- keep `multi_oracle_boost_*` and `min_visible_fill_ratio` unchanged in the
  first packet unless a watched specimen proves they are the active choke

### Non-goals
- no broad early-window widening
- no weaker midpoint truth
- no weaker oracle fail-closed
- no generic tuning spree

## Closure Test
The family is not closed until:
- raw `EXTREME_ONLY` no longer acts like live owner-law
- reports/docs/runtime all tell the same late-window story
- accessory leaves no longer dominate real taker execution
- a watched paper run proves taker can actually fire when blueprint conditions are present
