# BRO Pilot-Live Trust Packet 2: Maker Path Forensic Semantic Audit (2026-05-10)

## Role Declaration
- `VERIFIED`: bounded board sink

## Purpose
- `VERIFIED`: this audit materializes the missing current-code maker river map for `Packet 2`.
- `VERIFIED`: it exists to classify each meaningful maker-path seam before any runtime surgery is considered legal.
- `VERIFIED`: it is a read-only packet-local audit surface, not a tuning authorization surface.

## Authority Boundary
- `VERIFIED`: current packet authority still belongs first to:
  - `docs/BRO_PILOT_LIVE_TRUST_PACKET_2_MAKER_QUALIFICATION_2026-05-10.md`
  - runtime owner surfaces in:
    - `executor.py`
    - `prodesk/order_manager.py`
    - `prodesk/risk.py`
    - `configs/profiles/paper_universal.yaml`
- `VERIFIED`: this audit may classify, fence, and sharpen surgery legality.
- `VERIFIED`: this audit may not loosen thresholds, promote support tooling into owner-law, or authorize live arming.

## Mission Lock
- `VERIFIED`: prove the live maker path in one language from timing seed to outcome truth, so later economic-trust calls rest on strong owners instead of mixed stories.

## No-Change List
- no generic maker tuning
- no timing-window mutation by momentum
- no threshold loosening by frustration
- no support-tool promotion into runtime authority
- no history-to-present power laundering
- no live arming

## Current-Code Maker River Map
| Step | Strong surface | What it currently owns | Surface class | Current semantic or timing warning | Initial Packet 2 surgery class |
| --- | --- | --- | --- | --- | --- |
| 1 | `executor.py` stage / lifecycle seed surfaces | effective stage, stage lineage, and `sec_to_expiry` seed that later feeds maker gating and lifecycle context | `owner-law` | the path must keep one `sec_to_expiry` story from stage map into later submit/risk/report surfaces | `KEEP` |
| 2 | `executor.py::_maker_prereq_failure_reason()` | pre-eligibility fail-closed checks before maker is even allowed to compete | `valid interlock` | prereq failure explains legal abstain, not final economic meaning | `KEEP` |
| 3 | `executor.py::_maker_timing_gate_open()` plus active paper maker timing leaves | late-window eligibility for current paper maker posture | `valid interlock` | active lower bound is not semantically identical to the later risk gate lower bound | `KEEP` |
| 4 | `executor.py::_apply_canonical_maker_ws_source_gate()` | `maker_requires_ws_book_source` fail-closed ownership | `valid interlock` | this is source-truth legality only; it may not masquerade as expectancy proof | `KEEP` |
| 5 | `executor.py::_resolve_maker_market_reference_inputs()` and authoritative reference filter | `market_reference_not_authoritative`, midpoint legitimacy, and whether the row is even fit for maker evaluation | `owner-law` | many rows can die here before any selectivity argument is legal | `KEEP` |
| 6 | `executor.py::_maker_competitiveness_profile()` plus active paper profile leaves | size, spread, requote, one-sided posture, and recovery-context handoff for admissible rows | `owner-law` | this is live runtime leaf synthesis, but it still does not settle economic safety by itself | `KEEP` |
| 7 | queue-pressure family historical lineage | runtime/config/report family was removed on current code; only legacy-config ignored compatibility and archive replay awareness remain | `historical-only` | ancestry may still explain old packets, but current maker river may not carry silent queue-pressure mutation authority | `CUT / HISTORICAL-ONLY LINEAGE` |
| 8 | `order_manager.py::_maybe_clamp_post_only_intent()` plus `auth.enforce_post_only` | hard safety stop against accidental taker crossing | `valid interlock` | pure safety steel; must not be confused with maker fight-quality doctrine | `KEEP` |
| 9 | `order_manager.py::_evaluate_maker_selection_gate()` | secondary confirmation, repeat-target calm, optional timing-window fields, depth-vs-cannon comparison, and gate reject reasons | `owner-law` | this gate carries mixed leaf classes: some are current steel, some are dormant, and depth-multiple is currently blueprint-delta rather than live-paper owner law | `KEEP / LEAF SPLIT` |
| 10 | `order_manager.py` replace guard and cancel-replace path | cadence legality, replace-vs-hold logic, and `replace_guard_min_rest` abstain geometry | `valid interlock` | participation pain can live here without this surface owning the whole economic story | `KEEP` |
| 11 | `risk.py` new-exposure / size / open-order / book-freshness gates | legality of the actual submit under capital, timing, size, and freshness rules | `valid interlock` | maker inherits the global `15.0s` new-exposure gate, so the final left edge is stricter than the upstream maker timing gate alone | `KEEP` |
| 12 | `executor.py::_build_submission_lifecycle_context()` and lifecycle-context events | one-lane propagation of stage, authority, posture, and `sec_to_expiry` into submit/risk/lifecycle truth | `owner-law` | missing or mismatched lifecycle context is a fail-closed packet truth, not harmless wrapper noise | `KEEP` |
| 13 | emitted runtime truth surfaces (`edge_decision`, shadow events, submit/lifecycle events) | strongest readable truth of what maker saw, chose, submitted, and why it abstained | `owner-law` | later reports must stay aligned with this emitted truth instead of inventing a cleaner dialect | `KEEP` |
| 14 | validator surfaces (`edge_truth_audit`, `order_lifecycle_audit`, `outcome_truth_audit`) | hostile reread and population-proof against emitted runtime truth | `support-only` | stronger than wrappers, weaker than emitting runtime owners | `KEEP BUT FENCE` |
| 15 | `nightly_soak_report`, `FMA`, admission shadow, schematic, semantics map | operator summary, cross-run compression, research bridge, and atlas structure | `support-only` | these surfaces are highly useful and still illegal as runtime owner-law | `KEEP BUT FENCE` |

## Bad Authority / Doctrine-Semantic Break Register
| Break | Why it matters | Current Packet 2 call | First legal move |
| --- | --- | --- | --- |
| maker left-edge timing split | executor maker gate opens at `>=15.0`, while risk blocks new exposure at `<=15.0`, so the effective live paper submit window is stricter than the plain-language gate story | `VERIFIED` | preserve this explicitly in the timing owner layer before any timing debate |
| dormant duplicate timing carrier in selection gate | selection gate can also hold `min_sec_to_expiry` / `max_sec_to_expiry`, even though current paper leaves are unset | `VERIFIED` | keep dormant and fenced unless a later packet explicitly authorizes a second timing owner |
| depth-multiple split-brain | resolved on current paper: code/default/blueprint lineage and live paper leaf now align at `1.5x` | `VERIFIED` | reclassify as live-paper runtime authority, not blueprint-only delta |
| queue-pressure mutation on admissible fights | historical runtime family that previously mutated quote posture before submission and could force replacement behavior | `VERIFIED` | preserve only as historical lineage while current code keeps the family cut |
| queue-pressure downstream visibility asymmetry | historical reports often surfaced queue-depth pain more loudly than the old queue-pressure behavior branch itself | `VERIFIED` | preserve as ancestry-only semantics so future relocks do not resurrect live authority |
| recovery family carrying strategy-looking relaxations under safety cover | recovery headroom and quality relaxations may be valid safety behavior or economics laundering | `INFERRED` | keep fenced and compare against normal-maker truth populations |
| support-layer outcome compression seam | maker wound ratios in compressed harvest/tool layers can sound canonical even though raw outcome records and canonical outcome-truth populations are stronger owners | `VERIFIED` | keep compressed summaries fenced beneath raw outcome truth |
| historical run-manifest timing posture misread as current doctrine | historical packet-era runtime carried dirty-tree timing contracts that differ from current code | `VERIFIED` | keep those specimens as ancestry only and chain them through manifest truth first |
| support-tool promotion risk | `FMA`, admission shadow, and atlas surfaces can sound cleaner than runtime | `VERIFIED` | keep every packet answer chained back to emitting owners and validators |

## Initial Surgery Ledger
| Surface or family | Present class | Packet 2 surgery class | Legal next move |
| --- | --- | --- | --- |
| authoritative market-reference filter + WS-source legality | `owner-law` plus `valid interlock` | `KEEP` | treat as upstream truth gate, not as downstream economics verdict |
| post-only cross guard | `valid interlock` | `KEEP` | preserve as safety steel |
| selection-gate secondary confirmation + repeat-target calm | `owner-law` | `KEEP` | preserve until outcome proof disproves their legitimacy |
| selection-gate depth multiple leaf | `owner-law` | `KEEP` | current paper leaf now carries `1.5`, so this gate is back as live runtime steel |
| selection-gate dormant timing leaves | `compatibility seam` | `REHOME THEN CUT` candidate | keep dormant; if later activated, require explicit timing-owner review |
| maker queue-pressure family | `historical-only` | `CUT / HISTORICAL-ONLY LINEAGE` | keep only legacy-config ignored seam and archive replay awareness; forbid live/runtime/report resurrection |
| maker recovery family | `current-code cut on maker lane` | `CUT MAKER PRIVILEGES / KEEP SHARED SAFETY SPINE SEPARATE` | maker-side recovery relaxations and maker permission backfill are removed; shared recovery safety context remains a separate Packet 1/whole-fighter tribunal |
| dust-path hygiene family | `current-code cut` | `CUT CURRENT-LIVE DUST AUTHORITY` | dust shadow/enforcement authority is removed; only descriptive dust classification plus narrow retired-market non-defect handling remain |
| validators, wrappers, probes, and atlas surfaces | `support-only` | `KEEP BUT FENCE` | preserve usefulness, forbid owner-law impersonation |

## Strongest Falsifiers So Far
1. If authoritative-market-reference-only rows still show the same small-loss geometry, upstream truth-thin rows are not the primary economic owner.
2. If recovery-tagged maker rows remain economically isolated from normal-maker rows, the recovery family may be a valid safety boundary rather than strategy drift.
3. If exact `15.0s` rows never meaningfully survive to submit in live paper cadence, the left-edge timing split may be semantically real but economically low-severity.

## Current Packet 2 Read
- `VERIFIED`: the maker river is now explicit enough to support hostile rereads without collapsing into one blob.
- `VERIFIED`: the strongest current owner seams are upstream truth legality, active paper selectivity, risk legality, lifecycle propagation, and emitted runtime truth.
- `VERIFIED`: the strongest current economic-owner candidate is complete-outcome maker truth, with market-truth substrate held as the foundation trace that can still pollute or validate that answer.
- `VERIFIED`: the loudest current specimen frictions are quote-quality / queue-depth / replace-guard participation pain and the layered timing story, not a loud active recovery or dust patient.
- `VERIFIED`: the strongest current challenge seams are support-layer outcome compression, support-tool promotion, the layered timing story, and the coupled dust + maker recovery cluster.
- `INFERRED`: this audit is strong enough to legalize later family-cut planning, but not yet strong enough to bless runtime surgery by itself.
