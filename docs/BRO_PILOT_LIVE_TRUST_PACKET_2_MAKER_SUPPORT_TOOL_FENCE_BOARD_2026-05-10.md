# BRO Pilot-Live Trust Packet 2: Maker Support-Tool Fence Board (2026-05-10)

## Classification
- `historical-only`
- this is a Packet 2 support-tool boundary artifact
- lifecycle-era runtime ownership now lives elsewhere; any old stage-family
  language here is support-history only

## Purpose
- `VERIFIED`: this board materializes the Packet 2 support-tool vs runtime-owner split.
- `VERIFIED`: it exists to keep useful tooling useful without letting it silently reacquire runtime power.

## Fence Rule
- `VERIFIED`: useful does not equal authoritative.
- `VERIFIED`: validator/readout surfaces are stronger than wrappers and shop tools.
- `VERIFIED`: even strong validator/readout surfaces still do not outrank emitted runtime truth.
- `VERIFIED`: canonical paper proving should still enter through the public
  operator front door:
  - `broctl paper -- --active-minutes <minutes> --wait-sec 25`
- `VERIFIED`: backend wrappers like `scripts/canonical_paper_session.sh` remain
  implementation machinery behind that front door, not the first operator
  doctrine surface.

## Support-Tool Fence Board
| Surface | Class | Safe use | Forbidden overreach | Current call |
| --- | --- | --- | --- | --- |
| `scripts/edge_truth_audit.py` | validator surface | certify emitted edge-row semantics and canonical vocabulary | outrank runtime roots or mutate strategy by itself | `KEEP` |
| `scripts/order_lifecycle_audit.py` | validator surface | certify lifecycle coherence and missing-link geometry | become a substitute for runtime lifecycle ownership | `KEEP` |
| `scripts/outcome_truth_audit.py` | validator surface | certify outcome-population summaries and wound shape | become the sole owner of economic meaning without runtime reconciliation | `KEEP` |
| `scripts/nightly_soak_report.py` | downstream consumer/wrapper | provide operator-facing synthesis and cross-surface summaries | outrank raw event/runtime truth or hide mixed populations | `KEEP BUT FENCE` |
| `scripts/bro_metric_harvest.py` / `FMA` | support-only shop tool | compress report truth into reusable engineering instruments | act like BRO body steel or runtime owner-law | `KEEP BUT FENCE` |
| `maker_fight_admission_shadow*` | research bridge / slag candidate | classify and compare candidate fights for study | gate live maker runtime or certify live trust by itself | `KEEP BUT FENCE / CHALLENGE-OPEN` |
| timing ladder / mid-window / late-window probes | comparative support tool | observe alternative timing bands and late-window populations | become present-tense doctrine without packet approval | `KEEP BUT FENCE` |
| `docs/SOLAR_SLUG_MAKER_CIRCUIT_SCHEMATIC.md` | atlas | preserve pathway, population, and overlay structure | authorize runtime tuning or leaf promotion | `KEEP BUT FENCE` |
| `docs/SOLAR_SLUG_MAKER_TRUTH_SEMANTICS_MAP.json` | atlas / machine-readable legend | preserve map classifications and cross-run semantics | act like runtime owner or strategy law | `KEEP BUT FENCE` |
| maker comparator / fireability / low-price forensic packs | historical forensics | explain ancestry, contrasts, and candidate wound families | certify current live trust on their own | `KEEP BUT FENCE` |

## Current Packet 2 Fence Notes
- `VERIFIED`: `FMA` is the clearest fence case:
  - extremely useful
  - not runtime authority
  - not doctrine root
  - not permission to call closure
- `VERIFIED`: `financial_outcome_reconciliation` under the nightly report lane is support-only:
  - useful for proving when ledger cashflow and fixed-horizon outcome truth are different quantities
  - forbidden from impersonating runtime owner-law or forcing unlike economic surfaces to match numerically
- `VERIFIED`: the admission shadow remains a research bridge, not a live gate owner.
- `VERIFIED`: the admission shadow is useful only while it still improves
  reasoning density; if it remains dead mass relative to stronger owners, it
  should be eligible for later slag-removal review.
- `VERIFIED`: the schematic and semantics map are now useful only if they keep pointing back to stronger owners instead of trying to become stronger owners themselves.
- `VERIFIED`: maker-facing nightly/probe support surfaces must lead with:
  - `maker_new_risk_allowed`
  - `late_window_authority_class`
  - `effective_stage`
  while raw `EXTREME_ONLY` survives only as lineage / ancestry context.
