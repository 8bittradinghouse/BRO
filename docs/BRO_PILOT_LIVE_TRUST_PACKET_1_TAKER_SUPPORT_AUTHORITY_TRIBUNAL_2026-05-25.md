# BRO Pilot-Live Packet 1: Taker Support-Authority Tribunal

## Classification
- `follow-on packet owner`
- `pickup bridge`
- diagnosis-first taker hygiene packet opened on `2026-05-25`
- this packet does not reopen taker runtime rescue surgery by momentum

## Purpose
- clean the same *types* of residue maker exposed:
  - fake-primary config families
  - split support contracts
  - underpowered event witnesses
  - weaker observer surfaces
  - stale or descriptive artifacts posing as owner-law
- preserve the restored taker runtime core while fencing weaker support truth

## Authority Lock
Current pickup point:
- phase:
  - `pilot_live`
- macro lane:
  - `live trust qualification`
- immediate preceding lane:
  - `Packet 2 maker runtime-truth tribunal`
- this packet:
  - `Packet 1 taker support-authority tribunal`
- wallet lane:
  - still downstream and fail-closed

Authority chain:
1. `docs/JIN_RELOCK_PACK_2026-05-12.md`
2. `docs/PROJECT_TRUTH_STATE.md` as broad repo truth only
3. `docs/NEXT_PACKET_PLAN.md`
4. `docs/OPEN_LIMITATIONS.md`
5. `docs/BRO_PILOT_LIVE_TRUST_BOARD_SINK_2026-05-05.md`
6. `docs/BRO_PILOT_LIVE_TRUST_QUALIFICATION_PACKET_PROGRAM_2026-05-05.md`
7. `docs/BRO_PILOT_LIVE_TRUST_PACKET_1_TAKER_QUALIFICATION_2026-05-06.md`
8. `docs/BRO_PILOT_LIVE_TRUST_PACKET_1_TAKER_ARCHITECTURE_SURGERY_2026-05-22.md`
9. runtime owners:
   - `executor.py`
   - `prodesk/taker_competitiveness.py`
   - `prodesk/order_manager.py`
   - `prodesk/gateway.py`
   - `prodesk/risk.py`
   - `configs/profiles/paper_universal.yaml`
10. consumer and support surfaces:
   - `scripts/nightly_soak_report.py`
   - `prodesk/runtime_semantics.py`
   - status and telemetry rows

## No-Change Steel
- no new taker architecture surgery by momentum
- no threshold loosening
- no `min_edge` retune
- no visible-fill gate relaxation
- no spend-model changes unless a new runtime contradiction is proven
- no wallet drift

## Keep-Now Steel
- normal `taker_window` ownership
- min-edge fire law
- one-shot-per-window discipline
- executable-price sizing
- `TAKER_COMMITMENT` preservation on the normal lane
- secondary-oracle confirmation requirement
- report-side taker classification path

## Evidence Base
Primary runtime set used in this tribunal:
- current successful taker shots:
  - `bb592ab0-a5c6-48be-abda-87dcbe78cc52`
  - `1e35a29a-9fba-4563-9e95-b9a38ad360c6`
  - `0b4932f5-d599-4a2f-990e-a4341d2771d0`
- broader current-code behavioral contrast:
  - `f4d16884-844b-4cf6-bdf2-b596bb3ccb43`
- fresh watched taker behavior specimen:
  - `316e34b5-d291-4ade-82b8-04cf345a911c`
- prior fresh inert substrate specimen:
  - `17fe07e3-32ec-43e7-950f-2481ac05b9ff`

Evidence order used:
1. raw `events_*.jsonl`
2. raw `status_*.jsonl`
3. canonical report bundle artifacts
4. support / descriptive surfaces

## Runtime-Core Verdict
- `VERIFIED`: current taker normal-lane runtime still looks structurally clean.
- `VERIFIED`: recent successful shots preserved:
  - `lineage_stage=TAKER_COMMITMENT`
  - `lifecycle_phase=taker_window`
  - `target_usd_requested=5.0`
  - `target_usd_resolved=5.0`
  - executable-price sizing
- `VERIFIED`: this packet does not reopen the old taker rescue disease.

## Suspect-Family Verdict Table
| Family | Verdict | Tribunal call |
| --- | --- | --- |
| normal taker runtime core | `KEEP AS STEEL` | no fresh raw runtime contradiction reopens the repaired core |
| report-side taker classification path | `KEEP AS STEEL` | current-code runs still classify normal taker submits/fills cleanly |
| global `latency_verifier` owner path | `KEEP AS STEEL` | remains the stronger lag owner path |
| taker-local `lag_*` config family as active primary authority | `RETIRE / FIRE` | current code seeds from global verifier first; local lag keys are shadow authority unless explicit override semantics are later reintroduced |
| no single canonical plain-event shot contract | `OPEN NEXT SURGERY FAMILY` | current plain events split target / route / oracle / visible-fill truth across surfaces |
| visible-fill witness weakness on plain events | `OPEN NEXT SURGERY FAMILY` | gate appears clean, but the rejecting event row is under-explained |
| `taker_mode_active` and `taker_lag_verified_token_count` as operator-authoritative gauges | `DEMOTE TO SUPPORT-ONLY` | raw tape and recent-window counters beat them on current evidence |
| taker recent-window submit/fill gauges | `KEEP AS STEEL` | coarse but still truthful enough as recent-activity witness surfaces |
| fresh shared watched inert substrate specimen as taker behavior proof | `DEMOTE TO SUPPORT-ONLY` | structural substrate truth only; not legal as taker shot-quality proof |

## Config-Authority Tribunal
### What current code does
- `VERIFIED`: executor seeds the verifier from the global
  `latency_verifier` block first.
- `VERIFIED`: taker-local lag keys are only read as fallback when the global
  keys are absent:
  - `lag_window_samples`
  - `lag_min_samples`
  - `lag_hit_threshold_ms`
  - `lag_min_median_ms`
  - `lag_min_hit_rate`

### Lag-config owner statement
- `VERIFIED`: global `latency_verifier` is the current real owner path.
- `INFERRED`: taker-local lag keys are shadow-authority residue unless a future
  packet intentionally reintroduces lane-local override semantics with explicit
  precedence.
- tribunal verdict:
  - `RETIRE / FIRE` from active primary meaning

## Canonical Shot-Contract Tribunal
### What current plain events actually show
- `VERIFIED`: current normal taker success chains split the contract across
  multiple plain events.
- on current successful specimens:
  - `taker_decision`
    - keeps requested/resolved target USD
    - keeps source/submit/complement token contract
    - keeps complement-route truth
    - keeps secondary-oracle status / confirmation
    - keeps visible-fill notional and ratio
    - drops `target_ref`
    - drops downstream order binding
  - `edge_evaluation`
    - keeps `target_ref`
    - keeps `lineage_stage`
    - keeps the gate verdict
    - drops requested/resolved target USD
    - drops visible-fill numbers
    - drops canonical route/source/submit token fields
  - `taker_submit`
    - keeps requested/resolved target USD
    - keeps source/submit/complement token contract
    - keeps complement-route truth
    - keeps downstream order binding
    - drops `target_ref`
    - drops secondary-oracle confirmation
    - drops visible-fill numbers
  - `order_submit` and `fill`
    - keep `target_ref`
    - keep downstream order binding
    - keep source/submit/complement token contract
    - keep complement-route truth
    - drop requested/resolved target USD
    - drop secondary-oracle confirmation
    - drop visible-fill numbers

### Shot-contract owner statement
- `VERIFIED`: no single current plain event owns the full normal-taker shot
  contract.
- `VERIFIED`: `taker_decision` is currently the strongest pre-submit witness
  surface, not `edge_evaluation`.
- `INFERRED`: the next hygiene slice should either promote `taker_decision`
  into the canonical pre-submit shot-contract owner by adding `target_ref`, or
  create an equivalent single-owner contract that preserves the richer
  `taker_decision` witness fields through `taker_submit`, `order_submit`, and
  `fill`.
- tribunal verdict:
  - `OPEN NEXT SURGERY FAMILY`

## Visible-Fill Witness Tribunal
### What current runtime says
- `VERIFIED`: visible-fill gating still behaves like real gate steel.
- `VERIFIED`: the fresh watched taker specimen `316e34b5-d291-4ade-82b8-04cf345a911c`
  produced a real visible-fill reject at:
  - `visible_fill_notional_usd=3.4018`
  - `visible_fill_ratio=0.68036`
  - `target_usd_requested=5.0`
  - `target_usd_resolved=5.0`
  - `secondary_oracle_confirmation=true`
- `VERIFIED`: those witness numbers live on `taker_decision`, not on the plain
  `edge_evaluation` row that claims `taker_visible_fill_ratio_below_min`.
- `VERIFIED`: the same plain `edge_evaluation` rows still do not carry:
  - `visible_fill_notional_usd`
  - `visible_fill_ratio`
  - resolved target denominator fields

### Visible-fill witness verdict
- `VERIFIED`: current evidence supports "runtime gate clean, `edge_evaluation`
  witness surface weak, `taker_decision` witness surface materially stronger."
- tribunal verdict:
  - `OPEN NEXT SURGERY FAMILY`

## Observer / Gauge Tribunal
### Current gauge behavior
- `VERIFIED`: recent-window activity gauges eventually reflect real taker
  submits/fills.
- `VERIFIED`: current successful shots still sit next to status rows where:
  - `gauge.taker_mode_active=0.0`
  - `gauge.taker_lag_verified_token_count=0.0`
  - while:
    - `gauge.taker_submitted_last_status_window=1.0`
    - `gauge.taker_fills_last_status_window=1.0`
- `VERIFIED`: the fresh watched taker specimen `316e34b5-d291-4ade-82b8-04cf345a911c`
  sharpened the weakness further:
  - raw runtime emitted `2` taker decisions, `1` taker submit, `1` taker fill,
    and a visible-fill reject
  - the surrounding status rows still stayed `null` on
    `taker_actions_last_status_window`, `taker_submitted_last_status_window`,
    `taker_fills_last_status_window`, `taker_mode_active`, and
    `taker_lag_verified_token_count`

### Observer truth verdict
- `VERIFIED`: recent-window gauges are the stronger operator witness surfaces.
- `VERIFIED`: `taker_mode_active` and `taker_lag_verified_token_count` are
  weaker/coarser than the raw tape and should not speak as primary operator
  truth.
- `INFERRED`: status-window sampling can still miss a short-lived taker shot
  entirely, so raw tape outranks status surfaces even when the preferred
  recent-window family is empty.
- tribunal verdict:
  - `recent-window gauges = KEEP AS STEEL`
  - `taker_mode_active` / `taker_lag_verified_token_count` = `DEMOTE TO SUPPORT-ONLY`

## Lag-Sample Accumulation Closure
- `VERIFIED`: the current raw runtime does not re-prove a taker lag-readiness
  failure family comparable to maker’s current mixed readiness chain.
- `UNKNOWN`: the exact sample-population path is not fully self-evident from a
  single plain runtime read without a dedicated wire trace.
- `INFERRED`: this remains a closure task for the taker hygiene packet, not a
  reason to reopen taker rescue surgery.

## Support / Report Boundary Tribunal
- `VERIFIED`: current taker report surfaces still classify current-code normal
  taker runs cleanly:
  - `true_unknown_submit_count=0.0`
  - `submit_without_competitiveness_payload_count=0.0`
  - `partial_competitiveness_payload_count=0.0`
- `VERIFIED`: no current fake-badge runtime boss like maker’s old prereq
  waterfall issue was re-proved on taker.
- `INFERRED`: the current report path is stronger than the plain-event contract
  and should stay fenced as consumer truth until the canonical shot contract is
  strengthened.
- support/report verdict:
  - `KEEP AS STEEL` for classification path
  - `OPEN NEXT SURGERY FAMILY` for plain-event contract strengthening

## Fresh Watched Specimen Verdict
- fresh watched run:
  - `316e34b5-d291-4ade-82b8-04cf345a911c`
- result:
  - real mixed maker+taker behavior specimen
  - `2` taker decisions
  - `1` taker visible-fill reject
  - `1` taker submit / fill chain
  - taker blocker mix:
    - `edge_below_min=60`
    - `market_probability_missing=2`
    - `taker_visible_fill_ratio_below_min=1`
    - `taker_window_already_submitted=1`
- operational meaning:
  - no new contradiction against current taker runtime steel
  - fresh raw proof strengthens the "support-authority / event-contract /
    witness-surface hygiene" call
  - the strongest fresh taker evidence now comes from this specimen, not the
    older inert watched substrate-only run

## Completion Outputs
Updated taker suspect-family table:
- `KEEP AS STEEL`
  - normal taker runtime core
  - global `latency_verifier` owner path
  - report-side taker classification path
  - recent-window submit/fill gauges
- `DEMOTE TO SUPPORT-ONLY`
  - `taker_mode_active`
  - `taker_lag_verified_token_count`
  - fresh inert substrate specimen as taker behavior proof
- `RETIRE / FIRE`
  - taker-local `lag_*` config family as active primary authority
- `OPEN NEXT SURGERY FAMILY`
  - canonical plain-event shot contract
  - visible-fill witness-surface repair
  - lag-sample ownership closure if needed for final hygiene closeout

Taker lag-config owner statement:
- global `latency_verifier` remains the owner; taker-local lag keys are
  shadow-authority residue unless explicit override semantics are later created.

Canonical taker shot-contract statement:
- no current plain event owns the full normal-taker contract; `taker_decision`
  is the strongest current witness surface, and the next hygiene slice should
  promote it or an equivalent single-owner contract into that role while
  preserving downstream order binding.

Visible-fill witness verdict:
- runtime gate looks clean; witness surface is weak.

Observer / gauge truth verdict:
- recent-window gauges stay stronger than instantaneous coarse mode / lag-count
  surfaces.

Support / report boundary verdict:
- current report classification path remains truthful enough to keep
  - but it must stay subordinate to runtime and should not have to carry the
    entire shot-contract burden forever

## Recommendation
- `VERIFIED`: a new taker rescue surgery packet is not justified on current
  evidence.
- `VERIFIED`: a bounded taker hygiene packet is justified.
- recommended next taker packet family:
  1. canonical plain-event shot-contract closure
  2. visible-fill witness-surface repair
  3. taker lag-config deauthority / cleanup
  4. gauge fencing for weak operator surfaces
