bot_name: "Bro-XRP-Paper-Docker"
mode: "paper"

asset:
  symbol: "XRP"
  chainlink_symbols: ["xrp/usd"]
  discovery_symbols: ["XRP"]

targets:
  token_ids: []
  discovery:
    enabled: true
    max_pairs: 7

sizing:
  mode: "notional"
  min_usd: 1
  max_usd: 20
  target_usd: 5
  rounding: "floor"
  price_source: "mid"
  share_step: 0.01
  exposure_cap_mode: "per_market_total"

storage:
  log_dir: "/logs/xrp_paper"
  state_path: "/data/xrp_paper/state.json"

runtime:
  guard_stop_file: "/logs/xrp_paper/guard_stop.txt"

security:
  allowed_storage_roots:
    - "/logs"
    - "/data"

ramp:
  reconcile_status_path: "/logs/xrp_paper/reconcile_latest.json"
