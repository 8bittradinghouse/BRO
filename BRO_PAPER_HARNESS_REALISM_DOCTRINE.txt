BRO PAPER HARNESS REALISM DOCTRINE (LOCKED)

Authority: BRO Canonical Control-Plane Doctrine
Scope: Paper harness realism semantics only

Semantic Alignment:
- `BRO_CANONICAL_DOCTRINE.txt` is the semantic root.
- `execution_realism_class` is the emitted paper-realism contract field.
- current emitted `execution_realism_class` values are:
  - `not_modeled`
- `harness_realism_grade_semantics=descriptive_non_gating`
- `harness_realism_grade_authority=non_authoritative`
- `paper_claim_boundary.source_truth_semantics=legacy_alias_of_action_source_truth`
  is a compatibility disclosure only; it is not a new authority class.

Emulation Anchor:
Canonical paper harness is BRO's emulation/proving lane for paper evidence.
It is not a generic simulator and must not be described as one in active operator or control surfaces.

Core Rule:
Paper harness artifacts must be truthful about what is authoritative and what is not modeled.
No implied venue-fidelity claims are allowed outside explicit modeled scope.

Paper Claim-Boundary Classes:
- authoritative
- not_modeled

Authoritative Claims (paper-valid):
- control-plane authority and gating
- lifecycle state transitions and standdown semantics
- canonical decision-path causality (observation -> decision -> action/no-action)
- run-scoped artifact truth and deterministic validator replay

Not Modeled Claims (paper-invalid):
- taker immediate execution using visible best-touch price and observed top-size bounds
- source degradation behavior outside fully authoritative observed-live truth
- stale-view risk signaling and degraded-path disclosure
- venue queue position and exact time-priority outcomes
- exact maker expectancy and full passive fill economics
- full transport/exchange race microstructure
- live PnL equivalence

Maker Policy Semantics:
- synthetic_touch_fill: disabled by canonical paper profile
- near_touch_fill: disabled by canonical paper profile
- background_fill: disabled by canonical paper profile
- queue_position_mode: not_modeled
- maker_realism_class: not_modeled

Taker Policy Semantics:
- price_basis: best_touch
- size_basis: observed_top_size
- latency_model:
  - none
  - lag_emulation
- lag_unknown_handling: fail_closed_no_penalty
- lag classes: within_window | below_window | above_window | unknown
- unknown lag class never applies inferred lag penalty
- stale_view_risk: disclosed_true
- taker_realism_class: not_modeled

Decision Input Truth Semantics:
Every decision surface must disclose decision_input_type:
- observed_live
- observed_other
- replayed
- emulated

If action uses anything except observed_live, disclosure is mandatory and auditable.
Action on emulated input is forbidden unless explicitly enabled for controlled scenarios.

Paper Claim Boundary Surface (required in audit output):
paper_claim_boundary = {
  control_plane_truth,
  lifecycle_truth,
  decision_source_truth,
  action_source_truth,
  maker_fill_expectancy,
  taker_fill_expectancy,
  live_pnl_equivalence
}

Non-Negotiable:
If realism class or input provenance is missing for a decision/fill path, the harness audit must fail closed.

Scientific Proving-Lane Discipline (paper-harness specific):
- one canonical paper proving path per evidence claim
- one controlled variable at a time unless the packet explicitly declares a multi-variable investigation
- evidence claims must carry proving lineage tuple:
  - run_id
  - git_commit
  - config_fingerprint
  - code_fingerprint
- if proving-lane semantics change, comparison claims must be downgraded explicitly

Realism Grade Boundary:
- harness_realism_grade is descriptive only
- harness_realism_grade is non-gating
- harness_realism_grade is non-authoritative
- harness_realism_grade must not be used as a substitute for:
  - finding_count
  - validator determinism
  - run integrity
  - promotion eligibility

Audit Surface Addendum (required):
- harness_realism_grade (0-100, non-gating, machine-readable)
- harness_realism_grade_breakdown:
  - tod_liquidity_scaling
  - maker_queue_proxy_depth_model
  - taker_depth_slippage_model
  - taker_lag_emulation_with_unknown_guard
  - truth_surface_completeness
- top-level proving-lineage surface:
  - run_id
  - git_commit
  - config_fingerprint_sha256
  - code_fingerprint_sha256

Ownership Rule:
- `paper_harness_audit` is the sole owner of top-level `harness_realism_grade`,
  `harness_realism_grade_breakdown`,
  `harness_realism_grade_semantics`, and
  `harness_realism_grade_authority`
- `nightly_soak_report` may report exercised-only realism, but it must do so
  under `exercised_harness_realism`
- current nightly/report/operator surfaces must not publish exercised-only
  realism under the canonical top-level `harness_realism_grade*` names
- archived nightly artifacts that still carry legacy top-level
  `harness_realism_grade*` may be normalized on read into
  `exercised_harness_realism`, but current emitters must not emit both

Source Purity Ownership:
- canonical hard-fail owner: `paper_harness_audit`
- canonical hard-fail question:
  did any action-row `edge_evaluation` record with
  `action_taken in {maker,taker}` use `book_source != ws`
- whole-stream `market_data_source.book_updates_rest_ratio` remains valid
  watch/descriptive truth, but it is not the canonical hard-fail owner for
  source purity
- `soak_hardening_gate` must not co-own this blocker lane through a parallel
  whole-stream REST-ratio hard fail
- source-purity pass means acted rows were WS-clean; it does not imply both
  maker and taker produced action rows in the same specimen

Nightly exercised-only surface:
- `exercised_harness_realism`
  - `grade`
  - `breakdown`
  - `semantics`
  - `authority`
